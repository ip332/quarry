- PR-046: Document the temporary AST-facing NamespaceBuilder and SemanticValidator overloads while preserving the production YAML frontend
- PR-047: Reassess parser/AST compatibility tests; parser/AST compatibility is parser/AST-only and does not require symbols or semantic validation
- PR-048: Reassess AST-derived symbols and semantic tests; classify them as temporary transition coverage, not production or long-term compatibility contracts
- PR-049: Retire the legacy AST-facing symbols and semantic compatibility path after migrating representation-neutral diagnostics and retiring unsupported AST-only behavior
- PR-050: Align architecture documentation with the post-PR-049 boundary: parser/AST is parser-owned compatibility infrastructure and downstream production stages consume normalized source schema
- PR-051: Tighten YAML/source-schema/frontend ownership by removing the YAML source-schema alias adapter; the decoder now names source-schema result types directly while frontend orchestration remains unchanged
- PR-052: Define supported compiler source-tree API boundaries: YamlCompiler is the high-level YAML source compiler entry point, Backend starts from validated Schema IR, lower-layer APIs remain internal building blocks, and no external install/export API is promised
- PR-053: Add the first source-tree schema compiler executable for one YAML input file, frontend diagnostics, backend generation, and safe generated-file writes without imports, multiple inputs, or install/export packaging
- PR-054: Retain the schema compiler tool's per-file generated-output replacement contract; document that multi-file writes are not transactional while preserving unrelated outputs and adding focused replacement coverage
- PR-055: Retain the current single-document YAML schema-unit boundary; document one namespace path and one primary record per input while adding focused cardinality contract coverage
- PR-056: Establish the first runtime serialization boundary with generated C++ encode functions for top-level scalar and non-negative enum fields backed by a schema-neutral binary-record runtime helper; decoding, strings, bytes, arrays, and nested records remain follow-up work
- PR-057: Add the first generated scalar decoding path by structurally parsing top-level Binary Record Format v0.1 records in `quarry_runtime` and materializing generated records for the PR-056 scalar/non-negative-enum subset; richer decode diagnostics, strings/bytes, arrays, nested records, and unknown-field preservation remain follow-up work
- PR-058: Add bounded `string` and `bytes` encode/decode support to generated C++ codecs using raw Field Directory-delimited bytes, UTF-8 validation for strings on encode and decode, byte-count bounds, owning materialization, and continued rejection of present arrays and nested records; bounded arrays are the next coherent runtime increment
- PR-059: Add generated encode/decode support for bounded arrays of fixed-width scalar and non-negative enum leaf types using a `varuint` element count plus tightly packed element bytes; arrays of strings, bytes, records, and nested arrays remain deferred, with variable-length leaf arrays the next coherent runtime increment
- PR-060: Add generated encode/decode support for bounded arrays of strings and bytes using a `varuint` element count plus per-element `varuint` byte lengths, preserving empty arrays versus empty elements, UTF-8 validation for string elements, arbitrary byte elements, and exact payload consumption; nested record fields are the next coherent runtime increment before arrays of records
- PR-061: Add generated encode/decode support for nested record fields by storing complete embedded BRF records as the parent field payload; the embedded record carries its own header, `record_id`, version, Field Directory, and payload, and generated parent codecs reuse the nested record's generated codecs. Arrays of records remain deferred; the next PR should compare `element_count` plus length-delimited complete embedded records against any compact homogeneous fragment optimization before choosing a wire rule.
- PR-062: Add generated encode/decode support for bounded arrays of record references using a `varuint` element count plus one `varuint` byte length and complete embedded BRF record per element; every child record carries and verifies its own `record_id`, version, flags, Field Directory, and payload. The homogeneous-envelope optimization is deferred to a possible BRF vNext after measured wire-size pressure; structured generated codec errors are the next coherent runtime increment.
- PR-063: Add structured generated codec errors without changing BRF v0.1 bytes. Generated diagnostic APIs now return runtime `EncodeResult`/`DecodeResult` values with distinct encode and decode error enums, while existing optional encode/decode APIs remain compatibility wrappers that discard error detail. Nested records and record arrays propagate child root errors directly; field path, array index, byte-offset context, and diagnostic strings remain deferred. The next coherent increment is BRF parser fuzzing and malformed-input hardening, with richer field-path context as a later diagnostics PR.
- PR-064: Add opt-in Clang/libFuzzer coverage for BRF parser hardening without changing BRF v0.1 bytes. The fuzz setup builds generic `parse_record` and representative generated-style decoder targets only when `QUARRY_BUILD_FUZZERS` is enabled, stores a small reviewable hex seed corpus, and adds focused malformed-input edge tests for varuint overflow, insufficient directory bytes, maximum range rejection, and zero-length field-range policy. Runtime install/export support is the next coherent packaging increment; richer field/path codec diagnostics remain a separate diagnostics follow-up.
- PR-065: Make the header-only C++ runtime installable as a standalone CMake package. Installation now exports `Quarry::runtime`, installs `QuarryConfig.cmake` and `QuarryConfigVersion.cmake`, supports `<quarry/runtime/binary_record.hpp>`, verifies an external consumer project under `tests/consumer/runtime_package`, and adds a minimal C++ example under `examples/cpp/basic_encode_decode`. Future language-specific examples should be introduced with their corresponding language runtime or generator support; compiler and CLI installation remain deferred.
- PR-066: Define the supported downstream distribution model after PR-065. The supported SDK boundary is runtime SDK only: install and export `Quarry::runtime`, public runtime headers, and CMake package metadata; treat compiler libraries, generated protobufs, tests, fuzzers, examples, and the schema compiler executable as source-tree or documentation artifacts until separate distribution decisions stabilize them. A future compiler-tool distribution PR should first define CLI packaging, dependency discovery, and generated-code helper semantics.
- PR-067: Define the schema compiler tool distribution contract and keep `quarry-schema-compiler` source-tree-only for now. The current CLI supports one YAML input, one output directory, stable basic exit-code classes, stderr diagnostics, and per-file replacement, but lacks installed-tool prerequisites such as `--version`, relocatability validation, dynamic dependency policy for libyaml/Protobuf/absl, same-release generated-code/runtime compatibility guards, host-tool discovery, and CMake output-enumeration semantics. The next coherent PR should stabilize CLI invocation behavior and version reporting before adding any install rule.
- PR-068: Stabilize the schema compiler CLI for future installation while keeping it source-tree-only. The tool now reports the CMake project version via `quarry-schema-compiler --version`, treats version as a terminal informational option, and has subprocess coverage for absolute input/output paths from unrelated working directories plus input, output, and working-directory paths containing spaces. The documented compatibility rule is same-release compiler, generated C++ code, and `Quarry::runtime` usage; mechanical version guards remain deferred. The next coherent PR should decide whether to add generated/runtime compatibility guards before installing the executable.
- PR-069: Add a narrow generated-code/runtime compatibility guard without changing BRF bytes or installing the schema compiler. The runtime now exposes `kGeneratedCodeApiVersion`, generated record headers assert the expected value at compile time, and package release version remains distinct from generated-code API compatibility, runtime ABI compatibility, schema-language compatibility, and BRF wire-format compatibility. The next coherent packaging PR should decide whether installed compiler discovery requires exact release matching or only generated-code API compatibility before adding executable install rules.
- PR-070: Define future installed schema compiler discovery and version policy without adding install/export logic. The selected native CMake discovery model is an imported executable target `Quarry::schema_compiler` in the existing package after a prior standalone executable install PR proves clean-prefix relocatability; same-release compiler/runtime usage remains the recommended pre-1.0 workflow, while generated-source compatibility is mechanically enforced only by `kGeneratedCodeApiVersion`. The next coherent PR should install only the standalone executable and verify clean-prefix execution without exporting compiler libraries or adding CMake generation helpers.
- PR-071: Install the standalone `quarry-schema-compiler` executable to the standard executable directory and verify clean-prefix execution without exporting compiler libraries, generated protobuf targets, package components, imported executable targets, or CMake generation helpers. The installed tool is validated by direct `--version`, `--help`, and representative schema compilation from paths containing spaces; third-party dynamic dependencies remain provided by the platform or package manager. The next coherent PR should expose `Quarry::schema_compiler` as an imported executable target from the existing package.
- PR-072: Expose the installed schema compiler through the existing Quarry CMake package as imported executable target `Quarry::schema_compiler`. The target resolves to the compiler from the selected package prefix and is validated by a downstream `add_custom_command()` build that generates C++, compiles it against `Quarry::runtime`, and runs it. No compiler libraries, generated protobuf targets, package components, host-tool overrides, or generation helper functions are exported. The next coherent PR should add a minimal documented downstream generation example before deciding whether a reusable CMake helper is warranted.
- PR-073: Publish the supported downstream CMake integration pattern as `examples/cpp/schema_compiler_cmake`, reuse it in the clean-prefix package test, and document caller-owned generated output enumeration, include setup, dependencies, and stale-output cleanup without adding helper functions.
- PR-074: Keep manual `add_custom_command()` integration as the supported downstream CMake API and defer `quarry_generate_cpp()` until generated-output enumeration, stale-output ownership, multi-schema behavior, and host-tool override semantics are specified. A future minimal helper should be custom-command-only, return generated files, avoid target mutation, and live in an explicit installed module if implemented.
- PR-075: Define the generated-output planning contract. Filename and include-path decisions remain backend-owned; the selected architecture is an internal `GenerationPlan` stage before rendering so future file writing, tests, CLI output queries, and CMake integration can share one authoritative output inventory without duplicating backend naming logic. No `--dry-run`, manifest, depfile, or CMake helper is added in this PR.
- PR-076: Introduce the internal backend `GenerationPlan` stage without changing generated filenames, generated contents, CLI behavior, package APIs, manifests, depfiles, or CMake helpers. Rendering now consumes planned output inventory, `Backend::generate` preserves its existing `CodegenResult` contract, and schema compiler file-writing safety remains tool-owned.
- PR-077: Add `quarry-schema-compiler --list-outputs` as a read-only output-inventory query backed by the backend `GenerationPlan`. The mode prints one generated path per line using normal output-directory path semantics, skips rendering and file writing, is available through the installed compiler, and does not add manifests, depfiles, stale-output cleanup, or a CMake helper.
- PR-078: Define the CMake configure-time output discovery contract without adding a helper. A future `quarry_generate_cpp()` is viable only for installed native builds: it should resolve `Quarry::schema_compiler` from the selected package, invoke `--list-outputs` during configuration with absolute schema/output paths, register schema and compiler configure dependencies, reject CMake-unsafe paths, declare returned outputs in one custom command, avoid target mutation and stale cleanup, and defer source-tree, cross-compilation, multi-config, host-tool override, manifest, depfile, and cleanup semantics.
- PR-079: Add installed-native `quarry_generate_cpp()` through `QuarryGenerate.cmake`, auto-included from the installed `Quarry` package. The helper resolves the imported `Quarry::schema_compiler`, invokes `--list-outputs` at configure time, declares the reported absolute outputs in one build-time generation command, returns `OUT_FILES`, registers schema/compiler configure dependencies, rejects unsafe paths and duplicate output claims, and leaves target mutation, runtime linking, stale cleanup, depfiles, manifests, source-tree helper use, cross-compilation, host-tool overrides, multi-schema orchestration, and build-time inventory consistency checks deferred.
- PR-080: Enable default build-time generated-output inventory verification in `quarry_generate_cpp()`. The helper reruns `--list-outputs` with the same resolved compiler, schema, output directory, root stem, and extension before normal generation, compares the ordered normalized absolute output list against the configure-time inventory, and fails before writing files if the build graph is stale. The check preserves `CMAKE_CONFIGURE_DEPENDS` as the normal reconfiguration path and still defers manifests, depfiles, stale-output cleanup, target mutation, source-tree helper use, cross-compilation, compiler overrides, and multi-schema orchestration.
- PR-081: Define the host-tool override and cross-compilation policy without changing helper behavior. The selected future contract is one explicit `SCHEMA_COMPILER <absolute-host-executable>` override for `quarry_generate_cpp()`: native builds keep using `Quarry::schema_compiler` by default, cross-compiling remains rejected unless the override is supplied, the chosen compiler path is reused for configure-time listing, build-time inventory verification, and generation, one compiler path is enforced per build tree, same-release host compiler/runtime usage remains recommended, and generated-code API compatibility remains enforced by generated headers rather than package-release equality. Imported target overrides, host-tools packages, package components, manifests, depfiles, source-tree helper support, stale cleanup, and multi-schema orchestration remain deferred.
- PR-082: Add the explicit `SCHEMA_COMPILER <absolute-host-executable>` override to `quarry_generate_cpp()`. Native builds still use `Quarry::schema_compiler` by default, native callers may provide an absolute override, cross-compiling builds require that override, the helper validates host-runnability with `--version`, reuses the selected path for configure-time listing, build-time inventory verification, and generation, and enforces one lexically normalized compiler path per CMake build tree. Exact package-release matching, `PATH` discovery, environment fallbacks, imported-target overrides, host-tools packages, source-tree helper support, stale cleanup, manifests, depfiles, and multi-schema orchestration remain deferred. The next coherent PR should decide whether compiler capability queries are needed before adding any richer host-tool/package integration.
- PR-083: Decide that a narrow `quarry-schema-compiler --print-generated-code-api-version` scalar query and `Quarry_GENERATED_CODE_API_VERSION` package metadata are justified for future configure-time host compiler/runtime compatibility checks, but defer implementation until the generated-code API version has one canonical source of truth. Today the runtime header defines `kGeneratedCodeApiVersion`, generated headers assert against a duplicated backend literal, and the package does not expose the value to CMake; generated-source compilation remains the mechanical compatibility boundary. The next coherent PR should single-source the generated-code API version across runtime headers, backend expectations, package metadata, and the future compiler query before wiring helper enforcement.
- PR-084: Single-source the generated-code API version through the validated top-level `QUARRY_GENERATED_CODE_API_VERSION` CMake scalar. The runtime `version.hpp`, backend generated-assertion expectation, and installed `Quarry_GENERATED_CODE_API_VERSION` package metadata now derive from that value, while generated output remains unchanged and the generated-header `static_assert` remains the mechanical compatibility boundary. The future `quarry-schema-compiler --print-generated-code-api-version` query and `quarry_generate_cpp()` configure-time compiler/runtime API comparison remain deferred to the next coherent PR.
- PR-085: Add the generated-code API query to `quarry-schema-compiler` and enforce exact compatibility in `quarry_generate_cpp()` during CMake configuration before output discovery. The helper now queries the selected compiler's generated-code API epoch, compares it with `Quarry_GENERATED_CODE_API_VERSION`, and fails early on mismatch; the query remains a narrow scalar contract distinct from `--version`, package release version, and BRF compatibility.
- PR-086: Root-cause the `debug-clang-tidy` "`google/protobuf/runtime_version.h` file not found" failure and add the Docker-based development environment (`Dockerfile`, `docker-compose.yml`) plus CI parity (`.github/workflows/ci.yml` now builds and runs both the `debug` and `debug-clang-tidy` presets inside that same image) as the fix. Root cause was local toolchain drift, not a CMake or clang-tidy integration defect: Ubuntu 24.04's apt Protobuf (3.21.12, matching CI) never generates an include of that header at all, and even where a newer Protobuf generator does emit it (observed with Homebrew Protobuf 35.1 on macOS), the failure additionally required a `clang-tidy` binary from a different toolchain install than the configured C++ compiler, so it lacked an implicit include search path the primary compiler had. No CMakeLists.txt, `.clang-tidy`, or generated-code change was made; see `docs/development-environment.md` for the full investigation and the generated-code lint policy (generated Protobuf sources remain linted, not `SKIP_LINTING`-excluded). Also fixed an unrelated, pre-existing case-sensitivity bug in `tests/consumer/schema_compiler_package_test.cpp` (`SchemaCompilerPackageTest.HelperReportsConfigureFailures` expected lowercase "permission denied" against CMake's actual "Permission denied") that the new `clang-tidy` CI job's sibling `debug` job would otherwise have exposed. The next coherent PR should decide whether native (non-Docker) contributor builds need any further guardrail against heterogeneous Protobuf/clang-tidy toolchain pairings, or whether Docker-only support for `debug-clang-tidy` is sufficient.
- PR-087: Resolve the PR-086 open question by defining an explicit, documentation-only clang-tidy environment support boundary: Docker/CI `debug` and `debug-clang-tidy` are authoritative and CI-equivalent, native `debug` remains supported when documented dependencies are installed, and native `debug-clang-tidy` is best-effort — a native-only clang-tidy include-resolution failure is not a project build defect unless it also reproduces in Docker/CI. No compiler-version check, Protobuf-version check, container/vendor/package-manager detection, injected clang-tidy include path, or generated-source lint exclusion was added, since none is both reliable and portable across host toolchain installations; `docs/development-environment.md` and `README.md` now state the policy and a reproduce-in-Docker-first troubleshooting sequence instead. `CMakeLists.txt`, `CMakePresets.json`, `.clang-tidy`, `Dockerfile`, `docker-compose.yml`, `.github/workflows/ci.yml`, and tests are unchanged. This closes the native-guardrail question opened in PR-086; reopen only if the failure reproduces in Docker/CI or a portable detection mechanism becomes available.
- PR-088: Rename the project from Breadcrumbs to Quarry. The project originated from an asset-tracking application where "breadcrumbs" (a location-trail metaphor) fit; development has since shifted to a schema language, compiler/code generator, and binary wire-format runtime built around working directly with a wired stream of bytes, which the old name no longer reflects. Quarry is used as a plain project name (no forced backronym, matching Avro/Thrift precedent) — the metaphor is extracting fixed, precisely-cut blocks from raw material, mapping onto compile-time-known, fixed-layout binary records extracted from a schema. This is a breaking, repository-wide identity rename with no compatibility aliases, forwarding headers, or duplicate executables retained: `breadcrumbs` C++ namespaces become `quarry`, `include/breadcrumbs/` becomes `include/quarry/`, `Breadcrumbs::runtime`/`Breadcrumbs::schema_compiler` become `Quarry::runtime`/`Quarry::schema_compiler`, `BreadcrumbsConfig/Targets/Generate.cmake` become `QuarryConfig/Targets/Generate.cmake`, `breadcrumbs_generate_cpp()` becomes `quarry_generate_cpp()`, `breadcrumbs-schema-compiler` becomes `quarry-schema-compiler`, and `proto/breadcrumbs/` (package `breadcrumbs.*`) becomes `proto/quarry/` (package `quarry.*`). `.brd` (documented as Binary Record Definition) and BRF (Binary Record Format) terminology, wire bytes, schema syntax, and all other architecture/behavior are unchanged. The generated-code API epoch (`kGeneratedCodeApiVersion`) stays at `1` rather than incrementing: the old package is fully retired with no coexistence, so the namespace/include-path change alone already makes old generated code fail to compile against the new runtime unconditionally, and `1` accurately represents the first tracked epoch of the new Quarry contract rather than false continuity with a retired Breadcrumbs lineage. Outstanding manual follow-up: the GitHub remote (`git@github.com:ip332/breadcrumbs.git`) has not been renamed and cannot be from local source changes; a stray `breadcrumbs-main.tgz` archive at the repo root is untracked and was left for the user to review/remove.
- PR-089: Audit the post-rename Quarry specifications and documentation against the current implementation without changing production behavior. Removed the obsolete claim that arrays of records are unsupported; corrected the YAML frontend status, CompilerContext contents, bounded-array support, Layout pass responsibilities, and compiler pipeline description; fixed an invalid logical record type in the schema-model example; completed CLI query/exit-code documentation; refreshed the documentation index; and removed stale post-rename repository guidance. Remaining occurrences of "Breadcrumbs" are limited to the intentional rename history and neutral legacy parser/AST test namespace fixtures. The BC diagnostic-ID prefix, the undefined QUARANTINED ownership state, and aspirational architecture-document tone remain separate follow-up decisions.
- PR-090: Resolve the ownership-model QUARANTINED state boundary flagged in PR-089. Decided that ownership state (administrative control, `ownership-model.md`) and device lifecycle state (trust/connectivity/security posture, `device-lifecycle.md`) are two independent, orthogonal state machines, and QUARANTINED is lifecycle-only with no ownership-state equivalent. Removed QUARANTINED from the Retirement Flow's ownership-transition list (it was never one of the document's own Ownership States) and added an explicit note in "Relationship to Device Lifecycle" stating the two state machines are independent so this ambiguity does not recur. No code exists yet for either state machine (`agent/`, `cloud/`, `protocol/` remain empty placeholders), so this is documentation-only with no behavior change.
- PR-091: Define structured nested codec diagnostic context for `EncodeResult<T>`/`DecodeResult<T>` without implementing it. `CodecResult<T, E>` will gain a `std::vector<PathElement> path` member (`PathElement` = `field_index` plus an optional `array_index` present only for array-element frames); leaf-detecting generated field encode/decode call sites attach the first single-element path directly at the point of failure, and each nesting level (record embedding, array element) appends its own frame as the error unwinds outward, so `path.front()` is always the innermost failure and `path.back()` the outermost. Byte-offset context remains a separate, undecided follow-up. This is a breaking change to a type generated code depends on and will require bumping `kGeneratedCodeApiVersion`. Documented in `runtime/README.md`; no runtime, backend, or test code changed in this revision. The next coherent PR should implement this exact shape end to end.
- PR-092: Implement the PR-091 structured nested codec diagnostic context end to end. `runtime/binary_record.hpp` adds `PathElement` and a `path` member on `CodecResult<T, E>`; `encode_failure`/`decode_failure` take an optional path (default empty). Every `render_*_field_encoding`/`render_*_field_decoding` site in `compiler/backend/backend.cpp` now attaches a path frame: leaf failures (bounds, UTF-8, unknown enum, unsupported type, structural read failures) attach a single field-level frame at the point of failure; array element loops (converted from range-based to indexed loops on the encode side to expose an element index) attach `array_index` alongside `field_index`; nested-record and record-array-element forwarding sites extend the child's existing path with their own frame and forward the child's error code unchanged; whole-field structural failures with no attributable element (count/length parsing before the loop, trailing-byte mismatches after it, post-loop builder rejection) attach a field-level frame with no `array_index`. `QUARRY_GENERATED_CODE_API_VERSION` bumped 1 → 2. All 12 affected `tests/fixtures/backend/*.txt` golden fixtures regenerated from actual (verified-correct) codegen output; `SchemaCompilerPackageTest.HelperRejectsCompilerRuntimeVersionMismatchBeforeListingOutputs`'s fake-compiler version updated (its hardcoded "2" coincided with the new real epoch, silently turning the mismatch it tests into a match) since it hardcoded a literal version rather than deriving from the build; added `BackendCodegenTest.GeneratedRecordArrayDecoderPropagatesNestedFieldAndArrayIndexPath`, an end-to-end decode test with a hand-crafted malformed array-of-records payload verifying a genuine 2-frame path (inner field failure plus outer array index), and its encode-side counterpart `GeneratedRecordArrayEncoderPropagatesNestedFieldAndArrayIndexPath` (a manually-built `SchemaIrModel` with an array-of-records whose element type has an enum field, exercising an out-of-range enum value set via the builder bypass) verifying the same 2-frame shape on the encode path. No BRF wire bytes changed; byte-offset context remains deferred.
- PR-093: Define byte-offset context for structured codec diagnostics without implementing it. Investigated every `EncodeError`/`DecodeError` origin in `runtime/binary_record.hpp` and `compiler/backend/backend.cpp` and found the runtime already computes the needed positional cursors (`directory_offset`, `entry.field_offset`, `element_offset`) internally at nearly every failure site, then discards them. Decided: `CodecResult<T, E>` gains a single `std::optional<std::uint64_t> byte_offset`, decode-only (every `EncodeError` is a schema-value violation with no meaningful wire position, and approximating one would require inverting the single-pass encode architecture), absolute from the top-level decoder's input span (translated across nesting levels the same way `path` already accumulates, rather than left as a per-level local offset the caller would have to reassemble), `uint64_t` rather than `size_t` to match existing Field Directory offset/length width and avoid embedded 32-bit truncation. Defined deterministic byte semantics for every error category (record-start for whole-record failures, directory-entry start for Field Directory failures, field-payload start for field-level failures, element/length-prefix start for array failures, first-unconsumed-byte for trailing array bytes) and flagged that `invalid_header` and `invalid_payload_length` each currently combine two independent checks into one error value, needing either a split or a documented tie-break. Documented in `runtime/README.md` and `compiler/backend/README.md`; no runtime, backend, test, or epoch change in this revision. The next coherent PR (PR-094) should implement this exact model end to end: `FieldView`/`ParseRecordResult` offset members, nested/array offset translation, and a `kGeneratedCodeApiVersion` bump.
- PR-094: Implement the PR-093 decode byte-offset model end to end. `CodecResult<T, E>` gains `std::optional<std::uint64_t> byte_offset`; `FieldView` gains `field_offset` and `ParseRecordResult` gains `offset`, both populated by `parse_record` at every structural failure site using the deterministic per-category semantics PR-093 defined. `invalid_header`'s combined reserved-byte check was split into two return sites (offsets 3 and 8); `invalid_payload_length`'s two checks were left combined, both reporting offset 12. Generated `render_*_field_decoding`/`render_array_field_decoding` sites attach `field->field_offset` at leaf failures, translate nested-record and record-array-element child offsets by addition (mirroring `path`'s propagation), and encode-side codegen is untouched since `EncodeResult.byte_offset` stays permanently `std::nullopt`. `QUARRY_GENERATED_CODE_API_VERSION` bumped 2 → 3. All 12 affected golden fixtures regenerated; `SchemaCompilerPackageTest.HelperRejectsCompilerRuntimeVersionMismatchBeforeListingOutputs`'s hardcoded real-epoch assertion updated again (2 → 3) since it hardcodes literals rather than deriving from the build — flagged as a recurring maintenance cost worth addressing if another epoch bump follows. Added comprehensive end-to-end tests: runtime-level tests for every top-level header, Field Directory, and field-range failure's exact offset (all verified against hand-computed wire-format arithmetic); generated-code tests for scalar field-payload offsets, a 3-level nested-record offset translation (`Outer`→`Middle`→`Inner`), record-array element offset translation (both a 2-level array+nested-field case and a direct array-element `unexpected_record_id` case), an array element-count varuint failure, and explicit `byte_offset == std::nullopt` assertions on real nested-record and record-array encode failures. No BRF wire bytes changed.
- PR-095: Remove the recurring hardcoded generated-code API epoch literals from `schema_compiler_package_test.cpp` flagged in PR-094. `schema_compiler_package_test` now receives `QUARRY_TEST_GENERATED_CODE_API_VERSION` from the canonical `QUARRY_GENERATED_CODE_API_VERSION` CMake scalar, matching the runtime/backend/tool test targets, guarded by the same `#ifndef`/`#error` convention. `HelperRejectsCompilerRuntimeVersionMismatchBeforeListingOutputs`'s two hardcoded literals (`"1"` fake compiler version, `"  3"`/`"  1"` stderr assertions) are replaced: the fake compiler now reports `QUARRY_TEST_GENERATED_CODE_API_VERSION + 1` (always different from the real epoch by construction), and both stderr assertions derive their expected substrings from the same macro. Verified the fix by temporarily bumping `QUARRY_GENERATED_CODE_API_VERSION` to 47, confirming the test passes unmodified, then reverting — proving future epoch bumps need no manual test edits. No other current-epoch literal duplicates found in the file. Package compatibility policy, `CodecResult`/path/byte-offset behavior, generated C++ output, and BRF wire bytes are unchanged; no epoch bump in this revision.
- PR-096: Verify `GenerationPlan` is the sole source of generated-output planning; documentation/cleanup only, no duplicated planning found. Traced every production path that computes generated filenames, output directories, include paths, or predicts generated artifacts: `Backend::plan()` (backing `--list-outputs`) and `Backend::generate()` (backing actual file writing) both call the same internal `build_render_generation_plan`, and both the CLI's `--list-outputs` printer and the actual file-writer share the single `output_path_for_planned_file()` function — confirmed by reading every call site of `file_stem_for_namespace`/`file_path_for_namespace`/`include_path_for_namespace`/`output_path_for_planned_file`. The CMake helper (`quarry_generate_cpp()`) never predicts filenames; it treats the compiler's `--list-outputs` output as authoritative. No stale comments, TODOs, or duplicated-planning code found anywhere. `tools/README.md` and `docs/schema-compiler-tool-distribution.md` already correctly documented this; `docs/compiler-architecture.md`'s existing paragraph was enhanced to name `GenerationPlan`/`build_render_generation_plan`/`output_path_for_planned_file` explicitly rather than describing the mechanism only abstractly, and `compiler/backend/README.md` (which never mentioned `GenerationPlan`, `Backend::plan()`, or `--list-outputs` at all) gained a new bullet documenting the same. No code, wire format, runtime behavior, or public API changed.
- PR-097: Define the supported boundary of generated-code helper functions in `compiler/backend/backend.cpp`, investigating rather than assuming cleanup was needed. Enumerated all 68 functions in the file and their call counts; every function has at least one real call site beyond its own definition. Found no dead or purely-forwarding helper: the two encode/decode dispatchers route to substantial (15-220 line) type-specific renderers, not thin wrappers; small lookup/utility helpers each centralize a genuine mapping reused at real sites; and the one apparent forwarding candidate (`include_path_for_namespace`, which currently computes the same value as `file_stem_for_namespace`) turned out to document a real semantic distinction at its call site (include-directive text vs. on-disk path, stored in different fields) and was kept. Per-record encode/decode definition "duplication" is inherent, not redundant, since encoding and decoding are different operations. Documentation-only: added an "Internal rendering helper architecture" section to `compiler/backend/README.md` recording these conclusions; no code removed or restructured. Flagged one adjacent, explicitly out-of-scope finding for a future PR: `render_array_field_encoding`/`render_array_field_decoding` each repeat similar per-element-type loop/error-emission boilerplate inline without a shared per-branch helper — real duplication, but under-abstraction rather than the dead/forwarding over-abstraction this investigation targeted, and correcting it would be a larger, separately-scoped mechanical rewrite.
- PR-098: Investigate the repeated array codec rendering structure flagged in PR-097, and factor the evidence-justified subset. Categorized duplication across `render_array_field_encoding`/`render_array_field_decoding` and the related single-value renderers: field-presence/lookup wrappers (`if (value.has_X())`, `if (const auto* field = find_field(...))`) are harmless one-line textual similarity, not worth a helper. The array element loop-open (identical across all 5 `render_array_field_encoding` branches) and the fresh-failure-return emission (`encode_failure<...>(ERROR, {{.field_index=N, .array_index=EXPR}})`, 12 call sites spanning every encode renderer) are genuine, safely-extractable duplication with fully explicit parameters — extracted as `render_array_encode_loop_open` and `render_encode_failure_return`. The analogous decode-side fresh-failure helper (~27 call sites, an added byte-offset-expression dimension that varies more intricately per site) and the nested-error-forward pattern (2 encode + 2 decode sites, propagates a child's path/error rather than raising a fresh one, structurally distinct from fresh-failure emission) were investigated, found equally evidence-backed, but deferred to a follow-up PR to keep this one mechanical and narrowly verifiable rather than risking a larger single-pass rewrite. Selected boundary: Option B (narrow helper extraction), scoped to the encode direction only, per the preferred implementation boundary. Rejected Option A (current structure is not merely superficially similar — real, safely-collapsible duplication exists) and Option C/D for this pass (no internal element-rendering model or larger redesign is justified; the narrow extraction fully addresses the highest-confidence subset). Generated fixtures: zero changes — `git diff -- tests/fixtures/backend` is empty, confirmed via full native and Docker/CI-equivalent rebuilds (21/21 tests pass both ways). Wire format, generated public API, generated symbol names, diagnostics, field paths, element indices, byte offsets, runtime APIs, package compatibility, and the generated-code API epoch are all unchanged — only `compiler/backend/backend.cpp`'s internal helper structure changed (142 lines touched, net 14 lines removed). Not committed or pushed, per instructions.
- PR-099: Investigate the decode-side fresh-failure-return emission deferred in PR-098. Enumerated every fresh decode failure site across `render_unsupported_present_field_decoding`, `render_scalar_field_decoding`, `render_enum_field_decoding`, `render_string_field_decoding`, `render_bytes_field_decoding`, `render_record_field_decoding` (builder-rejection site only), and `render_array_field_decoding` (13 fresh sites across the count check, record/string-bytes/fixed-width branches, and the trailing builder-rejection check) — 27 sites classified by error expression, field index, array-index expression, and byte-offset expression. Contrary to PR-098's "more intricately varying" concern, the byte-offset expression resolves to exactly 3 distinct textual forms (bare `field->field_offset`, `field->field_offset + element_offset`, `field->field_offset + element_length_start`), not 27 unique expressions — since the helper only emits whatever expression the caller supplies rather than computing it, this is fully tractable. Separately discovered and classified 2 additional fresh sites in `render_record_decoder_definition` (parse failure and record-ID mismatch, both before any field lookup) that use an empty path `{}` rather than `{{.field_index=N, .array_index=EXPR}}` — a genuinely different shape, correctly left unextracted rather than forced into the same helper, since only 2 sites share it. Selected Option B: introduced `render_decode_failure_return(stream, indent_level, record_name, error_expression, field_index, array_index_expression, offset_expression)`, replacing all 27 field-context fresh-failure sites; one helper (not Option C's two), since splitting by field-level vs. array-element would only save passing a literal `"std::nullopt"` string, no material simplification. Rejected Option A (real, safely-collapsible duplication exists, not merely superficial), Option D (the whole-statement helper strictly dominates a path-only helper, since the surrounding `return decode_failure<Name>(...)` wrapper is also fully duplicated), and Option E (the offset-expression enumeration proved the decode side is tractable, not too varied). Nested-child failure forwarding (2 sites: single-record decode, array-of-records decode) is untouched, as required — those sites propagate an existing child error/path/offset and don't fit the fresh-failure helper's shape. Generated fixtures: zero changes — `git diff -- tests/fixtures/backend` is empty, confirmed via full native and Docker/CI-equivalent rebuilds (21/21 tests pass both ways), with the PR-092 nested-path tests and PR-094 byte-offset tests explicitly re-run and confirmed passing. Wire format, diagnostics, error enums, path ordering, array indices, byte-offset values, nested-child forwarding, runtime APIs, public generated APIs, and the generated-code API epoch are all unchanged — only `compiler/backend/backend.cpp`'s internal helper structure changed. Not committed or pushed, per instructions.
- PR-100: Investigate whether `compiler/backend/backend.cpp` (2221 lines, one translation unit, per `compiler/CMakeLists.txt`'s `quarry_compiler_backend` target) represents one coherent implementation or several independent subsystems that should eventually be separated. Traced the complete implementation dependency graph: (1) Type Catalog (`collect_named_types`/`lookup_named_type`) — one recursive Schema-IR pass producing a flat id→C++-name/file-path/include-path map; (2) Namespace/Declaration Analysis (`analyze_namespace`/`lower_field_type`/`lower_runtime_field_encoding`/`order_declarations_topologically`) — consumes the Type Catalog, produces the `NamespacePlan` tree (C++ types, runtime codec strategy, topologically-ordered declarations); (3) File Planning (`collect_planned_files`/`validate_generation_plan`/`detect_file_cycles`/`to_public_generation_plan`/`build_render_generation_plan`) — consumes only `NamespacePlan`'s file/include metadata (never declaration/field content), produces `RenderGenerationPlan`/public `GenerationPlan`; `Backend::plan()` stops here; (4) Rendering (all `render_*` functions) — consumes one fully-resolved `NamespacePlan` node at a time, produces C++ text, has no dependency on the Type Catalog or file plan; by far the largest phase (~1100 of 2221 lines); (5) Output Assembly (`render_generation_plan`/`output_path_for_planned_file`/`Backend::generate()`) — glues the file plan to the renderer; `backend.cpp` never touches the filesystem itself (writing `GeneratedFile::content` to disk is `tools/schema_compiler`'s job). A cross-cutting family of small pure text/name-formatting helpers is shared across phases 1, 2, and 4. Data flow is strictly one-directional and acyclic (1→2→3, 2→4, 3+4→5); every phase communicates only through `TypeCatalog`/`NamespacePlan`/`RenderGenerationPlan`/`GeneratedFile`, never raw shared mutable state. Selected **Option B**: clear subsystem boundaries already exist, so a future split is documented but not performed. Proposed organization: `backend_analysis.cpp` (phases 1+2: catalog + namespace/field lowering), `backend_plan.cpp` (phase 3: file planning), `backend_render.cpp` (phase 4a: enum/record/builder declaration shape — `render_enum_definition`, `render_record_builder_definition`, field validation, `render_record_definition`, `render_namespace_file`), `backend_codegen.cpp` (phase 4b: encode/decode codec rendering — the two dispatchers, all field-kind renderers, the runtime-function-name lookup family, and the PR-097/098/099 emission helpers), `backend_output.cpp` (phase 5 + `Backend::plan`/`generate`), plus a new library-internal `backend_internal.hpp` (not installed, not part of the public API) declaring the ~15 currently-anonymous-namespace types (`NamedTypeInfo`, `TypeCatalog`, `FieldPlan`, `RecordPlan`, `DeclarationPlan`, `NamespacePlan`, `RenderGenerationPlan`, the `Runtime*Encoding` family, etc.) in a library-internal `detail` namespace so the split files can share them, and hosting the small cross-cutting formatting helpers. Safe migration order (smallest/lowest-risk first): (1) extract `backend_internal.hpp` with zero behavior change (pure declaration move, still all defined in `backend.cpp` initially); (2) `backend_output.cpp` (smallest, most self-contained); (3) `backend_plan.cpp`; (4) `backend_analysis.cpp`; (5) split phase 4 last, into `backend_render.cpp`/`backend_codegen.cpp`, since it is the largest and riskiest surface, using the same byte-for-byte golden-fixture verification discipline as PR-097/098/099. Rejected **Option A** (keep one TU) as the primary conclusion despite the file's strong internal cohesion, because the phase boundaries are genuinely clean and already evidenced by well-defined data-structure handoffs, not because cohesion is weak — Option A's own criterion ("splitting would create artificial interfaces without improving maintainability") is not clearly met, since the required shared internal header is a one-time, low-risk cost rather than an artificial interface with no benefit; however, no current pain point (compile time, merge conflicts, or contributor confusion) was found to justify acting now, so the split is deliberately deferred rather than performed. Rejected **Option C** (small independent move): no piece can be moved with "essentially no dependency impact" — every phase depends on the shared internal type family, so any move first requires the `backend_internal.hpp` extraction step; there is no zero-cost narrow win available. No code, generated output, wire format, runtime, diagnostics, public backend API, generated-code API epoch, or package compatibility changed — documentation-only (`compiler/backend/README.md` gained a new "Implementation architecture" section; this backlog entry; `REPORT.md`). `git diff --check` clean; no build/test run required since no compiler source changed. Not committed or pushed, per instructions.
- PR-101: Define the supported human-readable diagnostic boundary for Quarry codec errors. Enumerated all 20 `DecodeError` and 6 `EncodeError` values (including `none`); found `DecodeError::overflow` is declared but never actually produced by any current decode path in `runtime/binary_record.hpp` or `compiler/backend/backend.cpp` — an accurate, minor, out-of-scope finding, not itself a diagnostic-boundary question. For every error value, the existing structured context (enum + `path` + decode-only `byte_offset`) is sufficient: on encode, the caller already holds the offending value themselves (every `EncodeError` is a schema-value violation against an already-known value); on decode, the caller retains the full input span, `byte_offset` locates the exact byte, and the publicly documented BRF wire format plus the runtime's own public read primitives let them reconstruct any further "expected vs. actual" detail themselves. Searched exhaustively for actual demand rather than inferring it from documentation: every current consumer (`tests/runtime/binary_record_test.cpp`, `tests/backend/backend_codegen_test.cpp`, `fuzz/*`) compares `.error`/`.path`/`.byte_offset` as pure structured data and never formats them into text; neither canonical example (`examples/cpp/basic_encode_decode`, `examples/cpp/schema_compiler_cmake`) does any error formatting at all; `docs/specifications/binary-record-format.md`'s Validation Rules mandate what must be validated, never message text. Zero demonstrated demand found anywhere. Selected **Option A**: `CodecResult` remains structured-only (error enum, path, array index, decode byte offset); applications own all presentation/formatting. Rejected Option B (allocation-free enum-name mapping) despite being genuinely trivial to implement, since the required bar was "overwhelmingly justified," not merely cheap — any shipped string (even a name) becomes something callers can mistakenly treat as stable/matchable, invites reasonable localization expectations this project has never signaled intent to support, and is trivially replicable by any caller in their own downstream code over this repository's fixed, documented enum set. Rejected Option C (schema-neutral full formatter) for the same lack-of-demand reason, despite being cheap as an opt-in, not-stored function. Rejected Option D (generated schema-aware formatting) as introducing exactly the reflection surface (field/record name tables in generated code) that this project's "no runtime schema interpretation in production" principle exists to avoid, with no demonstrated need driving it. Rejected Option E (store a string in every `CodecResult`) as the clear worst option evaluated: mandatory heap allocation on every result including the overwhelmingly common success case, direct conflict with the embedded-first/exception-free design, another breaking `CodecResult` shape change and `kGeneratedCodeApiVersion` bump, generated string-literal duplication across every record, and a new failure mode (allocation failure) on the path whose entire purpose is reporting failure. Documentation updated to state this is a closed decision, not a "not yet": `runtime/README.md` gained a new "Diagnostic String Boundary" section (with a full rationale) and updated its "Out of scope" bullet; `compiler/backend/README.md` updated its matching bullet and fixed an unrelated stale line that still claimed field-path/byte-offset codec diagnostics were "out of scope for this PR" despite PR-091 through PR-094 having fully implemented both. No code, generated output, wire format, runtime behavior, public API, or generated-code API epoch changed — documentation-only. `git diff --check` clean; no build/test run required since no compiler or runtime source changed; `tests/fixtures/backend` untouched (confirmed empty diff). Not committed or pushed, per instructions.
- PR-102: Repository-wide architectural consistency audit. Read all 19 `README.md` files under `compiler/`, `runtime/`, `tools/`, `docs/`; grepped the whole repository for `TODO`/`FIXME`/`deferred`/`future`/`eventually`/`later`/`temporary`/`compatibility`/`legacy`/`obsolete` (zero `TODO`/`FIXME` found in any source file); reviewed every `jira/backlog.md` entry (56 entries, PR-046 through PR-101 — no PR-033 through PR-045 entries exist in this file). Checked explicitly for dead code-level architecture: confirmed `quarry_compiler_ast`/`quarry_compiler_parser` are linked only by their own dedicated tests (`ast_test`, `parser_smoke_test`, `lexer_test`) with zero other consumers — but this is already accurately and consistently documented everywhere as an intentional, previously-decided state (PR-047 through PR-050), not fresh architectural debt. `compiler/backend/` was already thoroughly re-audited by PR-097 through PR-101 (no dead/forwarding helpers, no duplicated rendering paths beyond the evidence-backed subset already factored, clean phase boundaries, closed diagnostic-string decision) and found nothing further. No dead enum values requiring action (`DecodeError::overflow`'s unreachability was already noted as an accurate, out-of-scope observation in PR-101), no duplicate representations of the same concept, and no genuine multiple-sources-of-truth were found at the code level. Selected **Option A**: the repository is architecturally consistent at the code level; documentation updates were needed and applied. Six confirmed, evidence-backed documentation inaccuracies fixed: (1) `docs/README.md` and `docs/specifications/README.md` both independently listed "record envelope layout" (and `docs/README.md` additionally listed "schema language syntax") under "Planned/Future Specifications," while each file's own "Current Specifications" list — a few lines above — already cites `binary-record-format.md`/`schema-language.md` as covering exactly those topics; the clearest, duplicated instance of "documentation promising future work that repository evidence no longer supports." (2) `CLAUDE.md` described `CompilerContext` as owning "options, `recordId` allocation state" — verified false against `compiler/context/compiler_context.hpp`, which owns only `SourceManager`, `FileSystem`, `DiagnosticEngine`; `recordId`/`fieldIndex` allocation is `layout/`'s responsibility. (3) `compiler/layout/README.md` claimed the layer computes "sparse record layout metadata" — verified false against the actual `LayoutModel`/`RecordLayout`/`FieldLayout` structs (only `fqn`, `record_id`, `field_index`) and contradicted by the same document's own later "Current layout behavior" section; the same category of over-claim PR-097 already fixed in `docs/compiler-passes.md`/`docs/compiler-architecture.md` but missed in this file. (4) `compiler/ast/README.md` opened with "the future parser" — stale, contradicted by the rest of the same document's present-tense description of the legacy parser as already existing. (5) `compiler/yaml/README.md` opened by framing itself as "a migration step toward the normative YAML format" — stale, contradicted by its own later "Migration Status" section. (6) `compiler/support/README.md` said `FileSystem` is used by "compiler context and future file-loading boundaries" — stale; `tools/schema_compiler/main.cpp` already implements exactly this boundary via `FileSystem::read_text_file`/`SourceManager::add_source`. No architectural debt ranking above the deferred backend split documented in PR-100 was found. `git diff --check` clean; no code changed, no build/test run required. Not committed or pushed, per instructions.
- PR-103: 1.0 release readiness assessment. Read `README.md`, `CLAUDE.md`, `docs/`, `runtime/`, `compiler/`, `tools/`, `examples/`, `cmake/`, `tests/`, `jira/backlog.md`; performed a hands-on end-to-end downstream-consumer test (fresh `cmake --install` to a clean prefix, inspected the actual installed file tree) rather than relying on documentation claims alone. Confirmed `agent/`, `cloud/`, `protocol/`, `sdk/` remain empty placeholders and `project(quarry VERSION 0.1.0 ...)` plus README's own "Project planning and architecture phase" status line already transparently disclose that the full platform vision is unimplemented — not a hidden gap, not ranked as a blocker. **Blocker #1 (highest-ranked)**: the installed package exposes a third, undocumented, genericly-named header path. `runtime/README.md` documents `#include <quarry/runtime/binary_record.hpp>` as the supported contract, and `include/quarry/runtime/binary_record.hpp` is indeed installed — but it is only a 1-line forwarder (`#include "runtime/binary_record.hpp"`) to the *real* implementation, which is separately installed, unprefixed, at `include/runtime/binary_record.hpp` (via `CMakeLists.txt:74-78`'s `install(FILES runtime/binary_record.hpp ...)` rule) — a generic, collision-prone top-level name for any package manager's shared include tree. Root cause: `compiler/backend/backend.cpp:844` hardcodes `plan.includes.insert("runtime/binary_record.hpp")`, so every piece of *generated* code — the primary, intended consumer of the runtime — emits the undocumented unprefixed include, not the documented namespaced one; confirmed `tests/consumer/schema_compiler_package_test.cpp` already exercises exactly this path (generated code compiled against an installed package), so the undocumented path is load-bearing today, not dead. Even `docs/distribution-model.md`'s own SDK-surface table only lists `runtime/binary_record.hpp` and `include/quarry/runtime/binary_record.hpp` as if they were the same thing in two locations, without disclosing the third, separately-installed unprefixed copy. A correct fix requires changing `compiler/backend/backend.cpp`'s emitted include string, regenerating all golden fixtures, updating the `CMakeLists.txt` install rules, and reconciling which physical file is canonical (`runtime/binary_record.hpp` at the repo root vs. `include/quarry/runtime/binary_record.hpp`) — multi-file, codegen-touching work, not a small well-defined fix suitable for this investigation-first PR. **Blocker #2 (moderate)**: neither canonical example (`examples/cpp/basic_encode_decode`, `examples/cpp/schema_compiler_cmake`) demonstrates encode/decode failure handling (inspecting `.error`, `.path`, `.byte_offset`) despite that being a deliberately-built, differentiated feature across PR-091 through PR-101; both exercise only the success path. Doc/keyword sweep for `future`/`planned`/`experimental`/etc. found nothing beyond what PR-102 already fixed. Selected **Option C**: several release blockers remain (ranked above), none implemented; recommended roadmap is to address Blocker #1 in its own dedicated PR (backend codegen change + fixture regeneration + CMake install-rule cleanup + physical header consolidation, using the same byte-for-byte verification discipline as PR-097 through PR-099) before considering a 1.0 tag, and to add a failure-handling example (Blocker #2) as a smaller follow-up. No code changed in this PR. `git diff --check` clean. Not committed or pushed, per instructions.
- PR-104: Unify the runtime public include contract, resolving PR-103's Blocker #1. Verified the investigation before trusting it: `compiler/backend/backend.cpp:844` hardcodes the unprefixed `plan.includes.insert("runtime/binary_record.hpp")` for every generated file; `include/quarry/runtime/binary_record.hpp` was only a 1-line forwarder to the real, unprefixed `runtime/binary_record.hpp`; confirmed via full-repo grep that only in-tree, source-relative consumers (`tests/runtime/binary_record_test.cpp`, `fuzz/*`, the forwarder itself) ever used the unprefixed form, and every genuinely public-facing consumer (README, `runtime/README.md`, `tests/consumer/runtime_package/project/main.cpp`, `examples/cpp/basic_encode_decode`) already used the namespaced form — zero external or documented commitment to the unprefixed path. Selected **Option A**: made `quarry/runtime/binary_record.hpp`/`quarry/runtime/version.hpp` the single canonical public interface, with no compatibility forwarding retained (pre-1.0, zero git tags, zero documented external users of the unprefixed path — safe to remove cleanly rather than preserve a needless duplicate). Rejected Option B (unprefixed canonical) since it's the undocumented, generic, collision-prone path — exactly the opposite of what a namespaced 1.0 package should expose. Rejected Option C (retain forwarding) since "do not preserve duplicate public interfaces without a strong compatibility reason" and no such reason was found. Implementation: physically relocated the real header content from `runtime/binary_record.hpp`/`runtime/version.hpp.in` into `include/quarry/runtime/` (via `git mv`, preserving history) rather than leaving a forwarder in either direction; updated `compiler/backend/backend.cpp`'s emitted include string; updated `CMakeLists.txt`'s `configure_file()` source/destination and install rules (removed the old `install(FILES runtime/binary_record.hpp ...)` rule entirely, added `PATTERN "*.in" EXCLUDE` to the `install(DIRECTORY include/quarry ...)` rule so the checked-in `version.hpp.in` template is never installed verbatim); removed the now-unnecessary repo-root `BUILD_INTERFACE` entry from `runtime/CMakeLists.txt`; updated the 4 remaining in-tree consumers (`tests/runtime/binary_record_test.cpp`, 3 `fuzz/*` files) to the canonical include; regenerated all 12 golden fixtures (single-line, purely mechanical `#include` text change, confirmed via diff before applying); fixed two hardcoded compile-command include-path lists in `tests/backend/backend_codegen_test.cpp` (added the relocated `include/` directory) and one hardcoded literal `#include "runtime/binary_record.hpp"` string embedded in `RuntimeGeneratedCodeApiVersionMismatchFailsCompilation`'s synthetic test source (missed by an initial repo-wide grep because the literal was backslash-escaped); updated `docs/distribution-model.md`'s SDK-surface table, `compiler/backend/README.md`, `runtime/README.md` (new note confirming the single-canonical-path decision), `CLAUDE.md`, and `docs/schema-compiler-tool-distribution.md` (3 stale mentions of the old relative path). Added a regression-guard assertion to `tests/consumer/runtime_package_test.cpp` verifying the installed tree contains `include/quarry/runtime/{binary_record.hpp,version.hpp}` and does **not** contain an `include/runtime/` directory — directly closing the testing gap PR-103 identified (no test previously asserted on the installed file layout, which is exactly why the defect had no regression coverage). No `kGeneratedCodeApiVersion` bump: `CodecResult`'s shape and all runtime behavior are unchanged; only which `#include` string generated code emits changed, which is not a generated-code/runtime compatibility concern. Backward compatibility: safe to remove without forwarding since the project has never tagged a release (0.1.0, zero git tags) and the investigation found zero external consumers depending on the unprefixed path. Validated via `cmake --preset debug`/`cmake --build`/`ctest --preset debug` (21/21 pass), Docker/CI-equivalent rebuild, and a from-scratch hands-on clean install to a throwaway prefix verifying both `examples/cpp/basic_encode_decode` and `examples/cpp/schema_compiler_cmake` configure, build, and run successfully against the installed package using the canonical include path (generated header confirmed to emit `#include "quarry/runtime/binary_record.hpp"`). `git diff --check` clean. Committed, not pushed, per instructions.
- PR-105: Demonstrate structured codec error handling, resolving PR-103's Blocker #2. Investigated before implementing: `CodecResult<T, E>`'s public contract (`.value`/`.error`/`.path`/`.byte_offset`) is already fully and accurately documented in `runtime/README.md` ("Codec Diagnostic Context", "Byte-Offset Context", "Diagnostic String Boundary" — all PR-091 through PR-101 work), so the gap was purely that no example exercised it, not a documentation or API defect — confirmed the API needed no change. Traced exactly which functions expose the full diagnostic triad: the low-level runtime primitives used by `examples/cpp/basic_encode_decode` (`parse_record`, `read_u32`, etc.) return `ParseRecordResult`/`DecodeValueResult<T>`, which carry an error (and, for `parse_record`, a bare `offset`) but never a `path` — `path` is a generated-code-level concept, built by `decode_<Record>_result` functions that know their own `field_index`. Only `examples/cpp/schema_compiler_cmake`, which uses generated code, can meaningfully demonstrate `.path`. Selected **Option B**: extend `schema_compiler_cmake`'s `main.cpp` rather than extending `basic_encode_decode` (Option A, rejected — the raw runtime primitives it uses structurally cannot produce a populated `path`) or adding a dedicated `error_handling` example (Option C, rejected — would duplicate the schema/CMake setup `schema_compiler_cmake` already has, and this project's distribution model deliberately keeps the example surface minimal). Implementation: after the existing happy-path round trip, `main.cpp` now calls `decode_Sample_result` (the `DecodeResult`-returning function, not the optional-collapsing `decode_Sample`) against two genuinely corrupted/truncated copies of the actually-encoded bytes (not hand-built `CodecResult` objects) — verified byte-for-byte by compiling and running a standalone prototype against the real runtime and generated headers before finalizing: (1) truncating to 4 bytes produces `DecodeError::truncated_header` with an empty `path` and `byte_offset == 0`, demonstrating that whole-record structural failures are detected before any field is examined, so there is no field context to report; (2) flipping the Field Directory's field-0 length byte (offset 18: 16-byte header + 1-byte field_index + 1-byte field_offset varuint) from 4 to 3 leaves the record otherwise structurally valid and produces `DecodeError::invalid_field_length` with `path == [{field_index=0}]` and `byte_offset == 19`, demonstrating a field-level failure. A small local `decode_error_name()` switch in the example prints enum names for its own diagnostic output — deliberately not a runtime API addition, but exactly the "trivially write their own small mapping" pattern `runtime/README.md`'s "Diagnostic String Boundary" section already documents as the intended answer for callers who want presentation text, reinforcing rather than reopening that closed decision. Updated `runtime/README.md` (a pointer to the example plus a correction: its "Diagnostic String Boundary" section previously stated no example in the repository ever formats a `CodecResult` into text, which this PR makes no longer true — reworded to note the new example's formatting is itself evidence supporting the no-runtime-formatter conclusion, not a reversal of it), `examples/cpp/schema_compiler_cmake/README.md`, `examples/cpp/basic_encode_decode/README.md` (cross-reference explaining why that example cannot demonstrate `path`), and `README.md`. Added a regression-guard assertion to the existing `SchemaCompilerPackageTest.ImportedExecutableTargetGeneratesDownstreamCode` test (`tests/consumer/schema_compiler_package_test.cpp`), which already builds and runs this exact example against a fresh install: it now also asserts the executable's stdout contains both demonstrated error names, the empty-path explanation, the populated-path field index, and the expected byte offset — no new dedicated test file needed, since this existing test already gives the new example lines real CI coverage. No runtime, backend, or generated-code change; no `kGeneratedCodeApiVersion` bump. `cmake --preset debug`/`cmake --build --preset debug --parallel`/`ctest --preset debug --output-on-failure` all pass; `git diff --check` clean. Committed, not pushed, per instructions.
- PR-106: C backend architecture and API investigation — investigation-first, no generator, no runtime, no user-visible behavior change. Read `compiler/backend/backend.cpp` in full (re-confirming and extending the PR-100 phase analysis): only phase 1 (Type Catalog) is language-neutral — phases 2+ are C++-specific by construction, not by current default (`FieldPlan`/`RuntimeFieldEncoding`/`NamedTypeInfo` carry a member literally named `cpp_type`; `lower_field_type` maps Schema IR primitives directly to C++ type spellings; the Rendering phase emits C++ text directly with no intermediate language-neutral representation). Traced `backend.hpp`'s `GeneratedLanguage`/`GeneratedArtifactRole` enums (`{Cpp}`/`{Header}`, one value each) and confirmed via full use-site search they are forward-looking scaffolding, not an active dispatch mechanism — nothing ever produces a second value. Confirmed `proto/quarry/schema_ir.proto` is already fully language-neutral (generic proto3 primitive-type enum, no C++-specific concept anywhere), satisfying `docs/backend-api.md`'s existing constraint that "adding a backend does not require changes to compiler passes." Found and read an existing, previously-written aspirational document, `docs/architecture/language-generators.md`, that already specifies the C namespace-mapping convention this investigation would have otherwise had to invent from scratch (verbatim: "C: Symbol prefix," worked example `quarry.telemetry` -> `quarry_telemetry_`) — this proposal's naming convention is the concrete instantiation of that document, not a new invention. Confirmed via `docs/specifications/schema-language.md` that `string`/`bytes` `max_bytes` and array `max_elements` are mandatory, positive, and already enforced by semantic validation today — meaning worst-case encoded/decoded record size is already statically computable at schema-compile time, recursively (nesting cycles are already rejected by the backend), which resolved the memory-ownership question directly from existing repository evidence rather than requiring a prototype. Selected architecture (full rationale in the new `docs/design/c-backend.md`): (1) **Public API** — generated plain C structs with fixed-capacity inline storage and generated setter/accessor free functions, not opaque handles (rejected: implies heap allocation/hidden arena, conflicts with `docs/principles.md`'s embedded-first "limited heap allocation," blocks static/stack/memory-mapped placement) and not a literal builder-type port (rejected: C has no access control, so a distinct builder type enforces nothing a plain struct doesn't already allow) and not hand-written caller structs with only generated codec functions (rejected: makes hand-written code the source of truth for record shape). (2) **Memory ownership** — fixed-capacity, no-heap storage sized from schema bounds (selected, a strictly stronger embedded-first property than the C++ backend's own `std::string`/`std::vector`-based model); zero-copy/view-based decode investigated and explicitly not adopted as the default (a legitimate future direction, but the existing C++ backend already treats "generated read/view APIs" and "caller-provided output buffers" as out of scope for both languages today, and it is a larger, separate design question with its own lifetime/aliasing concerns). (3) **Runtime split** — unchanged in spirit from C++ (generic byte mechanics and generic diagnostic containers in a shared header-only C runtime; schema-specific structs/dispatch/bounds-checks in generated code), matching `docs/principles.md`'s already-language-neutral "Compile-Time Knowledge" principle. (4) **Error model** — one generated, non-generic per-record decode/encode-result struct (C has no templates) embedding a shared, fixed-capacity `quarry_codec_path_t` and the existing error enums by value: full feature parity with `CodecResult` (error/path/byte-offset), not a reduced subset; diagnostic strings remain out of scope for the same reasons PR-101 already closed that question for C++, since that reasoning was never C++-specific. (5) **File layout** — generated `.h`/`.c` pairs (the one deliberate divergence from the C++ backend's header-only approach, justified by weaker `static inline` portability across embedded toolchains and worse per-translation-unit cost at typical embedded-firmware scale), while the shared runtime itself stays header-only like the C++ runtime. (6) **Backend architecture** — a new, independent sibling library `compiler/backend_c/` (own CMake target, depends only on `compiler/schema_ir` and `compiler/support`, zero dependency on `compiler/backend`), not a language switch threaded through the existing `Backend` class, since no phase past the Type Catalog has reusable *code* for another language, only a reusable *methodology*. (7) **Build integration** — a `--language {cpp,c}` flag on the existing single `quarry-schema-compiler` binary (not a second installed executable), a parallel `quarry_generate_c()` CMake helper (the existing `QuarryGenerate.cmake` machinery is already language-agnostic about its `OUT_FILES` shape), and a second, independent generated-code API epoch scalar `QUARRY_GENERATED_CODE_API_VERSION_C` (not reusing the existing C++ scalar, to avoid forcing artificial version bumps to one language whenever only the other changes). (8) **Wire compatibility** — confirmed BRF needs zero changes: the format is already a plain byte-level layout (fixed header, Field Directory, LEB128 varuint, big-endian scalars) with no C++-specific assumption anywhere; no beneficial-but-deferred change was found, and none should be made before 1.0 without a concrete, demonstrated blocker. (9) **Scope/roadmap** — skeleton -> scalars -> enums -> strings -> bytes -> arrays -> nested records -> arrays of records -> diagnostics -> package tests -> example, matching, feature-for-feature, the sequence the C++ backend itself was actually built in per this file's own PR history. Produced `docs/design/c-backend.md` (new design-proposal document, indexed into `docs/README.md`'s "Backend Design" reading-order section and documentation-structure tree) and this backlog entry. No prototype code was written: every open question was resolved from existing repository evidence (schema bound requirements, existing backend structure, existing packaging model), consistent with the task's instruction to avoid implementation "unless the investigation proves that a very small prototype is necessary to validate a design decision." No compiler, runtime, generated-output, test, or example source changed. `cmake --preset debug`/`cmake --build --preset debug --parallel`/`ctest --preset debug --output-on-failure` (21/21 pass, unchanged since no production code changed)/`git diff --check` all clean. Committed, not pushed, per instructions.
- PR-107: Implement the first production increment of the C backend -- an architectural skeleton, not serialization. Purpose: prove the compiler architecture cleanly supports an independent C backend, per docs/design/c-backend.md and PR-106's investigation. Verified PR-106's conclusions against the current repository before writing any code (`git log` confirmed nothing touched `compiler/backend/`, `compiler/schema_ir/`, or `tools/schema_compiler/` since PR-104, well before PR-106): re-confirmed `backend.cpp`'s `cpp_type`-named fields (33 occurrences), the vestigial `GeneratedLanguage`/`GeneratedArtifactRole` enums, and the exact CLI parsing structure in `tools/schema_compiler/main.cpp` -- no contradiction found, nothing to document as a deviation. Implemented `compiler/backend_c/` as a new, independent CMake target (`quarry_compiler_backend_c`) depending only on `quarry_compiler_schema_ir` and `quarry_compiler_support`, with zero dependency on `quarry_compiler_backend` (the C++ backend) -- its own `CodegenOptions`/`GeneratedFile`/`PlannedGeneratedFile`/`GenerationPlan`/`PlanResult`/`CodegenResult`/`Backend` types, independently implemented rather than shared. `PlannedGeneratedFile` carries both `relative_header_path` and `relative_source_path` together, so the planner always knows both and nothing infers one from the other; `Backend::plan()` and `Backend::generate()` share one internal `build_generation_plan()` so the two modes cannot diverge, mirroring the discipline `compiler/backend/backend.cpp` documents for C++. Namespace FQN -> C symbol prefix (`quarry.telemetry` -> `quarry_telemetry_`) matches `docs/architecture/language-generators.md`'s existing specification exactly. Enums render as a plain C `enum { ... }` block with fully uppercase, namespace-prefixed constant names (`QUARRY_TELEMETRY_STATUS_OK`); enum values outside the 32-bit signed integer range fail generation with a diagnostic naming the enum and value, since a plain C `enum`'s underlying type is implementation-defined and this skeleton does not yet commit to a wider guaranteed-width representation. Records with **zero** fields render as an empty-shell `typedef struct { uint8_t _reserved; } quarry_<ns>_<Record>_t;` (the `_reserved` byte exists only because ISO C forbids an empty struct body, documented as internal, not a schema field) plus a `quarry_<ns>_<Record>_init()` function; **any record declaring one or more fields fails generation with a diagnostic naming the record and its field count** rather than silently emitting a struct that drops declared fields -- no field type is supported at any stage of this skeleton, consistent with "Do not emit partial code." Generated headers use traditional `#ifndef`/`#define`/`#endif` include guards (not `#pragma once`, matching the embedded/portable-toolchain audience `docs/design/c-backend.md` targets), `#include <stdint.h>`, and `#ifdef __cplusplus extern "C" { ... }` guards; generated `.c` files include their own header plus `<string.h>` only when at least one record exists (for `memset` in `_init()`). Declares **no** encode/decode API of any kind and introduces **no** C runtime: the generated skeleton calls no runtime primitive and needs none, so adding a runtime or a generated-code API compatibility epoch now would have been exactly the kind of placeholder API the task said to avoid -- both are deferred to the PR that actually needs them. Extended `quarry-schema-compiler` with `--language {cpp,c}` (default `cpp`; explicit `--language cpp` behaves identically to the default; an unrecognized value is a usage error, exit code 2, naming the invalid value and the two accepted ones); `--file-extension` remains C++-only and is rejected as a usage error when combined with `--language c`, since the C backend's `.generated.h`/`.generated.c` extensions are not yet independently CLI-configurable. `--list-outputs --language c` prints two lines per namespace file (header then source), deterministically. Genericized `tools/schema_compiler/main.cpp`'s file-writing/path-safety helpers (`write_generated_file`/`write_generated_files`) into templates shared by both backends' output, since that logic is a backend-agnostic CLI concern (duplicate-path detection, output-directory containment) rather than backend planning/rendering logic -- keeping one, not two, independently-maintained copies of that safety-critical code. Tests: added `tests/backend_c/backend_c_test.cpp` (8 focused cases against directly-constructed, `SchemaIrValidator`-validated Schema IR: empty schema, root-namespace file naming, nested-namespace symbol-prefix and rendered-content correctness, enum constant rendering, out-of-range enum value rejection, record-with-fields rejection with `plan()`/`generate()` agreement, a forced duplicate-namespace-FQN case exercising the defensive duplicate-output-path check, and `plan()` determinism across repeated calls); extended `tests/tools/schema_compiler_tool_test.cpp` with 9 new cases covering default backend, explicit `cpp` (byte-for-byte matches default), explicit `c` (`--list-outputs` header+source paths, determinism), invalid `--language`, `--file-extension`+`--language c` rejection, generated content assertions, the unsupported-field diagnostic end-to-end through the real compiled binary, and one downstream-consumer-style test that generates real `.h`/`.c` files through the actual compiled `quarry-schema-compiler` executable and compiles the `.c` file with the project's configured C++ compiler invoked as a genuine C89/C99 compiler (`-x c -std=c99 -Wall -Wextra -Wpedantic -Werror`), proving the output is valid, warning-clean ISO C, not merely C-like text a C++ frontend tolerates. An install-based C consumer test (mirroring `runtime_package_test.cpp`/`schema_compiler_package_test.cpp`) was deliberately not added: there is no installed C runtime, install target, or `quarry_generate_c()` CMake helper yet to exercise -- that test category is explicitly sequenced later in `docs/design/c-backend.md`'s roadmap ("package tests," after the runtime exists), so adding one now would test nothing real. Documentation: added `compiler/backend_c/README.md` (current C generation behavior, explicit "architectural skeleton only" status, deliberately-deferred list); added an "Implementation Status" note to the top of `docs/design/c-backend.md` pointing at what PR-107 actually built, leaving the rest of that document as forward-looking design unchanged from PR-106; updated `tools/README.md`'s CLI reference and `--list-outputs`/exit-code prose for `--language`. Manually verified end-to-end before finalizing: generated C and C++ compilation of the same output (both clean, zero warnings under strict flags), the unsupported-field diagnostic, the invalid-language diagnostic, and an enum-bearing schema's rendered output, using the actual built `quarry-schema-compiler` binary against real `.brd` files in a scratch directory. `cmake --preset debug`/`cmake --build --preset debug --parallel`/`ctest --preset debug --output-on-failure` all pass; `git diff --check` clean; generated C output additionally compiled directly with the configured C++ compiler in `-x c` mode as part of validation, independent of the new automated test. Recommendation for PR-108: implement scalar field support (the next roadmap step after skeleton), since it is the smallest step that requires committing to real field storage, presence tracking, and the first genuine C runtime/generated-code API epoch decision -- do not combine it with enum-as-field-type support in the same PR, matching this project's established one-narrow-step-at-a-time discipline. Committed, not pushed, per instructions.
- PR-108: C scalar field codec vertical slice, superseding the earlier same-numbered "scalar and enum" scope with a narrower "scalars only, no enums" directive received mid-implementation. Checkpoint verification before implementation: re-confirmed `backend_c` remains independent of `compiler/backend` (only comment references, zero code/CMake dependency), `--language c` planning/generation still agree, `QUARRY_GENERATED_CODE_API_VERSION_C`/C runtime/codec API did not yet exist, and field-bearing records still failed generation cleanly -- all matched PR-107's reported state, no discrepancy found. Confirmed the exact wire-format contract to replicate by reading `docs/specifications/binary-record-format.md` (big-endian byte order explicitly mandated -- note: the task text's own "little-endian scalar I/O" phrasing was a red herring, since the real, must-be-preserved BRF format is unambiguously big-endian; implemented big-endian and flagged the discrepancy rather than the literal wording) and the exact C++ runtime encode/parse algorithm in `include/quarry/runtime/binary_record.hpp` (header/Field Directory layout, big-endian scalar shifts, LEB128 varuint, signed-integer encoding via same-width unsigned cast, float/double via bit-pattern reinterpretation). Implemented `include/quarry/runtime_c/binary_record.h` (new, header-only, C99, no heap, caller-owned buffers, no global state): bounded writer/reader state; big-endian `u8`/`u16`/`u32`/`u64`/`i8`../`i64`/`bool`/`f32`/`f64` I/O (floats via `memcpy` bit-reinterpretation, not unions or pointer casts, to avoid strict-aliasing/alignment UB); unsigned LEB128 varuint I/O; whole-record `quarry_c_encode_record`/`quarry_c_parse_record`/`quarry_c_find_field` (structural header/directory validation, sort-by-field_index on encode, duplicate/unsorted/overlapping-range detection on decode, matching the C++ runtime's validation rules exactly); a `QUARRY_C_MAX_DIRECTORY_ENTRIES` (64) bound keeping storage fixed-size rather than an unbounded 256-field worst case, reported via a dedicated `QUARRY_C_STATUS_UNSUPPORTED_FIELD_COUNT` status distinct from genuine wire malformation. Verified byte-for-byte against a known-good C++-encoded reference (captured during PR-105) before wiring into codegen. Extended `compiler/backend_c/backend_c.cpp`: a `lower_scalar_field_type()` mapping covering exactly Schema IR's `PrimitiveType` set (`bool`, `i8`/`u8`/`i16`/`u16`/`i32`/`u32`/`i64`/`u64`, `f32`, `f64`); any other field kind (enum reference, string, bytes, array, record reference) fails generation with a diagnostic naming the record and field, and a record mixing supported and unsupported fields fails as a whole -- no partial struct silently drops a field. Generated structs declare fields directly in schema declaration order (not reordered for padding minimization: predictability against the schema outranks an unproven memory saving, matching this project's recurring no-speculative-optimization bar) with an adjacent `bool has_<field>` presence flag per field, matching the C++ backend's `std::optional` presence tracking translated into C; the PR-107 zero-field placeholder was renamed from `_reserved` to `reserved` (a leading underscore risks the C standard's reserved-identifier rules for file-scope-ish identifiers, worth avoiding even where arguably not strictly required). Generated codec API: evaluated (A) status-return-plus-out-parameters, (B) a result struct carrying status and byte counts, (C) separate status/diagnostic objects; selected (B) -- `{status, bytes_written}` for encode, `{status, value, has_byte_offset, byte_offset}` for decode -- as the closest plain-C analogue to C++'s value/status `CodecResult` bundle without needing templates or `std::optional`; rejected (A) for scattering one logical result across a return value and an out-parameter, and (C) as unneeded indirection for this slice's diagnostic surface. Explicitly does not claim full `CodecResult` parity: no `path` member exists yet (there is no nesting/array field for a path to describe), documented as a deliberate, not accidental, gap. `byte_offset` is populated for every decode failure except the runtime's own `QUARRY_C_STATUS_UNSUPPORTED_FIELD_COUNT` (not a wire defect); encode results never carry one, mirroring why C++'s `EncodeResult::byte_offset` never does. Generated-code API version: introduced `QUARRY_GENERATED_CODE_API_VERSION_C` (currently `1`) as a new, independent top-level CMake scalar -- not a reuse or bump of the existing C++ epoch, since this is genuinely the first C generated code with a real runtime contract to guard (PR-107's skeleton called no runtime function and needed no guard); single-sourced into both the public `quarry/runtime_c/version.h` (`#define QUARRY_C_GENERATED_CODE_API_VERSION`) and a new private `compiler/backend_c/generated_code_api_version_c.hpp` (mirroring the C++ backend's own two-sided pattern exactly); generated C headers that contain records emit a `#if QUARRY_C_GENERATED_CODE_API_VERSION != N / #error` compile-time guard (C99 has no `static_assert`; `#if`/`#error` is the portable equivalent). CMake/package: added `runtime_c/CMakeLists.txt` (header-only `Quarry::runtime_c` INTERFACE target, `c_std_99`); required formally enabling the C language at the top-level `project()` call (`LANGUAGES CXX C`) -- discovered because a C++-only test target consuming the C99 INTERFACE compile-feature requirement failed configuration ("No known features for C compiler") until C was a declared project language, not merely auto-detected as a side effect; installed the generated `quarry/runtime_c/version.h` under its own canonical path (mirroring PR-104's C++ precedent) with no duplicate or unprefixed C runtime headers; exposed `Quarry_GENERATED_CODE_API_VERSION_C` in `QuarryConfig.cmake`. Deliberately did not add a `quarry_generate_c()` CMake helper: with only scalar-field support and no demonstrated multi-schema/cross-compiling/output-verification need for C yet, writing the full helper contract now would be speculative; downstream C consumers use the manual `add_custom_command()` pattern instead, verified end-to-end via a fresh install. Tests: `tests/runtime_c/binary_record_test.cpp` (13 cases: scalar round trips including negative/large integers, invalid bool, varuint round-trip and malformed-input rejection, byte-for-byte record encoding matched against the captured C++ reference, out-of-order field sorting, duplicate-field/insufficient-capacity/encoded-size-consistency/truncated-header/bad-reserved-byte/overlapping-range rejections, absent-field lookup); extended `tests/backend_c/backend_c_test.cpp` (updated the PR-107 "any field fails" test to use a genuinely unsupported type now that scalars succeed, added mixed-record-fails-as-a-whole, generated-struct-and-codec-declaration content checks); extended `tests/tools/schema_compiler_tool_test.cpp` (updated the same stale assumption, added scalar-generation content checks and a strict-C99 compile check `-x c -std=c99 -Wall -Wextra -Wpedantic -Werror` against a four-scalar-field schema); added `tests/interop/c_cpp_codec_interop_test.cpp`, a genuine end-to-end proof (not a unit-level approximation): generates the same schema through both real backends, compiles small C and C++ harness programs against each, and asserts the C encoder's bytes are byte-for-byte identical to the C++ encoder's bytes, that C++ successfully decodes the C-encoded bytes, and that C successfully decodes the C++-encoded bytes; added `CConsumerBuildsAndRunsAgainstInstalledPackage` to `tests/consumer/schema_compiler_package_test.cpp` (fresh install, manual `add_custom_command()` C consumer, installed-tree layout assertion, build and run against the real installed package) since no `quarry_generate_c()` helper exists to test instead. Documentation: rewrote `compiler/backend_c/README.md` (supported field-type matrix, generated data model, the three codec-API alternatives and why B was chosen, the generated-code API version decision, byte-offset semantics); added `runtime_c/README.md` (mirroring `runtime/README.md`'s structure: current support, bounded-design rationale, diagnostic context, closed no-diagnostic-strings decision, CMake packaging); updated `docs/design/c-backend.md`'s Implementation Status; updated `tools/README.md`'s `--language c` description; updated `docs/distribution-model.md`'s SDK artifact table and "Evaluated Packaging Models" section (C runtime entry, deferred `quarry_generate_c()` rationale); added one paragraph to the top-level `README.md` stating the exact implemented C subset, deliberately not claiming complete C backend support. All existing C++ tests and generated output verified unchanged (existing `backend_codegen_test`/golden fixtures/`schema_compiler_package_test` C++ cases untouched and still passing). `cmake --preset debug`/`cmake --build --preset debug --parallel`/`ctest --preset debug --output-on-failure`/`git diff --check` all pass; generated C output compiled directly as strict C99 both manually and via the automated test; interoperability tests pass. Recommended scope for PR-109: enum-typed field support (the natural next increment now that plain enum declarations, scalar fields, and the runtime/codec-API/versioning machinery all exist) -- implement the enum-storage design already proposed in `docs/design/c-backend.md` (generated enum typedef as the field's C storage type, wire width chosen independently by max declared value, matching the C++ backend's `enum_width_for_max_value` exactly) and extend the interop test to cover enum fields; do not combine with string/bytes/array/nested-record support in the same PR. Committed, not pushed, per instructions.
- PR-109: C enum field support -- extends the PR-108 scalar codec vertical slice to enum-typed record fields, keeping strings/bytes/arrays/nested records/arrays of records unsupported. Checkpoint verification before implementation: re-confirmed `backend_c` still has zero code/CMake dependency on `compiler/backend` (grep-verified), read the C++ backend's exact `kEnumType` field-lowering logic (`compiler/backend/backend.cpp` lines ~467-493, ~524-533) and its reference golden fixture (`tests/fixtures/backend/enum_reference.txt`) to replicate encode/decode semantics precisely, and confirmed no architectural refactoring was required -- the addition is a namespace-scoped enum catalog pre-pass plus per-field lowering, not a restructure of existing planning/rendering. Design: introduced a whole-schema `EnumCatalog` (ir_id -> type name, value prefix, owning namespace, all-non-negative flag, wire width, declared values), built once via `collect_enum_catalog()` before namespace-file planning, replacing the previous per-namespace inline enum computation (also removing a small duplicate validation, since the catalog is now the single place that validates/computes enum declaration data). Selected representation Option A from `docs/design/c-backend.md` Section 4 (confirmed, not re-litigated): the generated enum typedef (`quarry_<ns>_<Enum>_t`) as the field's C storage type, decoupled from wire width by explicit casts at every encode/decode call site -- changed enum declarations from an anonymous `enum { ... };` block (PR-107) to `typedef enum { ... } <name>_t;` so fields have something to reference; verified this does not break any existing PR-107 test assertion (enum value constant substrings are unaffected by adding the typedef name). **Same-namespace-only scope, deliberate**: a field whose enum is declared in a different namespace fails generation with a diagnostic naming the field and the enum's owning namespace, since backend_c has no cross-generated-file include-dependency mechanism yet and building one prematurely for this narrow slice would be exactly the "framework code for future features" this backend has consistently avoided (the C++ backend's `TypeCatalog` does support this, via `generated_includes` tracking -- a real, acknowledged narrowing relative to C++, not an oversight). **Non-negative-only fields**: a field referencing an enum with any negative declared value also fails generation (the enum declaration itself still renders fine, since PR-107's int32-range check doesn't care about sign) -- matches the C++ backend's `runtime_enum_encoding` (returns nullopt for any negative value) and the BRF spec's Enum Encoding rule exactly. Wire width: smallest unsigned width (1/2/4/8 bytes) fitting the max declared value, matching `enum_width_for_max_value` byte-for-byte. Validation: replicated the C++ backend's exact generated pattern (found via the golden fixture) on **both** encode and decode -- encode checks `record->field == V0 || record->field == V1 || ...` over raw declared values before writing and returns `QUARRY_C_STATUS_UNKNOWN_ENUM_VALUE` if none match (skipped in `_encoded_size()`, which only depends on fixed wire width, not value validity); decode reads into a raw unsigned temporary, performs the same membership check, and only then casts into the enum-typed struct field -- values are never silently coerced. `QUARRY_C_STATUS_UNKNOWN_ENUM_VALUE` required no runtime change: it was already declared in PR-108's `quarry_c_status_t`, explicitly reserved for this moment. Generated-code API version: investigated and confirmed **no epoch bump needed** -- no runtime function signature changed, no new runtime function was added (enum fields reuse the exact `quarry_c_write_uN`/`quarry_c_read_uN` functions scalar fields already used), and the one "new" status value was already part of the epoch-1 contract; generated code compiled against the epoch-1 runtime before PR-109 continues to compile and behave identically. Verified byte-for-byte wire compatibility with a standalone prototype before wiring into automated tests (C encoding matched a hand-computed and then C++-cross-checked byte sequence exactly, including the unknown-enum-value rejection path), then formalized in `tests/interop/c_cpp_codec_interop_test.cpp` by extending the existing scalar schema/harnesses with a `status: Status` enum field plus a new `decode_expect_unknown_enum` harness mode that corrupts the encoded enum byte and confirms both `QUARRY_C_STATUS_UNKNOWN_ENUM_VALUE` (C) and `DecodeError::unknown_enum_value` (C++) are reported identically, both with a byte offset. Tests: extended `tests/backend_c/backend_c_test.cpp` with 4 new cases (enum field struct/codec generation content, enum wire-width selection for a value exceeding `uint8_t` range, cross-namespace rejection, negative-value rejection) -- one initial assertion in the width test was wrong (expected the scratch buffer's element type to vary with field width; it is always `uint8_t` regardless, only the array length varies) and was caught and fixed by actually running the test rather than assuming; extended `tests/tools/schema_compiler_tool_test.cpp` with a new enum-field generation + strict-C99-compile-only test (deliberately reusing the already-safe compile-only, no-link pattern rather than the interop test's combined-invocation history); extended the existing interop test rather than duplicating it. Documentation: rewrote `compiler/backend_c/README.md`'s "Supported field types" section and added a new "Enum fields" section (representation decision, wire encoding, validation semantics, cross-namespace limitation, all grounded in the actual implementation rather than restating the PR-106 proposal) and extended "Generated-code API version (C)" with the no-bump rationale; updated `runtime_c/README.md` to correct a now-stale claim that `QUARRY_C_STATUS_UNKNOWN_ENUM_VALUE` was "not yet produced by anything" and to note PR-109 added zero new runtime code; updated `docs/design/c-backend.md`'s Implementation Status; updated the top-level `README.md`'s C feature-summary paragraph. No change to `compiler/backend/`, `runtime/`, `include/quarry/runtime/`, `include/quarry/runtime_c/binary_record.h` (the C runtime itself), or any C++ golden fixture/generated-code epoch. `cmake --preset debug`/`cmake --build --preset debug --parallel`/`ctest --preset debug --output-on-failure`/`git diff --check` all pass; generated C output (including enum fields) compiled as strict C99; all C/C++ interoperability tests (scalar and enum, both directions, byte-for-byte, and unknown-value rejection) pass; Docker/CI-equivalent validation passed. Recommended scope for PR-110: cross-namespace enum field support (add the include-dependency tracking this PR deliberately deferred) as the smallest way to close the one acknowledged gap versus the C++ backend within the current scalar-plus-enum feature set, before moving on to a genuinely new field category (`string`); do not combine cross-namespace support with any new field category in the same PR. Committed, not pushed, per instructions.
- PR-110: C bounded string field support -- extends the PR-108/PR-109 C codec vertical slice to bounded (fixed-capacity) `string` fields, keeping bytes/arrays/nested records/arrays of records/cross-namespace enum fields unsupported. Checkpoint verified before implementation: compiler/backend_c/, include/quarry/runtime_c/, docs/design/c-backend.md, the C/C++ interop tests, YAML/schema-IR string-bound validation (compiler/semantic/semantic.cpp's validate_positive_u32 already guarantees every string field's max_bytes is present, positive, and fits uint32_t before backend_c ever sees it -- nothing to re-validate), the generated-code API version wiring, and the installed-package consumer test; confirmed the PR-106 fixed-capacity/no-heap ownership design remains applicable and the BRF wire representation for string (length-delimited via the Field Directory, no internal length prefix, no NUL terminator, UTF-8-required) needs no changes. Selected generated representation: `char <field>[max_bytes + 1]` (capacity reserves one byte for a trailing NUL the decoder always writes, so decoded content with no embedded NUL is usable directly with ordinary C string APIs) plus an explicit `uint32_t <field>_length` (matching max_bytes's own width, not size_t, for portability to narrower-size_t embedded targets) as the authoritative wire byte length -- not necessarily equal to strlen(), since the BRF spec explicitly allows embedded U+0000 as valid string data; present-empty vs. absent are distinguished solely by has_<field>, exactly like every other optional field kind this backend generates. No scratch buffer or generated setter/helper function was introduced for strings: record-><field> already holds the wire bytes verbatim, so encode points a quarry_c_field_t entry directly at it, and direct struct manipulation (write content + length + has_field) was judged sufficient, matching the scalar/enum precedent and the "no builder APIs in this PR" instruction. Encoding checks declared length against max_bytes first (QUARRY_C_STATUS_BOUNDS_EXCEEDED) then validates UTF-8 (QUARRY_C_STATUS_INVALID_UTF8), matching the C++ backend's render_string_field_encoding order exactly; decoding performs one checked bounds-check-and-copy runtime call (quarry_c_copy_bounded, new), then validates UTF-8 on the source bytes, and only then commits (NUL-terminates, sets the length, sets has_field) -- the "decode into a temporary and commit only on success" choice, applied per-field to match the PR-109 enum-decode precedent rather than introducing a whole-record temporary; result.value's existing "only meaningful when status == OK" contract means a copy that precedes a later UTF-8-validation failure is never exposed as a successful decode. Added quarry_c_is_valid_utf8 to include/quarry/runtime_c/binary_record.h, a line-for-line port of the C++ runtime's is_valid_utf8 (same accept/reject behavior for every byte sequence, including rejecting overlong encodings, lone continuation bytes, and encoded surrogate halves, and accepting embedded NUL) so the two backends can never diverge on what counts as valid UTF-8. This is the first PR since the C generated-code API epoch was introduced to actually bump it (QUARRY_GENERATED_CODE_API_VERSION_C 1 -> 2, in the top-level CMakeLists.txt's single source of truth): unlike PR-109 (which added zero runtime surface), this PR adds two new public runtime functions and two new quarry_c_status_t values that generated string-field code now depends on, so an old runtime paired with new generated code would otherwise fail with a confusing undefined-identifier compile error instead of the epoch guard's clear #error. Fixed two pre-existing backend_c_test.cpp/schema_compiler_tool_test.cpp tests that used `string` as their representative "unsupported field type" example (now supported) by swapping them to `bytes` (still unsupported), matching the same swap pattern used across both files. Documentation updated: docs/design/c-backend.md (Implementation Status), compiler/backend_c/README.md (new "String fields" section: representation alternatives considered/rejected, NUL-termination and embedded-NUL policy, empty-vs-absent semantics, encode/decode ordering, epoch-bump rationale), runtime_c/README.md (new runtime primitives, new status values, epoch bump), top-level README.md's C-backend summary paragraph. Tests added/extended: tests/runtime_c/binary_record_test.cpp (quarry_c_is_valid_utf8 well-formed/malformed coverage including overlong/surrogate/truncated cases; quarry_c_copy_bounded within-capacity/exact-capacity/over-capacity/null-argument coverage); tests/backend_c/backend_c_test.cpp (generated struct layout, encode/decode codec text, _encoded_size() non-validation, a mixed scalar+enum+string record); tests/tools/schema_compiler_tool_test.cpp (generated string field compiles as strict C99); tests/interop/c_cpp_codec_interop_test.cpp (extended in place: empty-present/absent/maximum-length/embedded-NUL string variants round-tripped byte-for-byte and cross-decoded in both directions, over-capacity rejected identically by both languages before any bytes are produced, and a truncated encoded record rejected by both); tests/consumer/schema_compiler_package_test.cpp's CConsumerBuildsAndRunsAgainstInstalledPackage extended with a string field round trip against a freshly installed package tree. Validation: cmake configure/build (0 warnings, warnings-as-errors on) and ctest --preset debug both clean locally (24/24) and under Docker/CI-equivalent Linux/GCC (24/24); git diff --check clean; generated C compiled as strict C99 (-std=c99 -Wall -Wextra -Wpedantic -Werror) both via the test suite and via a manual scratch-schema round trip (absent/present-empty/normal/maximum-length/over-capacity/embedded-NUL/invalid-UTF-8/truncated-decode all behaving exactly as designed); fresh install to a clean prefix plus a downstream C consumer build/run exercised by the extended package test. Known limitations carried forward unchanged: bytes/arrays/nested records/arrays of records remain unsupported; enum fields remain same-namespace-only and non-negative-values-only. Recommended scope for PR-111: bytes field support (a strict subset of strings' concerns -- fixed-capacity uint8_t buffer plus explicit length, no UTF-8 validation, reusing quarry_c_copy_bounded as-is) -- do not combine with arrays, nested records, or cross-namespace enum support in the same PR.
- PR-111: C bounded bytes field support -- extends the PR-108/PR-109/PR-110 C codec vertical slice to bounded (fixed-capacity) `bytes` fields, reusing the PR-110 string layout strategy and keeping arrays/nested records/arrays of records/cross-namespace enum fields unsupported. Checkpoint verified before implementation: compiler/backend_c/, include/quarry/runtime_c/, docs/design/c-backend.md, the PR-110 string implementation, and the BRF spec's "bytes" section ("Bytes data may contain any byte sequence... No UTF-8 validation applies... measured in raw bytes"); confirmed BytesType.max_bytes is validated identically to StringType.max_bytes by the same validate_positive_u32 call, and that the C++ backend's bytes encode/decode (render_bytes_field_encoding/render_bytes_field_decoding) is structurally identical to its string handling minus the UTF-8 step -- confirming bytes is a strict subset of string's concerns, exactly as recommended in PR-110's own REPORT.md. Representation decision: reused string's layout strategy (fixed-capacity buffer + explicit uint32_t length + has_field) with the two differences the BRF spec itself requires -- capacity is exactly max_bytes (no "+1"; no NUL-termination convenience applies to arbitrary binary data, matching docs/design/c-backend.md Section 2's original "Bytes" investigated area) and the content element type is uint8_t, not char. No UTF-8 validation is performed anywhere for bytes. Encoding checks record-><field>_length against max_bytes (QUARRY_C_STATUS_BOUNDS_EXCEEDED on violation) then adds the field directly (no scratch buffer, no cast needed since the buffer is already uint8_t*, unlike string's char* requiring a cast); decoding calls quarry_c_copy_bounded exactly as string decode does but skips UTF-8 validation and the NUL-terminator write. _encoded_size() performs no bounds check, matching the string/enum precedent. No runtime changes were needed: quarry_c_copy_bounded (added in PR-110) is reused completely unchanged for bytes decode, since a bounds-checked byte copy has no UTF-8-specific behavior to begin with -- confirmed by grep showing zero new runtime symbols added. Generated-code API version decision: QUARRY_GENERATED_CODE_API_VERSION_C stays at 2 (not bumped) -- back to the PR-109 "no bump needed" pattern, since bytes field encode/decode calls only quarry_c_copy_bounded and reuses QUARRY_C_STATUS_BOUNDS_EXCEEDED, both already part of the epoch-2 contract since PR-110; no new public runtime symbol was introduced. Fixed three pre-existing tests (two in backend_c_test.cpp, one in schema_compiler_tool_test.cpp) that used `bytes` as their representative "unsupported field type" example (now supported) by swapping them to `array` (still unsupported), matching the same swap pattern PR-110 used for `string`. Documentation updated: docs/design/c-backend.md (Implementation Status), compiler/backend_c/README.md (new "Bytes fields" section: representation strategy reuse and the two required differences, no-UTF-8-validation rationale, no-runtime-change rationale, epoch-stays-at-2 rationale), runtime_c/README.md (bytes reuses quarry_c_copy_bounded unchanged), top-level README.md/tools/README.md/docs/distribution-model.md's C feature matrices. Tests added/extended: tests/backend_c/backend_c_test.cpp (generated struct layout, encode/decode codec text confirming no UTF-8 validation and no NUL-terminator write, _encoded_size() non-validation, a mixed scalar+enum+string+bytes record); tests/tools/schema_compiler_tool_test.cpp (generated bytes field compiles as strict C99); tests/interop/c_cpp_codec_interop_test.cpp (extended in place: empty-present/absent/maximum-length/non-UTF-8-binary bytes variants round-tripped byte-for-byte and cross-decoded in both directions -- the binary variant proving bytes fields accept content string fields would reject -- over-capacity rejected identically by both languages before any bytes are produced, reusing the existing generic truncated-record coverage since it does not depend on which field is truncated); tests/consumer/schema_compiler_package_test.cpp's CConsumerBuildsAndRunsAgainstInstalledPackage extended with a bytes field round trip (including a non-UTF-8-valid byte sequence) against a freshly installed package tree. Validation: cmake configure/build (0 warnings, warnings-as-errors on) and ctest --preset debug both clean locally (24/24) and under Docker/CI-equivalent Linux/GCC (24/24, no issues found); git diff --check clean; generated C compiled as strict C99 both via the test suite and via a manual scratch-schema round trip (absent/empty-present/binary-content-with-invalid-UTF-8-accepted/maximum-length/over-capacity/truncated-decode all behaving exactly as designed, with the invalid-UTF-8 acceptance specifically confirming no UTF-8 validation leaked in from the string codepath); fresh install to a clean prefix plus a downstream C consumer build/run exercised by the extended package test. Local validation ran unusually slowly this time (~2.3 hours wall-clock for the full suite, dominated by backend_codegen_test/schema_compiler_package_test/c_cpp_codec_interop_test) due to heavy contemporaneous system load on the development machine, not any defect -- confirmed by observing forward progress across distinct compile subprocesses throughout, and by the final 24/24 pass with only per-test timings elevated. Known limitations carried forward unchanged: arrays/nested records/arrays of records remain unsupported; enum fields remain same-namespace-only and non-negative-values-only; no quarry_generate_c() CMake helper exists yet. Recommended scope for PR-112: do not combine multiple new field categories in one PR; the next natural increments are either arrays of the already-supported scalar/enum/string/bytes element kinds, or nested records -- each is a larger, separable unit of work with its own design questions (fixed-capacity array-plus-count shape for the former, by-value embedding and recursive worst-case-size verification for the latter) and should not be combined with each other or with cross-namespace enum support in the same PR.
- PR-112: C bounded array field support -- extends the PR-108/PR-109/PR-110/PR-111 C codec vertical slice to bounded (fixed-capacity) arrays whose element type is a scalar primitive or a same-namespace, non-negative-valued enum, establishing Quarry's fixed-capacity C array model before nested records; keeps arrays of string/bytes/record elements, nested arrays, and cross-namespace/negative-valued enum fields (plain or array-element) unsupported. Checkpoint verified before implementation: compiler/backend_c/, runtime_c, docs/design/c-backend.md, Schema IR's ArrayType representation, the current C generated API, BRF's "Array Encoding" section, and the existing interop tests; confirmed the C++ backend's array-of-scalar/enum encode/decode (render_array_field_encoding/render_array_field_decoding in compiler/backend/backend.cpp) is structurally the exact same per-element codec a plain scalar/enum field already uses, wrapped in a varuint element-count prefix -- and, critically, that include/quarry/runtime_c/binary_record.h already exposes every primitive needed (quarry_c_write_varuint/quarry_c_read_varuint, present since PR-108 for the Field Directory itself, plus the existing per-width quarry_c_write_uN/quarry_c_read_uN functions), meaning this PR requires zero new runtime code. Representation decision: selected the task spec's Option A as-is -- a fixed-capacity array of the element's own C type (the same type a *plain* field of that element type would use, not a separate "array element" type), sized directly from max_elements, plus an explicit uint32_t <field>_count and the same has_<field> presence flag every field kind uses. Refactored the enum-lookup logic (previously inline in the plain-enum-field branch) into a shared lower_enum_reference() helper, reused identically for array-of-enum element resolution, so array<enum> gets the exact same same-namespace/non-negative-values validation a plain enum field already enforces, with a context-specific diagnostic ("field 'X.Y' array element type references...") distinguishing it from the plain-field message. Encoding order matches BRF's "Array Encoding" section exactly: bounds check (count vs. max_elements) first, then the count written as a varuint, then every element written in index order via the same per-element codec (and, for enum elements, the same membership check with an explicit cast) a plain scalar/enum field already uses; the element-building loop carries an unconditional "&& element_index < max_elements" safety bound, independent of whether the call validates (_encode()) or not (_encoded_size()) -- harmless where already validated, but load-bearing in _encoded_size()'s unvalidated path, where it prevents an unbounded loop over an arbitrary caller-supplied count (a real availability concern specific to arrays, unlike a plain field's O(1) write cost). Decoding mirrors this: varuint count read and bounds-checked first, then an exact remaining-byte-count check against count*element_width (rejecting truncated/over-long payloads before any element read), then per-element reads sharing one quarry_c_reader_t (whose own internal bounds checks make manual offset bookkeeping unnecessary, a small simplification versus the C++ backend's subspan-based approach). Scratch buffer capacity uses a fixed worst-case-varuint-size constant (5 bytes, ceil(32/7)) plus max_elements*element_width, deliberately avoiding a second host-side reimplementation of quarry_c_varuint_encoded_size's algorithm that would need to be kept in sync with the runtime forever. Generated-code API version decision: QUARRY_GENERATED_CODE_API_VERSION_C stays at 2 (not bumped) -- back to the PR-109/PR-111 "no bump needed" pattern, since array encode/decode calls only pre-existing, already-public epoch-2 runtime functions and reuses already-declared quarry_c_status_t values (QUARRY_C_STATUS_BOUNDS_EXCEEDED, QUARRY_C_STATUS_INVALID_FIELD_LENGTH, QUARRY_C_STATUS_MALFORMED_VARUINT); confirmed via git diff that include/quarry/runtime_c/binary_record.h and CMakeLists.txt were untouched by this PR. Fixed three pre-existing tests (two in backend_c_test.cpp, one in schema_compiler_tool_test.cpp) that used a plain scalar array as their representative "unsupported field type" example (now supported): the backend_c_test.cpp ones were swapped to array<string> (constructible via direct Schema IR, still unsupported); the schema_compiler_tool_test.cpp one needed a different approach since array<string>/array<bytes> turn out to be inexpressible through the current YAML frontend at all (the semantic pass unconditionally rejects a field-level max_bytes on any array-typed field, a pre-existing schema-language gap independent of this PR) -- swapped instead to an array of a same-namespace enum with a negative declared value, a genuinely YAML-reachable, backend_c-specific rejection that directly exercises this PR's new array-element enum validation path end to end. Documentation updated: docs/design/c-backend.md (Implementation Status), compiler/backend_c/README.md (new "Array fields" section: representation decision, supported-element-kinds rationale, encode/decode ordering, scratch-buffer-sizing rationale, no-runtime-change rationale, epoch-stays-at-2 rationale, and why nested arrays/array-of-record can never actually reach this backend's field lowering through the production pipeline today), runtime_c/README.md (arrays reuse quarry_c_write_varuint/read_varuint and the per-width read/write functions unchanged), top-level README.md/tools/README.md/docs/distribution-model.md's C feature matrices. Tests added/extended: tests/backend_c/backend_c_test.cpp (generated array-of-scalar and array-of-enum struct layout and codec text, _encoded_size() non-validation, array-of-unsupported-element/cross-namespace-enum-element/negative-enum-element diagnostics, a mixed scalar+enum+string+bytes+array record); tests/tools/schema_compiler_tool_test.cpp (generated array field compiles as strict C99); tests/interop/c_cpp_codec_interop_test.cpp (extended in place: empty-present/absent/maximum-length/partially-filled array variants round-tripped byte-for-byte and cross-decoded in both directions for a float32 array, over-capacity rejected identically by both languages before any bytes are produced, reusing the existing generic truncated-record coverage); tests/consumer/schema_compiler_package_test.cpp's CConsumerBuildsAndRunsAgainstInstalledPackage extended with an array field round trip against a freshly installed package tree. Validation: cmake configure/build (0 warnings, warnings-as-errors on) and ctest --preset debug both clean locally (24/24) and under Docker/CI-equivalent Linux/GCC (24/24, no issues found); git diff --check clean; generated C compiled as strict C99 both via the test suite and via a manual scratch-schema round trip (absent/empty-present/partially-filled/maximum-length/over-capacity-rejected/enum-array-roundtrip/enum-array-unknown-value-rejected/truncated-array-rejected all behaving exactly as designed); fresh install to a clean prefix plus a downstream C consumer build/run exercised by the extended package test. Both the local and Docker validation runs took unusually long this session (multiple hours combined, plus one transient Docker daemon 500 error that resolved on retry) due to sustained heavy, unrelated system CPU load on the development machine -- confirmed throughout, at each check, to be genuine forward progress (different compile subprocesses/temp directories advancing over time) rather than any indication of a hang, and every run ultimately completed with a full, clean pass. Known limitations carried forward unchanged: nested records, arrays of records, and arrays of string/bytes elements remain unsupported; enum fields (plain or array-element) remain same-namespace-only and non-negative-values-only; no quarry_generate_c() CMake helper exists yet. Recommended scope for PR-113: nested records -- proves by-value embedding and the recursive worst-case-size property this backend's fixed-capacity design has relied on since PR-106, and is a substantially larger, more foundational unit of work than any prior increment (it introduces a genuinely new field-encoding shape -- an embedded generated struct, not a scalar/enum/string/bytes/array leaf -- and needs its own diagnostic-path-propagation design, unlike every PR-108 through PR-112 increment, which extended the existing flat-field codec pattern). Do not combine nested records with arrays of records, arrays of string/bytes elements, or cross-namespace enum support in the same PR; each remains a separable follow-on once nested records themselves are proven.
- PR-113: C nested record field support -- extends the PR-108 through PR-112 C codec vertical slice to same-namespace nested record fields (a record embedding another record, declared in the same namespace, by value), completing every field category the C backend now supports short of arrays of records/string/bytes and cross-namespace enum/nested-record references. Checkpoint verified before implementation: compiler/backend_c/, runtime_c, Schema IR's RecordRef representation, BRF's "Nested Records" section, the C++ backend's own nested-record implementation (compiler/backend/backend.cpp's order_declarations_topologically and its forward_record_reference/nested_record_fields fixtures), the existing interop tests, and generated-code API version handling; confirmed that (1) nested-record encode/decode is pure composition of already-generated per-record _encode()/_decode()/_encoded_size() functions, needing zero new runtime code, since a child record's own quarry_c_parse_record call (inside its own generated _decode()) already enforces every BRF "Nested Records" structural requirement (header validation, exact payload length, matching record id) when called on an isolated field-view byte span; (2) same-namespace-only is the correct scope boundary, mirroring PR-109's identical enum restriction and its "no cross-generated-file include-dependency mechanism" rationale exactly; (3) embedding a record by value requires the referenced record's C struct to be a complete type at the embedding point, requiring -- for the first time in this backend's history -- a real topological sort of same-namespace record declarations (previously unnecessary, since enums are dependency-free leaves rendered unconditionally before any record); (4) cycle detection (a record embedding itself directly or transitively) is enforced nowhere upstream of backend_c (verified via grep: no symbols/semantic/schema_ir/layout pass rejects it), so backend_c must and does implement it independently, mirroring the C++ backend's own equivalent check. Representation decision: generate nested C structs directly, embedded by value as a plain struct member (the referenced record's own generated _t type) -- no pointers, no heap allocation, matching this backend's existing "no opaque handle, no hidden state" rule for every other field kind. Presence uses the same has_<field> flag every other field kind uses; initialization needed zero special-case code, since _init()'s existing memset(record, 0, sizeof(*record)) already zero-initializes an embedded child completely, a "free" consequence of by-value embedding. Implemented via a three-phase per-namespace record-processing restructuring of collect_namespace_files: Phase 1 lowers every field (collecting same-namespace record dependencies as a side effect, deferring each record's own max_encoded_size), Phase 2 topologically sorts the namespace's records via a new order_records_topologically (Kahn's algorithm, ported from and structurally mirroring the C++ backend's order_declarations_topologically, with a set-based deterministic tie-break), and Phase 3 resolves each record-typed field's already-topologically-guaranteed-available record_max_encoded_size from a new whole-schema RecordCatalog (mirroring the existing EnumCatalog) and computes/stores this record's own max_encoded_size via a new compute_record_max_encoded_size, so a later-processed embedding record can always look up an already-resolved child size in constant time. compute_record_max_encoded_size sums a fixed 16-byte BRF header plus, per field, a 21-byte worst-case Field Directory entry overhead (1 + kMaxVaruintBytesForUint64 + kMaxVaruintBytesForUint64, a new 10-byte constant -- ceil(64/7) -- deliberately distinct from the existing 5-byte kMaxVaruintBytesForUint32 used for array element counts, since Field Directory offsets/lengths are size_t/uint64_t-ranged in the runtime API while element counts are genuinely uint32-bounded by max_elements) plus that field's own worst-case payload contribution (max_bytes/array_scratch_capacity/a nested record's own recursively-resolved max_encoded_size/width_bytes), assuming every field present simultaneously, the same safe-worst-case assumption arrays already use. Encoding calls the child's real, validating _encode() into a scratch buffer sized from this max_encoded_size and propagates any failure status directly; _encoded_size() instead calls only the child's own _encoded_size(), matching the string/bytes/array no-validation-in-encoded-size precedent exactly. Decoding calls the child's real _decode() directly on the isolated field-view byte span and propagates any failure status, composing an absolute byte offset (field_view.byte_offset + child_decode_result.byte_offset) exactly mirroring the array decode path's identical field_view.byte_offset + array_reader.offset composition and the C++ backend's field->field_offset + *decoded.byte_offset. Generated-code API version decision: QUARRY_GENERATED_CODE_API_VERSION_C stays at 2 (not bumped) -- the strongest "no bump needed" case yet, since nested-record encode/decode calls no runtime function directly at all, only the referenced record's own already-generated codec functions (themselves built entirely from already-epoch-2 runtime primitives); confirmed via git diff --stat that include/quarry/runtime_c/binary_record.h and CMakeLists.txt were both untouched by this PR. Documentation updated: docs/design/c-backend.md (Implementation Status), compiler/backend_c/README.md (new "Nested record fields" section covering the representation decision, same-namespace restriction, topological-sort/cycle-detection rationale, pure-composition encode/decode design, and scratch-buffer sizing; also corrected a stale "this slice has no nesting and no arrays" CodecResult-parity note left unupdated since PR-109/110, which had become doubly wrong after PR-112 added arrays), runtime_c/README.md (status line and epoch history), top-level README.md/tools/README.md/docs/distribution-model.md's C feature matrices. Tests added: tests/backend_c/backend_c_test.cpp (generated nested-struct layout and codec text including by-value-not-pointer verification, _encoded_size() calling only the child's _encoded_size(), a cross-namespace nested-record-field diagnostic, a self-referential-record cycle diagnostic, a forward-declared-dependency reordering test mirroring forward_record_reference.pbtxt exactly to prove topological reordering independent of Schema IR declaration order, an array-of-record-element-type diagnostic, and a mixed record extended with all previously-supported field kinds plus a nested record field); a new tests/interop/c_cpp_nested_record_interop_test.cpp, since the YAML frontend cannot express a nested-record schema at all (CLAUDE.md's "one primary record per schema file"; there is no import/cross-file resolution pass, confirmed by reading compiler/yaml/schema_decoder.cpp, compiler/source_schema/source_schema.hpp, and the CLI's single-input-file contract) -- this new test instead builds Schema IR directly (mirroring forward_record_reference.pbtxt exactly, so its expected encoded bytes are the same ones already independently proven correct by BackendCodegenTest.NestedRecordFieldUsesCompleteEmbeddedRecord) and calls both backends' Backend::generate() in-process, then compiles and runs small C and C++ harnesses against the real generated output exactly like the YAML-driven interop test does downstream of generation, proving byte-for-byte C/C++ encode compatibility and bidirectional cross-decoding for both an empty-but-present and a populated nested child, plus identical rejection of a wrong-nested-record-id payload and a truncated-nested-header payload. tests/tools/schema_compiler_tool_test.cpp and tests/consumer/schema_compiler_package_test.cpp were left unchanged for the same YAML-inexpressibility reason (verified their existing "unsupported field type" representative example was already an array-element-enum case, not a bare record reference, so no fixture needed updating). Validation: cmake configure/build (0 warnings, warnings-as-errors on) and ctest --preset debug both clean locally; git diff --check clean; generated C compiled as strict C99 (-std=c99 -Wall -Wextra -Wpedantic -Werror) via the new interop test's real compile/link/run cycle; a fresh install plus downstream C consumer validation via the existing (YAML-expressible-field-set-only) package test. Known limitations carried forward: arrays of records, arrays of string/bytes elements, cross-namespace nested record fields, and cross-namespace/negative-valued enum fields (plain or array-element) remain unsupported; no quarry_generate_c() CMake helper exists yet; the YAML frontend's "one primary record per schema file" constraint means nested records (and therefore also arrays of records, once implemented) can only ever be tested via direct Schema IR construction, not through the installed CLI tool or a real .brd file -- a pre-existing, deliberate frontend boundary, not a backend_c gap. Recommended scope for PR-114: either arrays of records (composes the now-proven array and nested-record models, likely the smallest remaining data-shape milestone per docs/design/c-backend.md Section 9) or a first cross-namespace include-dependency mechanism (which would lift the same-namespace restriction for both enums and nested records at once, rather than solving it twice) -- both are separable, well-scoped follow-ons; do not combine either with new diagnostic-path (`path`-equivalent) support, which remains deferred until a concrete need for symbolic (as opposed to byte-offset) failure location is demonstrated.
- PR-114: C bounded array-of-records support -- extends the PR-108 through PR-113 C codec vertical slice to bounded (fixed-capacity) arrays whose element type is a same-namespace record, completing the array-of-record milestone anticipated in docs/design/c-backend.md Section 9's roadmap. Two-stage process: an architectural investigation (produced as REPORT.md, not committed as part of this PR's code changes but summarized here) followed by implementation once the investigation's design was confirmed feasible. Investigation established: (1) Schema IR's ArrayType/RecordRef already fully support this combination with zero upstream changes needed, verified empirically via a real .brd file (a self-referencing array<record> field) reaching backend_c's own diagnostic through the full YAML/semantic/Schema-IR pipeline; (2) a precise correction to PR-113's own YAML-reachability claim -- a *self-referential* record or array (needing only one record declaration) is fully expressible through a real .brd file and the installed CLI, unlike a *distinctly-named second record* (still genuinely YAML-inexpressible, per CLAUDE.md's "one primary record per schema file"); (3) cycle detection generalizes to array-element dependencies for a hard, non-optional reason: a record containing an array of itself requires a complete element type at the point of declaring the fixed-size array member, exactly like a record embedding itself by value, so there is no valid declaration order -- verified this is correctly rejected by extending Phase 1's dependency collection by one condition, with zero changes needed to order_records_topologically's Kahn's-algorithm cycle detection itself (introduced in PR-113). A follow-up, deeper investigation (explicitly requested and separately validated before implementation began) proved the investigation's own initial conclusion wrong: it had concluded a new runtime primitive (quarry_c_write_bytes, an "append pre-encoded raw bytes into a writer" function, mirroring the C++ runtime's append_bytes) was required for encoding, reasoning by analogy with the C++ backend's implementation (which must fully encode each element into a heap-allocated std::vector first, since the C++ runtime has no size-only computation). That analogy does not hold for C: every generated C record has had a size-only, non-allocating <Type>_encoded_size() function since PR-108, specifically added so callers could pre-size an output buffer without a full encode -- this was simply never previously considered as the answer to the "must know a length before writing it as a wire length-prefix" ordering constraint. Once available, generated code can learn an array element's exact encoded length via _encoded_size(), write that length as the required wire varuint via the already-existing quarry_c_write_varuint(), and then encode the element for real directly into the array field's own writer at its current tail position (an ordinary quarry_..._encode() call with a repositioned output pointer and reduced capacity -- nothing about _encode()'s contract requires a fresh, zero-offset buffer), advancing the writer's already-public .length field directly afterward. This eliminates the need for any temporary per-element scratch buffer, any raw-byte copy, and any new runtime function whatsoever -- proven safe by the pre-existing invariant that _encoded_size() and a successful _encode() can never disagree on byte count (they differ only in that _encode() additionally validates, and wherever they would disagree, _encode() fails outright before the now-irrelevant speculative length prefix is ever exposed). Implemented exactly this design ("Option A" in both investigation documents), with zero deviation from the confirmed plan. Representation: reuses the existing array-of-scalar/enum struct shape verbatim (ElementType_t field[max_elements]; uint32_t field_count;) -- zero new rendering code in render_header/render_field_scratch_declarations, since c_type is simply set to the referenced record's own _t type name during lowering, mirroring exactly how array-of-enum already sets c_type to the enum's own typedef. Extracted lower_record_reference (mirroring PR-112's lower_enum_reference extraction) so a plain record-typed field and a record-typed array element share one same-namespace validation check; added a new array_element_is_record boolean to FieldEncoding (reusing, not duplicating, the existing record_target_ir_id/record_symbol_name/record_max_encoded_size fields, mirroring how is_enum already double-duties for plain-enum and array-of-enum fields). Extended Phase 1's dependency collection (collect_namespace_files) to also push a same-namespace dependency for array_element_is_record fields; deferred array_scratch_capacity computation for record-element arrays from Phase 1 to Phase 3 (alongside the existing record_max_encoded_size resolution), since it now depends on the element record's own max_encoded_size, only available after topological sorting -- new formula: kMaxVaruintBytesForUint32 (count prefix, unchanged) + max_elements * (kMaxVaruintBytesForUint64 (new per-element length-prefix worst case, reusing PR-113's existing constant) + element_record_max_encoded_size). No change was needed to compute_record_max_encoded_size itself, since its existing is_array branch already absorbs whatever this new formula produces. Encode (render_array_field_build's new per-element branch, render_record_array_element_build): exactly the "Option A" design above. Decode (render_array_field_decode's new per-element branch, render_record_array_element_decode): reads a per-element length-prefix varuint via the existing quarry_c_read_varuint (reusing the same array_reader the element count was read from), bounds-checks it against remaining reader bytes, then passes the isolated element byte span directly to the element type's own real _decode() -- pure composition, identical in spirit to a plain nested-record field's decode, needing zero new validation code since the child's own quarry_c_parse_record call already enforces every BRF "Nested Records" structural requirement (header validation, exact payload length, matching record id) on the isolated span. Unlike fixed-width scalar/enum arrays (which validate total remaining-byte-count against count*width up front), record (variable-width) elements instead check a running array_reader.offset against array_reader.length after the per-element loop, mirroring the C++ backend's identical post-loop trailing-bytes check exactly. Generated-code API version decision: QUARRY_GENERATED_CODE_API_VERSION_C stays at 2 -- confirmed via git diff --stat showing zero changes to include/quarry/runtime_c/binary_record.h and CMakeLists.txt; this is the strongest "no bump needed" case in this backend's history, since it is the first field kind whose encode side (not just decode) needed no runtime addition at all. Tests added: tests/backend_c/backend_c_test.cpp (struct/codec text for a record-array field including exact scratch-buffer-size verification, _encoded_size() non-validation, a cross-namespace record-array-element diagnostic, a self-referential-array-of-records cycle diagnostic, a multi-level composition test mirroring the C++ backend's own RecordArrayComposesWithNestedRecordFields, and the existing Mixed*GeneratesAllFieldKinds test extended with a record-array field); fixed one pre-existing test (ArrayOfRecordElementTypeFailsWithClearDiagnostic, which asserted array<record> was unsupported) by renaming/reworking it into ArrayOfCrossNamespaceRecordElementTypeFailsWithClearDiagnostic, the genuinely-still-unsupported cross-namespace variant, matching this session's established "swap the now-supported representative example" maintenance pattern; tests/tools/schema_compiler_tool_test.cpp (a new self-referential-array-of-records cycle-rejection test, reachable through a real .brd file and the installed CLI, not just direct Schema IR construction -- and a corrected comment on the pre-existing CBackendRejectsUnsupportedFieldType test clarifying the precise YAML-reachability nuance); tests/interop/c_cpp_nested_record_interop_test.cpp extended in place (not a new file) with an "items: B[]" array field added to the existing A/B schema, covering empty/partially-filled/maximum-length (3-element, mixed populated-and-empty elements) arrays verified byte-for-byte identical between C and C++ encoders and cross-decodable in both directions, plus a wrong-element-record-id and a truncated-array rejection case for both languages -- derived from the real, already-cross-verified "full" array encoding at runtime (with a hand-verified, schema-fixed byte offset for the embedded record id) rather than a separately hand-typed byte literal, to minimize manual-arithmetic risk; tests/consumer/schema_compiler_package_test.cpp confirmed unchanged (same YAML-inexpressibility limitation as PR-113: a record-array-of-a-different-record-type schema needs two record declarations, not expressible through this test's YAML-driven schema). Documentation updated: compiler/backend_c/README.md (status line, "Supported field types", new "Record array fields" section covering representation, same-namespace restriction, cycle-detection generalization, the write-side investigation and its corrected conclusion, scratch-buffer sizing, and multi-level composition; corrected the "Array fields" section's now-stale claims about record-reference array elements being permanently unreachable and unsupported), runtime_c/README.md (status line, epoch history, out-of-scope bullets), docs/design/c-backend.md (Implementation Status paragraph), top-level README.md/tools/README.md/docs/distribution-model.md's C feature matrices. Validation: cmake configure/build (0 warnings, warnings-as-errors on) and ctest --preset debug both clean locally; git diff --check clean; Docker/CI-equivalent validation run; generated C for the array-of-record field compiled as strict C99 (-std=c99 -Wall -Wextra -Wpedantic -Werror) via the extended interop test's real compile+link+run cycle (the only mechanism available for this field kind, per the YAML-inexpressibility finding); fresh install plus downstream C consumer validation via the existing (YAML-expressible-field-set-only) package test. No deviations from the investigation's confirmed design were needed during implementation. Known limitations carried forward: arrays of string/bytes elements and cross-namespace references (plain enum/record fields, or as array elements) remain unsupported; no quarry_generate_c() CMake helper exists yet; array-of-a-distinctly-named-second-record schemas remain testable only via direct Schema IR construction, not the installed CLI tool or a real .brd file, for the same pre-existing YAML frontend limitation as PR-113's plain nested records. Recommended next steps: a first cross-namespace include-dependency mechanism (would lift the same-namespace restriction for enums, plain nested records, and record-array elements all at once, rather than solving the same underlying problem three separate times across three future PRs) is now the single highest-leverage remaining C backend extension, since every other planned data-shape milestone from docs/design/c-backend.md Section 9 is now complete; arrays of string/bytes elements remain a separable, smaller follow-on if a concrete need is demonstrated.
- PR-118: Python backend architecture skeleton -- establishes `compiler/backend_python/` as a third, fully independent backend (depends only on `quarry_compiler_schema_ir` and `quarry_project_options`, zero dependency on either `compiler/backend/` or `compiler/backend_c/`), proving the C-backend-established architecture (`Backend`/`CodegenOptions`/`plan()`/`generate()`, one shared `build_generation_plan()` so plan/generate cannot diverge) generalizes to a structurally different language without any compiler-pipeline or Schema IR change. Preceded by three investigation-only PRs (PR-117: native Python backend architecture across 12 areas -- dataclasses, methods-vs-free-functions, namespace-to-package mapping, runtime boundary, epoch mechanism, roadmap; PR-118A: a focused follow-up resolving specifically where encode/decode should live, recommending methods that internally delegate to module-level free functions, backed by verified precedent from the C++ backend's own generated fixtures) whose conclusions this PR treats as frozen unless implementation revealed a concrete problem (none did). Scope is deliberately an architecture skeleton, not serialization: supports zero-field records only (any record declaring one or more fields fails generation with a diagnostic naming the record and field count, mirroring PR-107's identical original-C-skeleton rule exactly); enums are not rendered at all yet (an enum-only namespace emits nothing). The one genuinely novel design problem, absent from both existing backends: Python's import system requires *real* nested packages, unlike C's flat "last segment doubles as the filename" convention -- every namespace FQN segment becomes its own directory with its own `__init__.py`, with the namespace's own generated module living one level further in (e.g. FQN `acme.telemetry` -> `acme/__init__.py`, `acme/telemetry/__init__.py`, `acme/telemetry/schema.py`); sibling namespaces sharing a common ancestor package correctly deduplicate that ancestor's `__init__.py` (collected via `std::set`), while genuinely colliding module paths are still defensively rejected, mirroring `backend_c`'s own duplicate-output-path guard. Each zero-field record renders the exact template specified: an empty-bodied `@dataclass` whose `encode`/`decode`/`encoded_size` methods delegate their single line of implementation to `_encode_<snake_name>`/`_decode_<snake_name>`/`_encoded_size_<snake_name>` leading-underscore module-level helper functions (PR-118A's methods-delegate-to-free-functions recommendation, now wired end to end -- only the helpers `raise NotImplementedError`, never the methods themselves, so there is exactly one place "not implemented yet" is expressed, not two independent copies), using a PascalCase-to-snake_case conversion for helper names (documented limitation: no acronym special-casing, e.g. `HTTPResponse` -> `h_t_t_p_response`). A corrected mid-PR spec re-delivery made this delegation explicit (the first draft of the spec's own example showed the methods independently raising, which this PR's first implementation pass matched literally; a follow-up delivery of the same PR corrected the example to show delegation, and this PR's final implementation matches that corrected example, including its two-blank-line PEP8 spacing convention between the class and the module-level helpers, distinct from the single blank line used elsewhere in the template). Every generated module begins with an import-time compatibility check against a new, independent Python generated-code API epoch (`QUARRY_GENERATED_CODE_API_VERSION_PYTHON = 1`, a `constexpr` in `backend_python.cpp` kept manually in sync -- not yet CMake-`configure_file`-driven like the C/C++ epochs -- with the identical literal in the new `runtime/python/` package), raising `ImportError` with a clear diagnostic on mismatch: the same philosophy as the C/C++ compile-time epoch guards, expressed idiomatically as an import-time check since Python has no preprocessor. `runtime/python/` (`pyproject.toml` + `src/quarry/runtime/python/__init__.py`, importable as `quarry.runtime.python`) is a plain pip-installable package deliberately kept outside the CMake configure/install/export graph entirely, documented as an intentional distribution-model divergence in `docs/distribution-model.md`'s new "Python Runtime Packaging" section. CLI integration extends `--language` to accept `python` exactly like the existing `c` addition did (same `--file-extension`-rejection pattern, same `--list-outputs` support). Tests added: `tests/backend_python/backend_python_test.cpp` (13 cases: registration, root-namespace-no-package handling, nested-package/`__init__.py` generation, sibling-namespace ancestor-`__init__.py` deduplication, exact-template verification, snake_case helper naming, a dedicated delegation check confirming the methods call the helpers and never `raise` themselves, epoch-check preamble presence/placement, the fields-fail diagnostic, enum-only-namespace exclusion, plan/generate path agreement, and generation determinism); `tests/tools/schema_compiler_tool_test.cpp` (four new `--language python` cases plus an update to the pre-existing invalid-language-value test's expected message, now naming three accepted values instead of two); a genuinely new kind of test, `tests/backend_python/python_execution_test.cpp`, which invokes the real `quarry-schema-compiler` binary to generate a package and then a real `python3` subprocess (via `find_program`, gracefully `GTEST_SKIP`ped if unavailable) to import it, instantiate a zero-field record, verify `encode()` raises `NotImplementedError`, and verify an epoch mismatch raises `ImportError` at import time -- no serialization tests exist, since there is nothing to serialize yet. Documentation added/updated: `docs/design/python-backend.md` (new -- architecture, namespace-mapping rationale, generated API, runtime boundary, known limitations, roadmap sketch continuing PR-117 §11's sequencing: scalars, then enums, then string/bytes, then arrays, then nested records, then interop, in that order); `docs/distribution-model.md` (new artifact-classification rows for generated Python code and the Python runtime package, new "Python Runtime Packaging" section); `docs/architecture/language-generators.md` (brief status note confirming Python's row is now real, not merely aspirational); top-level `README.md` and `tools/README.md` (new Python paragraphs, `--language {cpp,c,python}` usage text). Validation: `cmake --preset debug` / `cmake --build --preset debug --parallel` / `ctest --preset debug --output-on-failure` clean locally including all new targets; `git diff --check` clean; manual end-to-end verification of both the golden-path zero-field-record import/encode-stub behavior and the epoch-mismatch-raises-ImportError behavior via a real `python3` invocation against a real generated package, independent of the automated test suite. No deviations from the frozen PR-117/PR-118A architecture were needed. Known limitations carried forward: no enum, scalar, string/bytes, array, or nested-record field support yet; no Python-keyword escaping; the Python runtime epoch and the backend's embedded expected-epoch literal are two independently-maintained copies of the same number (not CMake-driven, unlike C/C++); a schema using literally `quarry` as its own top-level namespace segment could collide with the runtime package's own `quarry.runtime.python` namespace if both ever shared a `sys.path` entry with non-namespace-package `__init__.py` files at that shared segment (unaddressed, not exercised by this PR's tests, which use a distinct test namespace precisely to sidestep it). Recommended next step: scalar field support (bool, fixed-width integers, f32/f64) plus the first real `runtime/python/` codec mechanics (varuint I/O, header/Field Directory assembly, a `struct`-based scalar pack/unpack helper) -- the natural next vertical slice, mirroring PR-108's role for the C backend.
- PR-119: Python scalar field support -- the first *functional* Python backend milestone, implementing PR-118's recommended next step exactly: bool and every fixed-width signed/unsigned integer and f32/f64 field, using the frozen PR-117/PR-118/PR-118A architecture unchanged (no backend redesign, no Schema IR or compiler-pipeline change, no modification to the C or C++ backends beyond what test registration already required). New runtime module `runtime/python/src/quarry/runtime/python/binary_record.py` -- the Python analog of `quarry/runtime/binary_record.hpp` (C++) and `quarry/runtime_c/binary_record.h` (C) for this same subset -- implements `pack_scalar`/`unpack_scalar` (big-endian scalar encode/decode for all eleven types via the standard library's `struct.pack`/`unpack` with an explicit `">"` format code, deliberately not hand-rolled bit-shifting; `struct.pack`'s own fixed-width range checking is reused directly as the Python-specific equivalent of what C++/C's native fixed-width types enforce structurally), `append_varuint`/`read_varuint` (unsigned LEB128, byte-for-byte matching the BRF spec), and `encode_record`/`parse_record` (whole-record 16-byte-header + Field-Directory + Payload assembly/parsing with the same structural validation the C++/C runtimes perform: header version, zero flags/reserved, exact payload-length match catching both truncation and trailing bytes, directory sort/duplicate/overlap checks, field-range-within-payload checks), plus two plain-`Exception` subclasses (`EncodeError`/`DecodeError`) generated code and callers raise/catch directly -- no C++/C-style `CodecResult<T,E>` value type, matching PR-118A's own reasoning that Python's exception mechanism is the idiomatic fit. `compiler/backend_python/backend_python.cpp` gained `lower_scalar_field_type()`, an independently-re-derived (not shared) mirror of `backend_c.cpp`'s own eleven-case scalar mapping, producing a `runtime_type_name` string (e.g. `"uint32"`) and a `python_type_hint` (`"bool"`/`"int"`/`"float"`) per field; any non-scalar field type (enum, string, bytes, array, nested record) still fails generation with a diagnostic naming the record and field, mirroring the C backend's own "swap the representative unsupported-type example once it becomes supported" test-maintenance pattern (the pre-existing "any field fails" test was reworked to use a `string` field, since `uint32` -- its previous representative example -- is now supported). Every scalar field becomes an `Optional[<hint>] = None` dataclass attribute (PR-117's decided absent/present-via-`None` representation); the three delegating methods PR-118 already wired (`encode`/`decode`/`encoded_size` -> `_encode_<name>`/`_decode_<name>`/`_encoded_size_<name>`) now call real, working helper bodies instead of `raise NotImplementedError` -- including for zero-field records, which simply hit the empty-fields-list case of the same real logic (no more permanent stub carve-out for that case). `_encoded_size_<name>` is implemented as `len(_encode_<name>(value))`: always exactly correct by construction, at the cost of a full encode to learn a size, an explicitly acknowledged simplicity/performance tradeoff versus the C/C++ backends' size-only computation. Every generated module's preamble gained one more import (`from quarry.runtime.python import binary_record as _brf`) alongside the pre-existing epoch-constant import, plus `from typing import Optional`. Verified byte-for-byte wire compatibility empirically, not just by code inspection: hand-computed the exact expected BRF bytes for a representative multi-field record directly against the BRF spec (header layout, Field Directory varuint encoding, payload order) before writing any interop test, then confirmed the Python runtime's actual output matched the hand computation exactly, before building the automated three-way interop test. Tests added: `runtime/python/tests/test_binary_record.py` (42 stdlib-`unittest` cases covering scalar round trips and boundary values for all eleven types, range-check rejection, varuint round trips including a 10-byte/64-bit boundary value and malformed/truncated-varuint rejection, whole-record round trips, and every structural rejection case BRF's Validation Rules section requires: truncated header, unsupported version, nonzero flags/reserved, payload-length mismatch, truncated directory, unsorted/duplicate directory entries, out-of-range field ranges, overlapping ranges, and too-many-fields); a new gtest wrapper `tests/backend_python/python_runtime_test.cpp` running that suite via a real `python3 -m unittest` subprocess (discovered mid-implementation, via a genuine ctest failure, that `python3 -m unittest <absolute-path>` fails to import the module on Python 3.14 -- fixed by `cd`-ing into the test's own directory and passing the bare module name instead, verified against both the failing and fixed forms before finalizing); `tests/backend_python/backend_python_test.cpp` extended with 3 new cases (all-eleven-scalar-types-succeed, generated encode/decode helper text correctness, and the reworked unsupported-type diagnostic test) for 15 total; `tests/backend_python/python_execution_test.cpp` extended from 2 to 6 cases (zero-field real round trip replacing the old stub-verification test, scalar round trip with an exact expected-bytes assertion, absent-field-decodes-as-`None`, out-of-range-value-raises-`EncodeError`, malformed/truncated/trailing-bytes-raises-`DecodeError`, plus the pre-existing epoch-mismatch case refactored onto a new shared `run_python_harness` helper); a new three-way interop test, `tests/interop/python_cpp_c_codec_interop_test.cpp`, generating one scalar-only schema covering all eleven supported types through the C++, C, and Python backends, compiling C and C++ harnesses and writing a Python harness script with byte-for-byte-identical set/check logic across all three, and verifying: all three encoders produce identical bytes for identical field values, all three decoders accept bytes produced by either of the other two languages (six cross-decode directions total), and all three identically reject a truncated buffer and identically reject extra trailing bytes appended after a valid record -- confirming Python's wire output is genuinely interoperable, not merely self-consistent. Documentation updated: `docs/design/python-backend.md` (Implementation Status, scalar field lowering section replacing the old zero-field-only rule, updated Generated API template with real codec bodies, expanded Runtime boundary section documenting `binary_record.py`'s full API surface, updated Known Limitations -- including a new float32-precision-loss note inherent to Python having only one native float type -- and roadmap with PR-119 marked done), `docs/distribution-model.md` (updated artifact-classification rows), top-level `README.md`/`tools/README.md` (updated Python feature-matrix paragraphs), this backlog entry. Validation: `cmake --preset debug` / `cmake --build --preset debug --parallel` / `ctest --preset debug --output-on-failure` clean locally, 29/29 passed, including all new/extended Python targets; `git diff --check` clean; Docker/CI-equivalent validation cycle (clean `build/debug`, full configure+build+ctest, 29/29 passed) followed by a full native reconfigure+rebuild+retest (since the Docker run's bind-mounted `build/debug` clobbers the native `CMakeCache.txt`, the same restore-afterward discipline every prior PR in this session has followed). One deviation from the literal task spec worth recording: the spec listed "Do not modify the C or C++ backends except where tests require additional language registration," and in fact zero modifications to `compiler/backend/` or `compiler/backend_c/` were needed at all -- both were already fully wired for `--language cpp`/`--language c` since PR-107/PR-118, so this PR's only C/C++-adjacent test changes were writing new harness *source text* embedded as string literals inside the new interop test, not touching either backend's own code. Known limitations carried forward/added: no enum, string, bytes, array, or nested-record field support yet; no Python-keyword escaping (now reachable, since field rendering exists, but not yet addressed); the Python runtime epoch and the backend's embedded expected-epoch literal remain two independently-maintained copies of the same number; the `quarry`-namespace-collision-with-the-runtime-package limitation from PR-118 remains unaddressed; `_encoded_size_<name>`'s full-encode-based implementation is a known, accepted simplicity/performance tradeoff. Recommended next step: enum support (`enum.IntEnum`, per PR-117 §8's finding that `IntEnum`'s constructor already raises `ValueError` for undefined values with no extra validation code needed) -- the natural next vertical slice per the roadmap, followed by string/bytes, then arrays, then nested records, matching the C backend's own historical field-category-at-a-time sequencing.
- PR-120: Python enum field support -- implements PR-119's recommended next step exactly: a field may now reference a same-namespace enum whose declared values are all non-negative, using the frozen PR-117/PR-118/PR-119 architecture unchanged (no backend redesign, no Schema IR or compiler-pipeline change, no modification to the C or C++ backends). `compiler/backend_python/backend_python.cpp` gained a whole-schema `EnumCatalog` (built by `collect_enum_catalog`, mirroring `backend_c.cpp`'s catalog in spirit but independently implemented and, deliberately, *not* capping declared values to the 32-bit signed integer range the way backend_c's own catalog does -- that cap is backend_c's own narrower implementation choice, not a BRF-wide restriction, and Python's width bucketing already covers every value up to `uint64::max` cleanly, so `collect_enum_catalog` is infallible, unlike its backend_c namesake) recording each enum's bare class name (e.g. `"Status"`, no namespace prefix -- Python's own package/module structure already disambiguates, matching record class naming) and wire width type name (`"uint8"`/`"uint16"`/`"uint32"`/`"uint64"`, via `enum_width_type_name_for_max_value`, independently mirroring both existing backends' identical `enum_width_for_max_value`). `lower_enum_reference()` resolves an enum-typed field by ir_id, rejecting a cross-namespace reference or a reference to an enum with any negative declared value with a diagnostic naming the record and field, in each case mirroring the C backend's own established wording style with a `backend_python:`/`docs/design/python-backend.md` pointer swapped in. `namespace_emits_file()` now also checks `ns.enums_size() > 0` (previously records-only), fixing the "enum-only namespace emits nothing" limitation PR-118/PR-119 had both documented and left open. Enums render as real `enum.IntEnum` subclasses (member names/values taken directly from the schema, no case transformation needed since schema enum value names are already conventionally uppercase), always emitted *before* any record in the same file: unlike nested records in the C/C++ backends, no topological sort is needed (an enum is a simple leaf value collection, never itself embedding a field, so there is no cycle to detect), but ordering still matters because a dataclass field annotation (`Optional[Status] = None`) evaluates eagerly when the class body executes, so the enum class must already exist in the module namespace at that point -- verified this by generating and running real Python before writing the automated test coverage. `from enum import IntEnum` is emitted only when the file actually declares an enum (unlike `from typing import Optional`, always emitted regardless, since every dataclass field -- scalar or enum -- uses `Optional[...]`). An enum-typed field reuses the existing `PlannedField` struct exactly (`python_type_hint` now doubles as the enum class name for the `Optional[...]` annotation, `runtime_type_name` doubles as the enum's wire width type name passed to the two new runtime functions, with an added `is_enum` flag selecting which rendering branch applies) -- no parallel field-encoding struct was introduced. New runtime additions, deliberately minimal per this PR's explicit "do not expand the runtime unless genuinely required" instruction: `pack_enum(enum_cls, value, type_name)` validates membership by constructing `enum_cls(value)` (accepting a raw int matching a defined member as validation-by-construction, per PR-117 §8's already-anticipated finding that `IntEnum`'s own constructor raises `ValueError` for undefined values with no extra code needed), converts that `ValueError` to `EncodeError`, then delegates to the existing `pack_scalar` for the actual big-endian encoding; `unpack_enum(enum_cls, type_name, data)` delegates to `unpack_scalar` then constructs `enum_cls` from the decoded integer, converting a `ValueError` to `DecodeError` -- matching the BRF spec's requirement that a decoded enum value the schema does not define is a decode failure, and this PR's explicit "invalid values must raise DecodeError" instruction. Both functions are pure validate-then-delegate: zero new wire-format logic, zero new `struct` usage, reusing `pack_scalar`/`unpack_scalar` entirely. Verified byte-for-byte wire compatibility empirically before writing any interop test, the same discipline established in PR-119: hand-traced the exact expected bytes for a representative enum-plus-scalar record, confirmed the Python runtime's actual output matched, confirmed a raw-int field value equal to a defined member round-trips identically to the enum-member form, and confirmed both encode-time (invalid value) and decode-time (unknown decoded value) rejection manually against a real `python3` process before automating any of it. Tests added: `runtime/python/tests/test_binary_record.py` extended from 42 to 49 cases (new `EnumRoundTripTest` class: round trip by member, round trip by a raw int matching a member, wire-identity with `pack_scalar` for the same width/value, rejection of a non-member value on both encode and decode, wrong-length decode rejection, and a wider (`uint32`) width round trip); `tests/backend_python/backend_python_test.cpp` extended from 15 to 19 cases (enum class generation with correct member ordering before the referencing record, enum wire-width selection for a value exceeding `uint8`, the cross-namespace-enum-field diagnostic, the negative-enum-value diagnostic, and the enum-only-namespace test reworked from "emits nothing" to "emits a file containing just the enum"); `tests/backend_python/python_execution_test.cpp` extended from 6 to 9 cases (enum field round trip including the raw-int-matching-a-member case and absence alongside an enum field, invalid-enum-value-raises-`EncodeError` at encode time, and unknown-decoded-enum-value-raises-`DecodeError` at decode time, this last one hand-computing the exact payload byte offset to corrupt rather than guessing); `tests/interop/python_cpp_c_codec_interop_test.cpp` extended in place (not a new file) with a same-namespace `Status` enum field (`OK`/`WARNING`/`ERROR`, values within int32 range so the C backend can also generate the same schema) added to the existing eleven-scalar-type schema, all three harnesses' set/check logic extended identically, and a new "corrupt the enum field's one-byte payload (the last-declared/highest-field_index field) to a value outside `{0, 1, 2}`" case added to the existing byte-comparison/cross-decode/truncation/trailing-bytes coverage, confirming all three languages reject an out-of-range enum byte identically -- this single extended test satisfies the task's C↔Python, C++↔Python, and three-way interoperability requirements together, since its cross-decode step already exercises every pairwise combination directly. Documentation updated: `docs/design/python-backend.md` (Implementation Status, updated Scope section, new "Enum field lowering" section, new "Enum fields" Generated API section with the ordering rationale, expanded Runtime boundary section documenting `pack_enum`/`unpack_enum`, updated Known Limitations, roadmap with PR-120 marked done and cross-namespace references called out as a separate future step), `docs/distribution-model.md` (updated artifact-classification rows), top-level `README.md`/`tools/README.md` (updated Python feature-matrix paragraphs), this backlog entry. Validation: `cmake --preset debug` / `cmake --build --preset debug --parallel` / `ctest --preset debug --output-on-failure` clean locally including all new/extended Python targets and no regressions in the untouched `backend_c_test`/`c_cpp_codec_interop_test`; `git diff --check` clean; Docker/CI-equivalent validation cycle followed by a full native reconfigure+rebuild+retest (the Docker run's bind-mounted `build/debug` clobbers the native `CMakeCache.txt`, the same restore-afterward discipline every prior PR in this session has followed). No deviations from the frozen PR-117/PR-118/PR-119 architecture were needed; zero modifications to `compiler/backend/` or `compiler/backend_c/` were required (both already fully wired for `--language cpp`/`--language c`). Known limitations carried forward/added: no string, bytes, array, or nested-record field support yet; no cross-namespace enum references (matches the same restriction the C/C++ backends impose); no Python-keyword escaping; the Python runtime epoch and the backend's embedded expected-epoch literal remain two independently-maintained copies of the same number; the `quarry`-namespace-collision-with-the-runtime-package limitation from PR-118 remains unaddressed; `_encoded_size_<name>`'s full-encode-based implementation remains a known, accepted simplicity/performance tradeoff. Recommended next step: string/bytes field support (bounded, fixed-`max_bytes` fields, matching the C/C++ backends' own bounded-capacity model) -- the natural next vertical slice per the roadmap, followed by arrays, then nested records.
- PR-121: Python string and bytes field support -- implements PR-120's recommended next step exactly: bounded `string`/`bytes` fields, using the frozen PR-117/PR-118/PR-119/PR-120 architecture unchanged (no backend redesign, no Schema IR or compiler-pipeline change, no C/C++ backend modification), and the BRF spec's existing "Variable-Length Data Encoding" rules unchanged -- no new wire-format decisions were needed, since string/bytes fields have no field-level length prefix of their own (the Field Directory's own `fieldLength` already supplies the byte count). New runtime additions to `runtime/python/src/quarry/runtime/python/binary_record.py`, deliberately minimal per this PR's explicit "extend the Python runtime only as required" and "do not duplicate UTF-8 validation already provided by Python" instructions: `pack_string(value, max_bytes)` encodes via Python's own `str.encode("utf-8")` (no hand-rolled UTF-8 validator the way the C++/C runtimes must write, since `str` already guarantees well-formed Unicode text and `encode()` itself raises `UnicodeEncodeError` for the one remaining edge case -- a lone surrogate code point a Python `str` can hold but that has no UTF-8 encoding, converted to `EncodeError`), then checks the encoded length against `max_bytes` (`EncodeError` if exceeded); `unpack_string(data, max_bytes)` checks the length bound first (mirroring the C++/C runtimes' own bounds-check-before-content-validate ordering), then decodes via `bytes.decode("utf-8")`, converting `UnicodeDecodeError` to `DecodeError` -- Python's strict-mode UTF-8 codec already rejects overlong encodings, lone continuation bytes, truncated sequences, and encoded surrogate halves, exactly matching the BRF spec's requirements and the C++/C runtimes' custom validators' behavior, with zero validation code written; `pack_bytes`/`unpack_bytes` mirror the string functions' bounds check exactly but perform no UTF-8 validation at all, matching the BRF spec's "bytes" section ("Bytes data may contain any byte sequence... No UTF-8 validation applies"). `compiler/backend_python/backend_python.cpp` extended `PlannedField` with `is_string`/`string_max_bytes` and `is_bytes`/`bytes_max_bytes` (following the established boolean-flag-per-kind pattern `backend_c.cpp`'s own `FieldEncoding` already uses, rather than introducing a tagged-union/enum field-kind type); `lower_field_encoding()` gained `kString`/`kBytes` branches producing `python_type_hint = "str"`/`"bytes"` (trusting Schema IR's already-enforced `validate_positive_u32` guarantee that `max_bytes` is present, positive, and fits `uint32_t`, exactly as both existing backends already do -- nothing re-validated at lowering time); the generic "unsupported field type" diagnostic's supported-types list was updated accordingly, narrowing the still-unsupported list to array and nested-record fields only. Dataclass field rendering needed zero changes (`Optional[<hint>] = None` was already fully generic via `python_type_hint`, so `Optional[str]`/`Optional[bytes]` fell out for free); only the encode/decode helper-body rendering gained two new branches (`is_string`/`is_bytes`) calling `_brf.pack_string(value.<field>, <max_bytes>)`/`_brf.pack_bytes(...)` and `_brf.unpack_string(fields[<idx>], <max_bytes>)`/`_brf.unpack_bytes(...)`. Verified byte-for-byte wire compatibility empirically before writing any interop test, the same discipline established in PR-119/PR-120: manually round-tripped a representative string (`"café"`, chosen specifically to exercise real multi-byte UTF-8 encoding rather than only ASCII) and bytes value against a real generated package and the real runtime module, confirmed encoded bytes matched hand-verification against the BRF spec's byte layout, and confirmed both encode-time (over-length, non-str/non-bytes value, lone-surrogate) and decode-time (over-length, malformed UTF-8) rejection manually via a real `python3` process before automating any of it. Tests added: `runtime/python/tests/test_binary_record.py` extended from 49 to 66 cases (new `StringBytesRoundTripTest` class: round trip, empty value, embedded-U+0000 validity, maximum-length value, over-length rejection on both encode and decode, non-str/non-bytes value rejection on encode, malformed-UTF-8 rejection on decode, lone-surrogate rejection on encode, and bytes-never-validates-UTF-8 round trip including a `bytearray`-accepting case); `tests/backend_python/backend_python_test.cpp` extended from 19 to 20 cases (new string/bytes dataclass-and-helper-generation test; the representative "unsupported field type" test swapped from `string`/`bytes` -- now supported -- to `array`, matching the C backend's own established representative-example-swap pattern, constructed via a real `ArrayType` with a `float32` element type since Schema IR validation requires a fully-formed field type even for a field expected to be rejected); `tests/backend_python/python_execution_test.cpp` extended from 9 to 12 cases (string/bytes round trip covering empty-present/absent/maximum-length/non-UTF-8-binary-content cases, over-length-value-raises-`EncodeError`, and malformed-UTF-8-raises-`DecodeError` at decode time, this last one hand-locating the exact payload byte to corrupt); `tests/interop/python_cpp_c_codec_interop_test.cpp` extended in place (not a new file) with bounded `label` (`string`, `max_bytes=16`, value `"café"` deliberately chosen to include a non-ASCII character so the byte-comparison genuinely exercises UTF-8 encoding, not just ASCII passthrough) and `blob` (`bytes`, `max_bytes=16`, arbitrary non-UTF-8-valid binary content) fields added to the existing scalar-plus-enum schema, all three harnesses' set/check logic extended identically (C via `memcpy` into the generated struct's fixed-capacity buffer plus explicit `_length` field, C++ via `builder.set_label(std::string)`/`builder.set_blob(std::vector<std::byte>)`, Python via keyword arguments), and a new "corrupt one byte of the label field's UTF-8 continuation sequence to an invalid lead byte" case added to the existing byte-comparison/cross-decode/truncation/trailing-bytes/unknown-enum-value coverage -- both new corruption cases (unknown-enum-value and malformed-UTF-8) were refactored to locate their target byte by searching for the label field's own known plaintext bytes (`std::string::find`) rather than hand-computing a payload offset from header/directory sizes, since adding the two new variable-length fields shifted the status field out of its previous "always the last byte" position, and a search-based approach needs no further updates if the schema's field list changes again -- confirming all three languages reject an out-of-range enum byte and malformed UTF-8 identically; this single extended test satisfies the task's "extend the existing three-way interoperability tests" and "verify identical BRF encoding and decoding across C++/C/Python" requirements together. Documentation updated: `docs/design/python-backend.md` (Implementation Status, updated Scope section, new "String/bytes field lowering" section, new "String/bytes fields" Generated API section, expanded Runtime boundary section documenting all four new functions, updated Known Limitations, roadmap with PR-121 marked done), `docs/distribution-model.md` (updated artifact-classification rows), top-level `README.md`/`tools/README.md` (updated Python feature-matrix paragraphs), this backlog entry. Validation: `cmake --preset debug` / `cmake --build --preset debug --parallel` / `ctest --preset debug --output-on-failure` clean locally including all new/extended Python targets and no regressions in the untouched `backend_c_test`/`c_cpp_codec_interop_test`; `git diff --check` clean; Docker/CI-equivalent validation cycle followed by a full native reconfigure+rebuild+retest (the Docker run's bind-mounted `build/debug` clobbers the native `CMakeCache.txt`, the same restore-afterward discipline every prior PR in this session has followed). No deviations from the frozen PR-117/PR-118/PR-119/PR-120 architecture were needed; zero modifications to `compiler/backend/` or `compiler/backend_c/` were required. Known limitations carried forward/added: no array or nested-record field support yet; no cross-namespace enum references; no Python-keyword escaping; the Python runtime epoch and the backend's embedded expected-epoch literal remain two independently-maintained copies of the same number; the `quarry`-namespace-collision-with-the-runtime-package limitation from PR-118 remains unaddressed; `_encoded_size_<name>`'s full-encode-based implementation remains a known, accepted simplicity/performance tradeoff. Recommended next step: array field support (bounded arrays of the already-supported scalar/enum element kinds, matching the C/C++ backends' own bounded-capacity array model) -- the natural next vertical slice per the roadmap, followed by nested records.
- PR-123: Python arrays of bounded strings and bytes -- extends the frozen PR-117 through PR-122 Python backend architecture without Schema IR, compiler-pipeline, C/C++ wire-format, or generated public API changes. The backend now lowers array element `string`/`bytes` Schema IR nodes to `Optional[list[str]]`/`Optional[list[bytes]]`; `None` remains absent and an empty list remains present with a zero count. The runtime adds only variable-width array helpers that reuse `pack_string`/`unpack_string` and `pack_bytes`/`unpack_bytes`: count varuint, per-element length varuint, exact raw element bytes, max-elements/max-bytes checks, strict UTF-8 for strings, arbitrary bytes for bytes, and exact payload consumption with offset/index context in Python exception messages. The C backend already rejects string/bytes array elements, so three-way interop was deliberately not expanded; Python/C++ coverage is documented as the applicable interoperability boundary. Focused runtime and generated-code tests cover absent/present-empty semantics, empty elements, UTF-8 and arbitrary binary content, bounds, malformed varuints, truncation, malformed UTF-8, and trailing data. No deviations from the approved scope.
- PR-124: Python nested record support -- extends the frozen PR-117 through PR-123 Python backend architecture without Schema IR, compiler-pipeline, wire-format, runtime-framework, or generated public API changes. Same-namespace nested record fields render as `Optional[Child]`, preserve absent versus present-empty child records, and compose the existing generated child encode/decode helpers as embedded BRF records. The planner uses a deterministic same-namespace dependency order and rejects recursive by-value cycles; record arrays, nested arrays, and cross-namespace record references remain unsupported. No new runtime helper was required. Added backend-generation, runtime composition, real Python execution, malformed nested-payload, and three-way C/C++/Python interoperability coverage, including byte identity, pairwise cross-decoding, and trailing bytes inside an embedded record. No deviations from the approved scope.
- PR-125: Python arrays of same-namespace records -- renders `Optional[list[Child]]`, preserves absent versus present-empty semantics, and uses existing varuint framing plus generated child record codecs for complete embedded BRF elements. Nested arrays and cross-namespace references remain unsupported. Added generation and real-interpreter execution coverage for bounds, malformed/truncated elements, and trailing payloads. No runtime helper or architecture deviation was required.
- PR-128: Validate Python runtime wheel/sdist distribution and clean downstream installation. The runtime package metadata now declares its README, Apache-2.0 license, Python 3.9+ requirement, and optional standard packaging test tools. A clean packaging test builds wheel and sdist, inspects artifact contents, installs the wheel and a wheel rebuilt from the sdist into isolated virtual environments, installs the Quarry compiler to a clean prefix, generates YAML-expressible Python code, runs scalar/enum/string/bytes/array encode/decode assertions without repository imports, and verifies epoch-mismatch ImportError diagnostics. No PyPI publication, BRF, Schema IR, compiler-pipeline, or generated public API changes.
- PR-131: Complete C backend support for bounded arrays of bounded strings and bytes. Generated C uses named per-field fixed-capacity element structs with inline `char`/`uint8_t` storage and a per-element `uint32_t length`, plus the existing fixed-capacity array storage and count. Encode/decode reuse existing varuint and bounded-copy/UTF-8 runtime primitives, add no runtime helpers, preserve C generated-code API epoch 2, and remain strict C99 with no heap allocation. Extended backend generation and three-way C/C++/Python interoperability coverage; cross-namespace references and nested arrays remain deferred. No Schema IR or BRF changes.
- PR-135: Build the source-unit import graph and compiler-context foundation for future cross-namespace resolution. YAML imports are relative non-empty scalar paths; canonical filesystem paths suppress repeated loads, dependency-first DFS ordering is deterministic, cycles and duplicate namespace/primary-record source identities are diagnosed, and imported source buffers/edges are retained by `CompilerContext`. External type resolution, aggregate Schema IR, backend dependencies, imported output planning, and multi-record YAML remain deferred. No BRF, runtime, Schema IR, or generated-code epoch changes.
- PR-136: Build one compiler-wide record/enum symbol index from the PR-135 source-unit graph and resolve dotted qualified references before layout/Schema IR lowering. Local lookup remains lexical, imported documents are validated through the shared semantic pass, duplicate FQNs are diagnosed, and Schema IR, BRF, runtimes, generated APIs, backend dependencies, and output planning remain unchanged. Next: PR-137 output planning and backend dependency generation.
