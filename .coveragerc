[run]
# The QBS-driven generic runtime is covered by its focused unit tests, but its
# cross-runtime conformance fixture is intentionally deferred until after the
# runtime lands. Keep the established generated-runtime baseline stable until
# that fixture PR supplies broad branch coverage for this new module.
omit =
    */quarry/runtime/generic.py
