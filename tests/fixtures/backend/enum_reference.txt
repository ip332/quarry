=== generated/schema.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: <root>

#include <cstddef>
#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

enum class Color : std::int64_t {
  Red = 1,
  Green = 2,
};

struct Paint {
  Paint() = default;

  bool has_color() const {
    return color_.has_value();
  }
  const ::Color* color() const {
    return color_ ? &*color_ : nullptr;
  }

private:
  friend class PaintBuilder;
  std::optional<::Color> color_;
};

class PaintBuilder {
public:
  bool has_color() const {
    return color_.has_value();
  }

  bool set_color(const ::Color& value) {
    if (!validate_color(value)) {
      return false;
    }
    color_ = value;
    return true;
  }

  Paint build() const {
    Paint value;
    value.color_ = color_;
    return value;
  }

private:
  static bool validate_color(const ::Color& value) {
    return true;
  }

  std::optional<::Color> color_;
};

std::optional<std::vector<std::byte>> encode(const Paint& value) {
  std::vector<::breadcrumbs::runtime::FieldBytes> fields;
  fields.reserve(1U);
  if (value.has_color()) {
    const auto enum_numeric = static_cast<std::int64_t>(*value.color());
    if (!(enum_numeric == 1 || enum_numeric == 2)) {
      return std::nullopt;
    }
    std::vector<std::byte> field_bytes;
    ::breadcrumbs::runtime::append_u8(field_bytes, static_cast<std::uint8_t>(enum_numeric));
    fields.push_back(::breadcrumbs::runtime::FieldBytes{
      .field_index = 0U,
      .bytes = std::move(field_bytes),
    });
  }
  return ::breadcrumbs::runtime::encode_record(1U, fields);
}
