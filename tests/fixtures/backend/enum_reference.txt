=== generated/schema.generated.hpp ===
// Generated by Quarry.
// Namespace: <root>

#include <cstddef>
#include <cstdint>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 2U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

enum class Color : std::int64_t {
  Red = 1,
  Green = 2,
};

struct Paint {
  Paint() = default;

  bool has_color() const {
    return color_.has_value();
  }
  const ::Color* color() const {
    return color_ ? &*color_ : nullptr;
  }

private:
  friend class PaintBuilder;
  std::optional<::Color> color_;
};

class PaintBuilder {
public:
  bool has_color() const {
    return color_.has_value();
  }

  bool set_color(const ::Color& value) {
    if (!validate_color(value)) {
      return false;
    }
    color_ = value;
    return true;
  }

  Paint build() const {
    Paint value;
    value.color_ = color_;
    return value;
  }

private:
  static bool validate_color(const ::Color& value) {
    return true;
  }

  std::optional<::Color> color_;
};

::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Paint& value) {
  std::vector<::quarry::runtime::FieldBytes> fields;
  fields.reserve(1U);
  if (value.has_color()) {
    const auto enum_numeric = static_cast<std::int64_t>(*value.color());
    if (!(enum_numeric == 1 || enum_numeric == 2)) {
      return ::quarry::runtime::encode_failure<std::vector<std::byte>>(::quarry::runtime::EncodeError::unknown_enum_value, {{.field_index = 0U, .array_index = std::nullopt}});
    }
    std::vector<std::byte> field_bytes;
    ::quarry::runtime::append_u8(field_bytes, static_cast<std::uint8_t>(enum_numeric));
    fields.push_back(::quarry::runtime::FieldBytes{
      .field_index = 0U,
      .bytes = std::move(field_bytes),
    });
  }
  return ::quarry::runtime::encode_record_result(1U, fields);
}


std::optional<std::vector<std::byte>> encode(const Paint& value) {
  auto encoded = encode_result(value);
  if (!encoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(encoded.value);
}


::quarry::runtime::DecodeResult<Paint> decode_Paint_result(std::span<const std::byte> input) {
  const auto parsed = ::quarry::runtime::parse_record(input);
  if (!parsed.record.has_value()) {
    return ::quarry::runtime::decode_failure<Paint>(parsed.error);
  }
  if (parsed.record->record_id != 1U) {
    return ::quarry::runtime::decode_failure<Paint>(::quarry::runtime::DecodeError::unexpected_record_id);
  }
  PaintBuilder builder;
  if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 0U); field != nullptr) {
    const auto decoded = ::quarry::runtime::read_u8(field->bytes);
    if (!decoded.value.has_value()) {
      return ::quarry::runtime::decode_failure<Paint>(decoded.error, {{.field_index = 0U, .array_index = std::nullopt}});
    }
    const auto enum_numeric = static_cast<std::int64_t>(*decoded.value);
    if (!(enum_numeric == 1 || enum_numeric == 2)) {
      return ::quarry::runtime::decode_failure<Paint>(::quarry::runtime::DecodeError::unknown_enum_value, {{.field_index = 0U, .array_index = std::nullopt}});
    }
    if (!builder.set_color(static_cast<::Color>(enum_numeric))) {
      return ::quarry::runtime::decode_failure<Paint>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}});
    }
  }
  return ::quarry::runtime::decoded_value<Paint>(builder.build());
}


std::optional<Paint> decode_Paint(std::span<const std::byte> input) {
  auto decoded = decode_Paint_result(input);
  if (!decoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(decoded.value);
}
