=== generated/alpha/one.generated.hpp ===
// Generated by Quarry.
// Namespace: alpha.one

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "quarry/runtime/binary_record_v2.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 3U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

namespace alpha {
namespace one {

    inline const ::quarry::runtime::BrfV2RecordLayout& brf_v2_layout_First() {
      static const ::quarry::runtime::BrfV2RecordLayout layout{
        .record_id = 1U,
        .presence_bitmap_size = 0U,
        .fixed_region_size = 0U,
        .complete_fixed_record_size = std::optional<std::uint32_t>{16U},
        .fields = {
        },
      };
      return layout;
    }

    struct First {};

    class FirstBuilder {
    public:
      First build() const {
        return First{};
      }
    };

    ::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const First& value) {
      std::vector<::quarry::runtime::FieldBytes> fields;
      (void)value;
      return ::quarry::runtime::encode_brf_v2_record_result(brf_v2_layout_First(), fields);
    }


    std::optional<std::vector<std::byte>> encode(const First& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::quarry::runtime::DecodeResult<First> decode_First_result(std::span<const std::byte> input) {
      const auto parsed = ::quarry::runtime::parse_brf_v2_record(input, brf_v2_layout_First());
      if (!parsed.record.has_value()) {
        return ::quarry::runtime::decode_failure<First>(::quarry::runtime::brf_v2_decode_error(parsed.error), {}, parsed.offset);
      }
      if (parsed.record->record_id != 1U) {
        return ::quarry::runtime::decode_failure<First>(::quarry::runtime::DecodeError::unexpected_record_id, {}, 0U);
      }
      FirstBuilder builder;
      return ::quarry::runtime::decoded_value<First>(builder.build());
    }


    std::optional<First> decode_First(std::span<const std::byte> input) {
      auto decoded = decode_First_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

} // namespace one
} // namespace alpha
=== generated/beta/two.generated.hpp ===
// Generated by Quarry.
// Namespace: beta.two

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "quarry/runtime/binary_record_v2.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 3U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

namespace beta {
namespace two {

    inline const ::quarry::runtime::BrfV2RecordLayout& brf_v2_layout_Second() {
      static const ::quarry::runtime::BrfV2RecordLayout layout{
        .record_id = 2U,
        .presence_bitmap_size = 0U,
        .fixed_region_size = 0U,
        .complete_fixed_record_size = std::optional<std::uint32_t>{16U},
        .fields = {
        },
      };
      return layout;
    }

    struct Second {};

    class SecondBuilder {
    public:
      Second build() const {
        return Second{};
      }
    };

    ::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Second& value) {
      std::vector<::quarry::runtime::FieldBytes> fields;
      (void)value;
      return ::quarry::runtime::encode_brf_v2_record_result(brf_v2_layout_Second(), fields);
    }


    std::optional<std::vector<std::byte>> encode(const Second& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::quarry::runtime::DecodeResult<Second> decode_Second_result(std::span<const std::byte> input) {
      const auto parsed = ::quarry::runtime::parse_brf_v2_record(input, brf_v2_layout_Second());
      if (!parsed.record.has_value()) {
        return ::quarry::runtime::decode_failure<Second>(::quarry::runtime::brf_v2_decode_error(parsed.error), {}, parsed.offset);
      }
      if (parsed.record->record_id != 2U) {
        return ::quarry::runtime::decode_failure<Second>(::quarry::runtime::DecodeError::unexpected_record_id, {}, 0U);
      }
      SecondBuilder builder;
      return ::quarry::runtime::decoded_value<Second>(builder.build());
    }


    std::optional<Second> decode_Second(std::span<const std::byte> input) {
      auto decoded = decode_Second_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

} // namespace two
} // namespace beta