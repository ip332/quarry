=== generated/alpha/one.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: alpha.one

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

static_assert(::breadcrumbs::runtime::kGeneratedCodeApiVersion == 1U,
              "Generated Breadcrumbs code is incompatible with the installed Breadcrumbs runtime. Regenerate the code using a compatible breadcrumbs-schema-compiler release.");

namespace alpha {
namespace one {

    struct First {};

    class FirstBuilder {
    public:
      First build() const {
        return First{};
      }
    };

    ::breadcrumbs::runtime::EncodeResult<std::vector<std::byte>> encode_result(const First& value) {
      std::vector<::breadcrumbs::runtime::FieldBytes> fields;
      (void)value;
      return ::breadcrumbs::runtime::encode_record_result(1U, fields);
    }


    std::optional<std::vector<std::byte>> encode(const First& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::breadcrumbs::runtime::DecodeResult<First> decode_First_result(std::span<const std::byte> input) {
      const auto parsed = ::breadcrumbs::runtime::parse_record(input);
      if (!parsed.record.has_value()) {
        return ::breadcrumbs::runtime::decode_failure<First>(parsed.error);
      }
      if (parsed.record->record_id != 1U) {
        return ::breadcrumbs::runtime::decode_failure<First>(::breadcrumbs::runtime::DecodeError::unexpected_record_id);
      }
      FirstBuilder builder;
      return ::breadcrumbs::runtime::decoded_value<First>(builder.build());
    }


    std::optional<First> decode_First(std::span<const std::byte> input) {
      auto decoded = decode_First_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

} // namespace one
} // namespace alpha
=== generated/beta/two.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: beta.two

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

static_assert(::breadcrumbs::runtime::kGeneratedCodeApiVersion == 1U,
              "Generated Breadcrumbs code is incompatible with the installed Breadcrumbs runtime. Regenerate the code using a compatible breadcrumbs-schema-compiler release.");

namespace beta {
namespace two {

    struct Second {};

    class SecondBuilder {
    public:
      Second build() const {
        return Second{};
      }
    };

    ::breadcrumbs::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Second& value) {
      std::vector<::breadcrumbs::runtime::FieldBytes> fields;
      (void)value;
      return ::breadcrumbs::runtime::encode_record_result(2U, fields);
    }


    std::optional<std::vector<std::byte>> encode(const Second& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::breadcrumbs::runtime::DecodeResult<Second> decode_Second_result(std::span<const std::byte> input) {
      const auto parsed = ::breadcrumbs::runtime::parse_record(input);
      if (!parsed.record.has_value()) {
        return ::breadcrumbs::runtime::decode_failure<Second>(parsed.error);
      }
      if (parsed.record->record_id != 2U) {
        return ::breadcrumbs::runtime::decode_failure<Second>(::breadcrumbs::runtime::DecodeError::unexpected_record_id);
      }
      SecondBuilder builder;
      return ::breadcrumbs::runtime::decoded_value<Second>(builder.build());
    }


    std::optional<Second> decode_Second(std::span<const std::byte> input) {
      auto decoded = decode_Second_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

} // namespace two
} // namespace beta
