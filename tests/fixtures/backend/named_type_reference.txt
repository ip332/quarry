=== generated/quarry/geo.generated.hpp ===
// Generated by Quarry.
// Namespace: quarry.geo

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "quarry/runtime/binary_record.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 3U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

namespace quarry {
namespace geo {

    struct Location {};

    class LocationBuilder {
    public:
      Location build() const {
        return Location{};
      }
    };

    ::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Location& value) {
      std::vector<::quarry::runtime::FieldBytes> fields;
      (void)value;
      return ::quarry::runtime::encode_record_result(1U, fields);
    }


    std::optional<std::vector<std::byte>> encode(const Location& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::quarry::runtime::DecodeResult<Location> decode_Location_result(std::span<const std::byte> input) {
      const auto parsed = ::quarry::runtime::parse_record(input);
      if (!parsed.record.has_value()) {
        return ::quarry::runtime::decode_failure<Location>(parsed.error, {}, parsed.offset);
      }
      if (parsed.record->record_id != 1U) {
        return ::quarry::runtime::decode_failure<Location>(::quarry::runtime::DecodeError::unexpected_record_id, {}, 0U);
      }
      LocationBuilder builder;
      return ::quarry::runtime::decoded_value<Location>(builder.build());
    }


    std::optional<Location> decode_Location(std::span<const std::byte> input) {
      auto decoded = decode_Location_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

    struct Route {
      Route() = default;

      bool has_origin() const {
        return origin_.has_value();
      }
      const ::quarry::geo::Location* origin() const {
        return origin_ ? &*origin_ : nullptr;
      }
      bool has_fallback() const {
        return fallback_.has_value();
      }
      const ::quarry::geo::Location* fallback() const {
        return fallback_ ? &*fallback_ : nullptr;
      }

    private:
      friend class RouteBuilder;
      std::optional<::quarry::geo::Location> origin_;
      std::optional<::quarry::geo::Location> fallback_;
    };

    class RouteBuilder {
    public:
      bool has_origin() const {
        return origin_.has_value();
      }
      bool has_fallback() const {
        return fallback_.has_value();
      }

      bool set_origin(const ::quarry::geo::Location& value) {
        if (!validate_origin(value)) {
          return false;
        }
        origin_ = value;
        return true;
      }
      bool set_fallback(const ::quarry::geo::Location& value) {
        if (!validate_fallback(value)) {
          return false;
        }
        fallback_ = value;
        return true;
      }

      Route build() const {
        Route value;
        value.origin_ = origin_;
        value.fallback_ = fallback_;
        return value;
      }

    private:
      static bool validate_origin(const ::quarry::geo::Location& value) {
        return true;
      }
      static bool validate_fallback(const ::quarry::geo::Location& value) {
        return true;
      }

      std::optional<::quarry::geo::Location> origin_;
      std::optional<::quarry::geo::Location> fallback_;
    };

    ::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Route& value) {
      std::vector<::quarry::runtime::FieldBytes> fields;
      fields.reserve(2U);
      if (value.has_origin()) {
        auto field_bytes = ::quarry::geo::encode_result(*value.origin());
        if (!field_bytes.has_value()) {
          auto nested_path = std::move(field_bytes.path);
          nested_path.push_back(::quarry::runtime::PathElement{
            .field_index = 0U,
            .array_index = std::nullopt,
          });
          return ::quarry::runtime::encode_failure<std::vector<std::byte>>(field_bytes.error, std::move(nested_path));
        }
        fields.push_back(::quarry::runtime::FieldBytes{
          .field_index = 0U,
          .bytes = std::move(*field_bytes.value),
        });
      }
      if (value.has_fallback()) {
        auto field_bytes = ::quarry::geo::encode_result(*value.fallback());
        if (!field_bytes.has_value()) {
          auto nested_path = std::move(field_bytes.path);
          nested_path.push_back(::quarry::runtime::PathElement{
            .field_index = 1U,
            .array_index = std::nullopt,
          });
          return ::quarry::runtime::encode_failure<std::vector<std::byte>>(field_bytes.error, std::move(nested_path));
        }
        fields.push_back(::quarry::runtime::FieldBytes{
          .field_index = 1U,
          .bytes = std::move(*field_bytes.value),
        });
      }
      return ::quarry::runtime::encode_record_result(2U, fields);
    }


    std::optional<std::vector<std::byte>> encode(const Route& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::quarry::runtime::DecodeResult<Route> decode_Route_result(std::span<const std::byte> input) {
      const auto parsed = ::quarry::runtime::parse_record(input);
      if (!parsed.record.has_value()) {
        return ::quarry::runtime::decode_failure<Route>(parsed.error, {}, parsed.offset);
      }
      if (parsed.record->record_id != 2U) {
        return ::quarry::runtime::decode_failure<Route>(::quarry::runtime::DecodeError::unexpected_record_id, {}, 0U);
      }
      RouteBuilder builder;
      if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 0U); field != nullptr) {
        auto decoded = ::quarry::geo::decode_Location_result(field->bytes);
        if (!decoded.value.has_value()) {
          auto nested_path = std::move(decoded.path);
          nested_path.push_back(::quarry::runtime::PathElement{
            .field_index = 0U,
            .array_index = std::nullopt,
          });
          const std::optional<std::uint64_t> nested_offset = decoded.byte_offset.has_value()
            ? std::optional<std::uint64_t>(field->field_offset + *decoded.byte_offset)
            : std::nullopt;
          return ::quarry::runtime::decode_failure<Route>(decoded.error, std::move(nested_path), nested_offset);
        }
        if (!builder.set_origin(*decoded.value)) {
          return ::quarry::runtime::decode_failure<Route>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
        }
      }
      if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 1U); field != nullptr) {
        auto decoded = ::quarry::geo::decode_Location_result(field->bytes);
        if (!decoded.value.has_value()) {
          auto nested_path = std::move(decoded.path);
          nested_path.push_back(::quarry::runtime::PathElement{
            .field_index = 1U,
            .array_index = std::nullopt,
          });
          const std::optional<std::uint64_t> nested_offset = decoded.byte_offset.has_value()
            ? std::optional<std::uint64_t>(field->field_offset + *decoded.byte_offset)
            : std::nullopt;
          return ::quarry::runtime::decode_failure<Route>(decoded.error, std::move(nested_path), nested_offset);
        }
        if (!builder.set_fallback(*decoded.value)) {
          return ::quarry::runtime::decode_failure<Route>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 1U, .array_index = std::nullopt}}, field->field_offset);
        }
      }
      return ::quarry::runtime::decoded_value<Route>(builder.build());
    }


    std::optional<Route> decode_Route(std::span<const std::byte> input) {
      auto decoded = decode_Route_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

} // namespace geo
} // namespace quarry
