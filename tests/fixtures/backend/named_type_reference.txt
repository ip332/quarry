=== generated/breadcrumbs/geo.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: breadcrumbs.geo

#include <cstddef>
#include <optional>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

namespace breadcrumbs {
namespace geo {

    struct Location {};

    class LocationBuilder {
    public:
      Location build() const {
        return Location{};
      }
    };

    std::optional<std::vector<std::byte>> encode(const Location& value) {
      std::vector<::breadcrumbs::runtime::FieldBytes> fields;
      (void)value;
      return ::breadcrumbs::runtime::encode_record(1U, fields);
    }

    struct Route {
      Route() = default;

      bool has_origin() const {
        return origin_.has_value();
      }
      const ::breadcrumbs::geo::Location* origin() const {
        return origin_ ? &*origin_ : nullptr;
      }
      bool has_fallback() const {
        return fallback_.has_value();
      }
      const ::breadcrumbs::geo::Location* fallback() const {
        return fallback_ ? &*fallback_ : nullptr;
      }

    private:
      friend class RouteBuilder;
      std::optional<::breadcrumbs::geo::Location> origin_;
      std::optional<::breadcrumbs::geo::Location> fallback_;
    };

    class RouteBuilder {
    public:
      bool has_origin() const {
        return origin_.has_value();
      }
      bool has_fallback() const {
        return fallback_.has_value();
      }

      bool set_origin(const ::breadcrumbs::geo::Location& value) {
        if (!validate_origin(value)) {
          return false;
        }
        origin_ = value;
        return true;
      }
      bool set_fallback(const ::breadcrumbs::geo::Location& value) {
        if (!validate_fallback(value)) {
          return false;
        }
        fallback_ = value;
        return true;
      }

      Route build() const {
        Route value;
        value.origin_ = origin_;
        value.fallback_ = fallback_;
        return value;
      }

    private:
      static bool validate_origin(const ::breadcrumbs::geo::Location& value) {
        return true;
      }
      static bool validate_fallback(const ::breadcrumbs::geo::Location& value) {
        return true;
      }

      std::optional<::breadcrumbs::geo::Location> origin_;
      std::optional<::breadcrumbs::geo::Location> fallback_;
    };

    std::optional<std::vector<std::byte>> encode(const Route& value) {
      std::vector<::breadcrumbs::runtime::FieldBytes> fields;
      fields.reserve(2U);
      if (value.has_origin()) {
        return std::nullopt;
      }
      if (value.has_fallback()) {
        return std::nullopt;
      }
      return ::breadcrumbs::runtime::encode_record(2U, fields);
    }

} // namespace geo
} // namespace breadcrumbs
