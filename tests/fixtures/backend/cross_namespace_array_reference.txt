=== generated/alpha/one.generated.hpp ===
// Generated by Quarry.
// Namespace: alpha.one

#include <cstddef>
#include <cstdint>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "quarry/runtime/binary_record_v2.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 3U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

namespace alpha {
namespace one {

    enum class Mode : std::int64_t {
      Off = 0,
      On = 1,
    };

    inline const ::quarry::runtime::BrfV2RecordLayout& brf_v2_layout_Element() {
      static const ::quarry::runtime::BrfV2RecordLayout layout{
        .record_id = 1U,
        .presence_bitmap_size = 1U,
        .fixed_region_size = 5U,
        .complete_fixed_record_size = std::optional<std::uint32_t>{21U},
        .fields = {
          ::quarry::runtime::BrfV2FieldLayout{0U, 0U, 17U, 0U, 32U, 4U, ::quarry::runtime::BrfV2FieldStorage::Fixed, {}} ,
        },
      };
      return layout;
    }

    struct Element {
      Element() = default;

      bool has_id() const {
        return id_.has_value();
      }
      const std::uint32_t* id() const {
        return id_ ? &*id_ : nullptr;
      }

    private:
      friend class ElementBuilder;
      std::optional<std::uint32_t> id_;
    };

    class ElementBuilder {
    public:
      bool has_id() const {
        return id_.has_value();
      }

      bool set_id(const std::uint32_t& value) {
        if (!validate_id(value)) {
          return false;
        }
        id_ = value;
        return true;
      }

      Element build() const {
        Element value;
        value.id_ = id_;
        return value;
      }

    private:
      static bool validate_id(const std::uint32_t& value) {
        return true;
      }

      std::optional<std::uint32_t> id_;
    };

    ::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Element& value) {
      std::vector<::quarry::runtime::FieldBytes> fields;
      fields.reserve(1U);
      if (value.has_id()) {
        std::vector<std::byte> field_bytes;
        if (!::quarry::runtime::append_u32(field_bytes, *value.id())) {
          return ::quarry::runtime::encode_failure<std::vector<std::byte>>(::quarry::runtime::EncodeError::overflow, {{.field_index = 0U, .array_index = std::nullopt}});
        }
        fields.push_back(::quarry::runtime::FieldBytes{
          .field_index = 0U,
          .bytes = std::move(field_bytes),
        });
      }
      return ::quarry::runtime::encode_brf_v2_record_result(brf_v2_layout_Element(), fields);
    }


    std::optional<std::vector<std::byte>> encode(const Element& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::quarry::runtime::DecodeResult<Element> decode_Element_result(std::span<const std::byte> input) {
      const auto parsed = ::quarry::runtime::parse_brf_v2_record(input, brf_v2_layout_Element());
      if (!parsed.record.has_value()) {
        return ::quarry::runtime::decode_failure<Element>(::quarry::runtime::brf_v2_decode_error(parsed.error), {}, parsed.offset);
      }
      if (parsed.record->record_id != 1U) {
        return ::quarry::runtime::decode_failure<Element>(::quarry::runtime::DecodeError::unexpected_record_id, {}, 0U);
      }
      ElementBuilder builder;
      if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 0U); field != nullptr) {
        const auto decoded = ::quarry::runtime::read_u32(field->bytes);
        if (!decoded.value.has_value()) {
          return ::quarry::runtime::decode_failure<Element>(decoded.error, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
        }
        if (!builder.set_id(*decoded.value)) {
          return ::quarry::runtime::decode_failure<Element>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
        }
      }
      return ::quarry::runtime::decoded_value<Element>(builder.build());
    }


    std::optional<Element> decode_Element(std::span<const std::byte> input) {
      auto decoded = decode_Element_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

} // namespace one
} // namespace alpha
=== generated/beta/two.generated.hpp ===
// Generated by Quarry.
// Namespace: beta.two

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "alpha/one.generated.hpp"
#include "quarry/runtime/binary_record_v2.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 3U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

namespace beta {
namespace two {

    inline const ::quarry::runtime::BrfV2RecordLayout& brf_v2_layout_Basket() {
      static const ::quarry::runtime::BrfV2RecordLayout layout{
        .record_id = 2U,
        .presence_bitmap_size = 1U,
        .fixed_region_size = 17U,
        .complete_fixed_record_size = std::nullopt,
        .fields = {
          ::quarry::runtime::BrfV2FieldLayout{0U, 0U, 17U, 0U, 64U, 8U, ::quarry::runtime::BrfV2FieldStorage::VariableDescriptor, {}} ,
          ::quarry::runtime::BrfV2FieldLayout{1U, 1U, 25U, 0U, 64U, 8U, ::quarry::runtime::BrfV2FieldStorage::VariableDescriptor, {}} ,
        },
      };
      return layout;
    }

    struct Basket {
      Basket() = default;

      bool has_elements() const {
        return elements_.has_value();
      }
      const std::vector<::alpha::one::Element>* elements() const {
        return elements_ ? &*elements_ : nullptr;
      }
      bool has_modes() const {
        return modes_.has_value();
      }
      const std::vector<::alpha::one::Mode>* modes() const {
        return modes_ ? &*modes_ : nullptr;
      }

    private:
      friend class BasketBuilder;
      std::optional<std::vector<::alpha::one::Element>> elements_;
      std::optional<std::vector<::alpha::one::Mode>> modes_;
    };

    class BasketBuilder {
    public:
      bool has_elements() const {
        return elements_.has_value();
      }
      bool has_modes() const {
        return modes_.has_value();
      }

      bool set_elements(const std::vector<::alpha::one::Element>& value) {
        if (!validate_elements(value)) {
          return false;
        }
        elements_ = value;
        return true;
      }
      bool set_modes(const std::vector<::alpha::one::Mode>& value) {
        if (!validate_modes(value)) {
          return false;
        }
        modes_ = value;
        return true;
      }

      Basket build() const {
        Basket value;
        value.elements_ = elements_;
        value.modes_ = modes_;
        return value;
      }

    private:
      static bool validate_elements(const std::vector<::alpha::one::Element>& value) {
        if (value.size() > 2) {
          return false;
        }
        for (const auto& element_0 : value) {
        }
        return true;
      }
      static bool validate_modes(const std::vector<::alpha::one::Mode>& value) {
        if (value.size() > 2) {
          return false;
        }
        for (const auto& element_0 : value) {
        }
        return true;
      }

      std::optional<std::vector<::alpha::one::Element>> elements_;
      std::optional<std::vector<::alpha::one::Mode>> modes_;
    };

    ::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Basket& value) {
      std::vector<::quarry::runtime::FieldBytes> fields;
      fields.reserve(2U);
      if (value.has_elements()) {
        if (value.elements()->size() > 2U) {
          return ::quarry::runtime::encode_failure<std::vector<std::byte>>(::quarry::runtime::EncodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}});
        }
        std::vector<std::byte> field_bytes;
        ::quarry::runtime::append_varuint(field_bytes, value.elements()->size());
        for (std::size_t element_index = 0U; element_index < value.elements()->size(); ++element_index) {
          const auto& element = (*value.elements())[element_index];
          auto element_bytes = ::alpha::one::encode_result(element);
          if (!element_bytes.has_value()) {
            auto nested_path = std::move(element_bytes.path);
            nested_path.push_back(::quarry::runtime::PathElement{
              .field_index = 0U,
              .array_index = static_cast<std::uint32_t>(element_index),
            });
            return ::quarry::runtime::encode_failure<std::vector<std::byte>>(element_bytes.error, std::move(nested_path));
          }
          ::quarry::runtime::append_varuint(field_bytes, element_bytes.value->size());
          ::quarry::runtime::append_bytes(field_bytes, std::span<const std::byte>(element_bytes.value->data(), element_bytes.value->size()));
        }
        fields.push_back(::quarry::runtime::FieldBytes{
          .field_index = 0U,
          .bytes = std::move(field_bytes),
        });
      }
      if (value.has_modes()) {
        if (value.modes()->size() > 2U) {
          return ::quarry::runtime::encode_failure<std::vector<std::byte>>(::quarry::runtime::EncodeError::bounds_exceeded, {{.field_index = 1U, .array_index = std::nullopt}});
        }
        std::vector<std::byte> field_bytes;
        ::quarry::runtime::append_varuint(field_bytes, value.modes()->size());
        for (std::size_t element_index = 0U; element_index < value.modes()->size(); ++element_index) {
          const auto& element = (*value.modes())[element_index];
          const auto enum_numeric = static_cast<std::int64_t>(element);
          if (!(enum_numeric == 0 || enum_numeric == 1)) {
            return ::quarry::runtime::encode_failure<std::vector<std::byte>>(::quarry::runtime::EncodeError::unknown_enum_value, {{.field_index = 1U, .array_index = static_cast<std::uint32_t>(element_index)}});
          }
          ::quarry::runtime::append_u8(field_bytes, static_cast<std::uint8_t>(enum_numeric));
        }
        fields.push_back(::quarry::runtime::FieldBytes{
          .field_index = 1U,
          .bytes = std::move(field_bytes),
        });
      }
      return ::quarry::runtime::encode_brf_v2_record_result(brf_v2_layout_Basket(), fields);
    }


    std::optional<std::vector<std::byte>> encode(const Basket& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::quarry::runtime::DecodeResult<Basket> decode_Basket_result(std::span<const std::byte> input) {
      const auto parsed = ::quarry::runtime::parse_brf_v2_record(input, brf_v2_layout_Basket());
      if (!parsed.record.has_value()) {
        return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::brf_v2_decode_error(parsed.error), {}, parsed.offset);
      }
      if (parsed.record->record_id != 2U) {
        return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::DecodeError::unexpected_record_id, {}, 0U);
      }
      BasketBuilder builder;
      if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 0U); field != nullptr) {
        std::size_t element_offset = 0U;
        const auto decoded_count = ::quarry::runtime::read_varuint(field->bytes, element_offset);
        if (!decoded_count.value.has_value() || *decoded_count.value > 2U) {
          if (!decoded_count.value.has_value()) {
            return ::quarry::runtime::decode_failure<Basket>(decoded_count.error, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
          }
          return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
        }
        const auto element_count = static_cast<std::size_t>(*decoded_count.value);
        std::vector<::alpha::one::Element> elements;
        elements.reserve(element_count);
        for (std::size_t index = 0U; index < element_count; ++index) {
          const std::size_t element_length_start = element_offset;
          const auto decoded_length = ::quarry::runtime::read_varuint(field->bytes, element_offset);
          if (!decoded_length.value.has_value()) {
            return ::quarry::runtime::decode_failure<Basket>(decoded_length.error, {{.field_index = 0U, .array_index = static_cast<std::uint32_t>(index)}}, field->field_offset + element_length_start);
          }
          if (*decoded_length.value > field->bytes.size() - element_offset) {
            return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::DecodeError::invalid_field_length, {{.field_index = 0U, .array_index = static_cast<std::uint32_t>(index)}}, field->field_offset + element_offset);
          }
          const auto element_length = static_cast<std::size_t>(*decoded_length.value);
          const auto element_bytes = field->bytes.subspan(element_offset, element_length);
          auto decoded = ::alpha::one::decode_Element_result(element_bytes);
          if (!decoded.value.has_value()) {
            auto nested_path = std::move(decoded.path);
            nested_path.push_back(::quarry::runtime::PathElement{
              .field_index = 0U,
              .array_index = static_cast<std::uint32_t>(index),
            });
            const std::optional<std::uint64_t> nested_offset = decoded.byte_offset.has_value()
              ? std::optional<std::uint64_t>(field->field_offset + element_offset + *decoded.byte_offset)
              : std::nullopt;
            return ::quarry::runtime::decode_failure<Basket>(decoded.error, std::move(nested_path), nested_offset);
          }
          elements.push_back(*decoded.value);
          element_offset += element_length;
        }
        if (element_offset != field->bytes.size()) {
          return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::DecodeError::invalid_field_length, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset + element_offset);
        }
        if (!builder.set_elements(std::move(elements))) {
          return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
        }
      }
      if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 1U); field != nullptr) {
        std::size_t element_offset = 0U;
        const auto decoded_count = ::quarry::runtime::read_varuint(field->bytes, element_offset);
        if (!decoded_count.value.has_value() || *decoded_count.value > 2U) {
          if (!decoded_count.value.has_value()) {
            return ::quarry::runtime::decode_failure<Basket>(decoded_count.error, {{.field_index = 1U, .array_index = std::nullopt}}, field->field_offset);
          }
          return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 1U, .array_index = std::nullopt}}, field->field_offset);
        }
        const auto element_count = static_cast<std::size_t>(*decoded_count.value);
        std::vector<::alpha::one::Mode> elements;
        elements.reserve(element_count);
        const std::size_t element_width = 1U;
        const std::size_t remaining_bytes = field->bytes.size() - element_offset;
        if (element_width == 0U || element_count > remaining_bytes / element_width || remaining_bytes != element_count * element_width) {
          return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::DecodeError::invalid_field_length, {{.field_index = 1U, .array_index = std::nullopt}}, field->field_offset);
        }
        for (std::size_t index = 0U; index < element_count; ++index) {
          const auto decoded = ::quarry::runtime::read_u8(field->bytes.subspan(element_offset, element_width));
          if (!decoded.value.has_value()) {
            return ::quarry::runtime::decode_failure<Basket>(decoded.error, {{.field_index = 1U, .array_index = static_cast<std::uint32_t>(index)}}, field->field_offset + element_offset);
          }
          const auto enum_numeric = static_cast<std::int64_t>(*decoded.value);
          if (!(enum_numeric == 0 || enum_numeric == 1)) {
            return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::DecodeError::unknown_enum_value, {{.field_index = 1U, .array_index = static_cast<std::uint32_t>(index)}}, field->field_offset + element_offset);
          }
          elements.push_back(static_cast<::alpha::one::Mode>(enum_numeric));
          element_offset += element_width;
        }
        if (!builder.set_modes(std::move(elements))) {
          return ::quarry::runtime::decode_failure<Basket>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 1U, .array_index = std::nullopt}}, field->field_offset);
        }
      }
      return ::quarry::runtime::decoded_value<Basket>(builder.build());
    }


    std::optional<Basket> decode_Basket(std::span<const std::byte> input) {
      auto decoded = decode_Basket_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

} // namespace two
} // namespace beta