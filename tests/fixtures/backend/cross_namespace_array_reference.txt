=== generated/alpha/one.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: alpha.one

#include <cstddef>
#include <cstdint>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

namespace alpha {
namespace one {

    enum class Mode : std::int64_t {
      Off = 0,
      On = 1,
    };

    struct Element {
      Element() = default;

      bool has_id() const {
        return id_.has_value();
      }
      const std::uint32_t* id() const {
        return id_ ? &*id_ : nullptr;
      }

    private:
      friend class ElementBuilder;
      std::optional<std::uint32_t> id_;
    };

    class ElementBuilder {
    public:
      bool has_id() const {
        return id_.has_value();
      }

      bool set_id(const std::uint32_t& value) {
        if (!validate_id(value)) {
          return false;
        }
        id_ = value;
        return true;
      }

      Element build() const {
        Element value;
        value.id_ = id_;
        return value;
      }

    private:
      static bool validate_id(const std::uint32_t& value) {
        return true;
      }

      std::optional<std::uint32_t> id_;
    };

    std::optional<std::vector<std::byte>> encode(const Element& value) {
      std::vector<::breadcrumbs::runtime::FieldBytes> fields;
      fields.reserve(1U);
      if (value.has_id()) {
        std::vector<std::byte> field_bytes;
        if (!::breadcrumbs::runtime::append_u32(field_bytes, *value.id())) {
          return std::nullopt;
        }
        fields.push_back(::breadcrumbs::runtime::FieldBytes{
          .field_index = 0U,
          .bytes = std::move(field_bytes),
        });
      }
      return ::breadcrumbs::runtime::encode_record(1U, fields);
    }


    std::optional<Element> decode_Element(std::span<const std::byte> input) {
      const auto parsed = ::breadcrumbs::runtime::parse_record(input);
      if (!parsed.record.has_value() || parsed.record->record_id != 1U) {
        return std::nullopt;
      }
      ElementBuilder builder;
      if (const auto* field = ::breadcrumbs::runtime::find_field(*parsed.record, 0U); field != nullptr) {
        const auto decoded = ::breadcrumbs::runtime::read_u32(field->bytes);
        if (!decoded.value.has_value() || !builder.set_id(*decoded.value)) {
          return std::nullopt;
        }
      }
      return builder.build();
    }

} // namespace one
} // namespace alpha
=== generated/beta/two.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: beta.two

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "alpha/one.generated.hpp"
#include "runtime/binary_record.hpp"

namespace beta {
namespace two {

    struct Basket {
      Basket() = default;

      bool has_elements() const {
        return elements_.has_value();
      }
      const std::vector<::alpha::one::Element>* elements() const {
        return elements_ ? &*elements_ : nullptr;
      }
      bool has_modes() const {
        return modes_.has_value();
      }
      const std::vector<::alpha::one::Mode>* modes() const {
        return modes_ ? &*modes_ : nullptr;
      }

    private:
      friend class BasketBuilder;
      std::optional<std::vector<::alpha::one::Element>> elements_;
      std::optional<std::vector<::alpha::one::Mode>> modes_;
    };

    class BasketBuilder {
    public:
      bool has_elements() const {
        return elements_.has_value();
      }
      bool has_modes() const {
        return modes_.has_value();
      }

      bool set_elements(const std::vector<::alpha::one::Element>& value) {
        if (!validate_elements(value)) {
          return false;
        }
        elements_ = value;
        return true;
      }
      bool set_modes(const std::vector<::alpha::one::Mode>& value) {
        if (!validate_modes(value)) {
          return false;
        }
        modes_ = value;
        return true;
      }

      Basket build() const {
        Basket value;
        value.elements_ = elements_;
        value.modes_ = modes_;
        return value;
      }

    private:
      static bool validate_elements(const std::vector<::alpha::one::Element>& value) {
        if (value.size() > 2) {
          return false;
        }
        for (const auto& element_0 : value) {
        }
        return true;
      }
      static bool validate_modes(const std::vector<::alpha::one::Mode>& value) {
        if (value.size() > 2) {
          return false;
        }
        for (const auto& element_0 : value) {
        }
        return true;
      }

      std::optional<std::vector<::alpha::one::Element>> elements_;
      std::optional<std::vector<::alpha::one::Mode>> modes_;
    };

    std::optional<std::vector<std::byte>> encode(const Basket& value) {
      std::vector<::breadcrumbs::runtime::FieldBytes> fields;
      fields.reserve(2U);
      if (value.has_elements()) {
        return std::nullopt;
      }
      if (value.has_modes()) {
        if (value.modes()->size() > 2U) {
          return std::nullopt;
        }
        std::vector<std::byte> field_bytes;
        ::breadcrumbs::runtime::append_varuint(field_bytes, value.modes()->size());
        for (const auto& element : *value.modes()) {
          const auto enum_numeric = static_cast<std::int64_t>(element);
          if (!(enum_numeric == 0 || enum_numeric == 1)) {
            return std::nullopt;
          }
          ::breadcrumbs::runtime::append_u8(field_bytes, static_cast<std::uint8_t>(enum_numeric));
        }
        fields.push_back(::breadcrumbs::runtime::FieldBytes{
          .field_index = 1U,
          .bytes = std::move(field_bytes),
        });
      }
      return ::breadcrumbs::runtime::encode_record(2U, fields);
    }


    std::optional<Basket> decode_Basket(std::span<const std::byte> input) {
      const auto parsed = ::breadcrumbs::runtime::parse_record(input);
      if (!parsed.record.has_value() || parsed.record->record_id != 2U) {
        return std::nullopt;
      }
      BasketBuilder builder;
      if (::breadcrumbs::runtime::find_field(*parsed.record, 0U) != nullptr) {
        return std::nullopt;
      }
      if (const auto* field = ::breadcrumbs::runtime::find_field(*parsed.record, 1U); field != nullptr) {
        std::size_t element_offset = 0U;
        const auto decoded_count = ::breadcrumbs::runtime::read_varuint(field->bytes, element_offset);
        if (!decoded_count.value.has_value() || *decoded_count.value > 2U) {
          return std::nullopt;
        }
        const auto element_count = static_cast<std::size_t>(*decoded_count.value);
        std::vector<::alpha::one::Mode> elements;
        elements.reserve(element_count);
        const std::size_t element_width = 1U;
        const std::size_t remaining_bytes = field->bytes.size() - element_offset;
        if (element_width == 0U || element_count > remaining_bytes / element_width || remaining_bytes != element_count * element_width) {
          return std::nullopt;
        }
        for (std::size_t index = 0U; index < element_count; ++index) {
          const auto decoded = ::breadcrumbs::runtime::read_u8(field->bytes.subspan(element_offset, element_width));
          if (!decoded.value.has_value()) {
            return std::nullopt;
          }
          const auto enum_numeric = static_cast<std::int64_t>(*decoded.value);
          if (!(enum_numeric == 0 || enum_numeric == 1)) {
            return std::nullopt;
          }
          elements.push_back(static_cast<::alpha::one::Mode>(enum_numeric));
          element_offset += element_width;
        }
        if (!builder.set_modes(std::move(elements))) {
          return std::nullopt;
        }
      }
      return builder.build();
    }

} // namespace two
} // namespace beta
