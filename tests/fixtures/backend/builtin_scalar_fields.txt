=== generated/schema.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: <root>

#include <cstddef>
#include <cstdint>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

static_assert(::breadcrumbs::runtime::kGeneratedCodeApiVersion == 1U,
              "Generated Breadcrumbs code is incompatible with the installed Breadcrumbs runtime. Regenerate the code using a compatible breadcrumbs-schema-compiler release.");

struct Example {
  Example() = default;

  bool has_active() const {
    return active_.has_value();
  }
  const bool* active() const {
    return active_ ? &*active_ : nullptr;
  }
  bool has_count() const {
    return count_.has_value();
  }
  const std::uint32_t* count() const {
    return count_ ? &*count_ : nullptr;
  }
  bool has_ratio() const {
    return ratio_.has_value();
  }
  const double* ratio() const {
    return ratio_ ? &*ratio_ : nullptr;
  }

private:
  friend class ExampleBuilder;
  std::optional<bool> active_;
  std::optional<std::uint32_t> count_;
  std::optional<double> ratio_;
};

class ExampleBuilder {
public:
  bool has_active() const {
    return active_.has_value();
  }
  bool has_count() const {
    return count_.has_value();
  }
  bool has_ratio() const {
    return ratio_.has_value();
  }

  bool set_active(const bool& value) {
    if (!validate_active(value)) {
      return false;
    }
    active_ = value;
    return true;
  }
  bool set_count(const std::uint32_t& value) {
    if (!validate_count(value)) {
      return false;
    }
    count_ = value;
    return true;
  }
  bool set_ratio(const double& value) {
    if (!validate_ratio(value)) {
      return false;
    }
    ratio_ = value;
    return true;
  }

  Example build() const {
    Example value;
    value.active_ = active_;
    value.count_ = count_;
    value.ratio_ = ratio_;
    return value;
  }

private:
  static bool validate_active(const bool& value) {
    return true;
  }
  static bool validate_count(const std::uint32_t& value) {
    return true;
  }
  static bool validate_ratio(const double& value) {
    return true;
  }

  std::optional<bool> active_;
  std::optional<std::uint32_t> count_;
  std::optional<double> ratio_;
};

::breadcrumbs::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Example& value) {
  std::vector<::breadcrumbs::runtime::FieldBytes> fields;
  fields.reserve(3U);
  if (value.has_active()) {
    std::vector<std::byte> field_bytes;
    if (!::breadcrumbs::runtime::append_bool(field_bytes, *value.active())) {
      return ::breadcrumbs::runtime::encode_failure<std::vector<std::byte>>(::breadcrumbs::runtime::EncodeError::overflow);
    }
    fields.push_back(::breadcrumbs::runtime::FieldBytes{
      .field_index = 0U,
      .bytes = std::move(field_bytes),
    });
  }
  if (value.has_count()) {
    std::vector<std::byte> field_bytes;
    if (!::breadcrumbs::runtime::append_u32(field_bytes, *value.count())) {
      return ::breadcrumbs::runtime::encode_failure<std::vector<std::byte>>(::breadcrumbs::runtime::EncodeError::overflow);
    }
    fields.push_back(::breadcrumbs::runtime::FieldBytes{
      .field_index = 1U,
      .bytes = std::move(field_bytes),
    });
  }
  if (value.has_ratio()) {
    std::vector<std::byte> field_bytes;
    if (!::breadcrumbs::runtime::append_f64(field_bytes, *value.ratio())) {
      return ::breadcrumbs::runtime::encode_failure<std::vector<std::byte>>(::breadcrumbs::runtime::EncodeError::overflow);
    }
    fields.push_back(::breadcrumbs::runtime::FieldBytes{
      .field_index = 2U,
      .bytes = std::move(field_bytes),
    });
  }
  return ::breadcrumbs::runtime::encode_record_result(1U, fields);
}


std::optional<std::vector<std::byte>> encode(const Example& value) {
  auto encoded = encode_result(value);
  if (!encoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(encoded.value);
}


::breadcrumbs::runtime::DecodeResult<Example> decode_Example_result(std::span<const std::byte> input) {
  const auto parsed = ::breadcrumbs::runtime::parse_record(input);
  if (!parsed.record.has_value()) {
    return ::breadcrumbs::runtime::decode_failure<Example>(parsed.error);
  }
  if (parsed.record->record_id != 1U) {
    return ::breadcrumbs::runtime::decode_failure<Example>(::breadcrumbs::runtime::DecodeError::unexpected_record_id);
  }
  ExampleBuilder builder;
  if (const auto* field = ::breadcrumbs::runtime::find_field(*parsed.record, 0U); field != nullptr) {
    const auto decoded = ::breadcrumbs::runtime::read_bool(field->bytes);
    if (!decoded.value.has_value()) {
      return ::breadcrumbs::runtime::decode_failure<Example>(decoded.error);
    }
    if (!builder.set_active(*decoded.value)) {
      return ::breadcrumbs::runtime::decode_failure<Example>(::breadcrumbs::runtime::DecodeError::bounds_exceeded);
    }
  }
  if (const auto* field = ::breadcrumbs::runtime::find_field(*parsed.record, 1U); field != nullptr) {
    const auto decoded = ::breadcrumbs::runtime::read_u32(field->bytes);
    if (!decoded.value.has_value()) {
      return ::breadcrumbs::runtime::decode_failure<Example>(decoded.error);
    }
    if (!builder.set_count(*decoded.value)) {
      return ::breadcrumbs::runtime::decode_failure<Example>(::breadcrumbs::runtime::DecodeError::bounds_exceeded);
    }
  }
  if (const auto* field = ::breadcrumbs::runtime::find_field(*parsed.record, 2U); field != nullptr) {
    const auto decoded = ::breadcrumbs::runtime::read_f64(field->bytes);
    if (!decoded.value.has_value()) {
      return ::breadcrumbs::runtime::decode_failure<Example>(decoded.error);
    }
    if (!builder.set_ratio(*decoded.value)) {
      return ::breadcrumbs::runtime::decode_failure<Example>(::breadcrumbs::runtime::DecodeError::bounds_exceeded);
    }
  }
  return ::breadcrumbs::runtime::decoded_value<Example>(builder.build());
}


std::optional<Example> decode_Example(std::span<const std::byte> input) {
  auto decoded = decode_Example_result(input);
  if (!decoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(decoded.value);
}
