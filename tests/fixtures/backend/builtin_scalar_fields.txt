=== generated/schema.generated.hpp ===
// Generated by Quarry.
// Namespace: <root>

#include <cstddef>
#include <cstdint>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "quarry/runtime/binary_record_v2.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 3U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

inline const ::quarry::runtime::BrfV2RecordLayout& brf_v2_layout_Example() {
  static const ::quarry::runtime::BrfV2RecordLayout layout{
    .record_id = 1U,
    .presence_bitmap_size = 1U,
    .fixed_region_size = 14U,
    .complete_fixed_record_size = std::optional<std::uint32_t>{30U},
    .fields = {
      ::quarry::runtime::BrfV2FieldLayout{0U, 0U, 17U, 0U, 8U, 1U, ::quarry::runtime::BrfV2FieldStorage::Fixed, {}},
      ::quarry::runtime::BrfV2FieldLayout{1U, 1U, 18U, 0U, 32U, 4U, ::quarry::runtime::BrfV2FieldStorage::Fixed, {}},
      ::quarry::runtime::BrfV2FieldLayout{2U, 2U, 22U, 0U, 64U, 8U, ::quarry::runtime::BrfV2FieldStorage::Fixed, {}},
    },
  };
  return layout;
}

struct Example {
  Example() = default;

  bool has_active() const {
    return active_.has_value();
  }
  const bool* active() const {
    return active_ ? &*active_ : nullptr;
  }
  bool has_count() const {
    return count_.has_value();
  }
  const std::uint32_t* count() const {
    return count_ ? &*count_ : nullptr;
  }
  bool has_ratio() const {
    return ratio_.has_value();
  }
  const double* ratio() const {
    return ratio_ ? &*ratio_ : nullptr;
  }

private:
  friend class ExampleBuilder;
  std::optional<bool> active_;
  std::optional<std::uint32_t> count_;
  std::optional<double> ratio_;
};

class ExampleBuilder {
public:
  bool has_active() const {
    return active_.has_value();
  }
  bool has_count() const {
    return count_.has_value();
  }
  bool has_ratio() const {
    return ratio_.has_value();
  }

  bool set_active(const bool& value) {
    if (!validate_active(value)) {
      return false;
    }
    active_ = value;
    return true;
  }
  bool set_count(const std::uint32_t& value) {
    if (!validate_count(value)) {
      return false;
    }
    count_ = value;
    return true;
  }
  bool set_ratio(const double& value) {
    if (!validate_ratio(value)) {
      return false;
    }
    ratio_ = value;
    return true;
  }

  Example build() const {
    Example value;
    value.active_ = active_;
    value.count_ = count_;
    value.ratio_ = ratio_;
    return value;
  }

private:
  static bool validate_active(const bool& value) {
    return true;
  }
  static bool validate_count(const std::uint32_t& value) {
    return true;
  }
  static bool validate_ratio(const double& value) {
    return true;
  }

  std::optional<bool> active_;
  std::optional<std::uint32_t> count_;
  std::optional<double> ratio_;
};

::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Example& value) {
  std::vector<::quarry::runtime::FieldBytes> fields;
  fields.reserve(3U);
  if (value.has_active()) {
    std::vector<std::byte> field_bytes;
    if (!::quarry::runtime::append_bool(field_bytes, *value.active())) {
      return ::quarry::runtime::encode_failure<std::vector<std::byte>>(::quarry::runtime::EncodeError::overflow, {{.field_index = 0U, .array_index = std::nullopt}});
    }
    fields.push_back(::quarry::runtime::FieldBytes{
      .field_index = 0U,
      .bytes = std::move(field_bytes),
    });
  }
  if (value.has_count()) {
    std::vector<std::byte> field_bytes;
    if (!::quarry::runtime::append_u32(field_bytes, *value.count())) {
      return ::quarry::runtime::encode_failure<std::vector<std::byte>>(::quarry::runtime::EncodeError::overflow, {{.field_index = 1U, .array_index = std::nullopt}});
    }
    fields.push_back(::quarry::runtime::FieldBytes{
      .field_index = 1U,
      .bytes = std::move(field_bytes),
    });
  }
  if (value.has_ratio()) {
    std::vector<std::byte> field_bytes;
    if (!::quarry::runtime::append_f64(field_bytes, *value.ratio())) {
      return ::quarry::runtime::encode_failure<std::vector<std::byte>>(::quarry::runtime::EncodeError::overflow, {{.field_index = 2U, .array_index = std::nullopt}});
    }
    fields.push_back(::quarry::runtime::FieldBytes{
      .field_index = 2U,
      .bytes = std::move(field_bytes),
    });
  }
  return ::quarry::runtime::encode_brf_v2_record_result(brf_v2_layout_Example(), fields);
}


std::optional<std::vector<std::byte>> encode(const Example& value) {
  auto encoded = encode_result(value);
  if (!encoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(encoded.value);
}


::quarry::runtime::DecodeResult<Example> decode_Example_result(std::span<const std::byte> input) {
  const auto parsed = ::quarry::runtime::parse_brf_v2_record(input, brf_v2_layout_Example());
  if (!parsed.record.has_value()) {
    return ::quarry::runtime::decode_failure<Example>(::quarry::runtime::brf_v2_decode_error(parsed.error), {}, parsed.offset);
  }
  if (parsed.record->record_id != 1U) {
    return ::quarry::runtime::decode_failure<Example>(::quarry::runtime::DecodeError::unexpected_record_id, {}, 0U);
  }
  ExampleBuilder builder;
  if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 0U); field != nullptr) {
    const auto decoded = ::quarry::runtime::read_bool(field->bytes);
    if (!decoded.value.has_value()) {
      return ::quarry::runtime::decode_failure<Example>(decoded.error, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
    }
    if (!builder.set_active(*decoded.value)) {
      return ::quarry::runtime::decode_failure<Example>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
    }
  }
  if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 1U); field != nullptr) {
    const auto decoded = ::quarry::runtime::read_u32(field->bytes);
    if (!decoded.value.has_value()) {
      return ::quarry::runtime::decode_failure<Example>(decoded.error, {{.field_index = 1U, .array_index = std::nullopt}}, field->field_offset);
    }
    if (!builder.set_count(*decoded.value)) {
      return ::quarry::runtime::decode_failure<Example>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 1U, .array_index = std::nullopt}}, field->field_offset);
    }
  }
  if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 2U); field != nullptr) {
    const auto decoded = ::quarry::runtime::read_f64(field->bytes);
    if (!decoded.value.has_value()) {
      return ::quarry::runtime::decode_failure<Example>(decoded.error, {{.field_index = 2U, .array_index = std::nullopt}}, field->field_offset);
    }
    if (!builder.set_ratio(*decoded.value)) {
      return ::quarry::runtime::decode_failure<Example>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 2U, .array_index = std::nullopt}}, field->field_offset);
    }
  }
  return ::quarry::runtime::decoded_value<Example>(builder.build());
}


std::optional<Example> decode_Example(std::span<const std::byte> input) {
  auto decoded = decode_Example_result(input);
  if (!decoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(decoded.value);
}