=== generated/schema.generated.hpp ===
// Generated by Quarry.
// Namespace: <root>

#include <cstddef>
#include <cstdint>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "quarry/runtime/binary_record_v2.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 3U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

inline const ::quarry::runtime::BrfV2RecordLayout& brf_v2_layout_B() {
  static const ::quarry::runtime::BrfV2RecordLayout layout{
    .record_id = 2U,
    .presence_bitmap_size = 1U,
    .fixed_region_size = 5U,
    .complete_fixed_record_size = std::optional<std::uint32_t>{21U},
    .fields = {
      ::quarry::runtime::BrfV2FieldLayout{0U, 0U, 17U, 0U, 32U, 4U, ::quarry::runtime::BrfV2FieldStorage::Fixed, {}} ,
    },
  };
  return layout;
}

struct B {
  B() = default;

  bool has_count() const {
    return count_.has_value();
  }
  const std::uint32_t* count() const {
    return count_ ? &*count_ : nullptr;
  }

private:
  friend class BBuilder;
  std::optional<std::uint32_t> count_;
};

class BBuilder {
public:
  bool has_count() const {
    return count_.has_value();
  }

  bool set_count(const std::uint32_t& value) {
    if (!validate_count(value)) {
      return false;
    }
    count_ = value;
    return true;
  }

  B build() const {
    B value;
    value.count_ = count_;
    return value;
  }

private:
  static bool validate_count(const std::uint32_t& value) {
    return true;
  }

  std::optional<std::uint32_t> count_;
};

::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const B& value) {
  std::vector<::quarry::runtime::FieldBytes> fields;
  fields.reserve(1U);
  if (value.has_count()) {
    std::vector<std::byte> field_bytes;
    if (!::quarry::runtime::append_u32(field_bytes, *value.count())) {
      return ::quarry::runtime::encode_failure<std::vector<std::byte>>(::quarry::runtime::EncodeError::overflow, {{.field_index = 0U, .array_index = std::nullopt}});
    }
    fields.push_back(::quarry::runtime::FieldBytes{
      .field_index = 0U,
      .bytes = std::move(field_bytes),
    });
  }
  return ::quarry::runtime::encode_brf_v2_record_result(brf_v2_layout_B(), fields);
}


std::optional<std::vector<std::byte>> encode(const B& value) {
  auto encoded = encode_result(value);
  if (!encoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(encoded.value);
}


::quarry::runtime::DecodeResult<B> decode_B_result(std::span<const std::byte> input) {
  const auto parsed = ::quarry::runtime::parse_brf_v2_record(input, brf_v2_layout_B());
  if (!parsed.record.has_value()) {
    return ::quarry::runtime::decode_failure<B>(::quarry::runtime::brf_v2_decode_error(parsed.error), {}, parsed.offset);
  }
  if (parsed.record->record_id != 2U) {
    return ::quarry::runtime::decode_failure<B>(::quarry::runtime::DecodeError::unexpected_record_id, {}, 0U);
  }
  BBuilder builder;
  if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 0U); field != nullptr) {
    const auto decoded = ::quarry::runtime::read_u32(field->bytes);
    if (!decoded.value.has_value()) {
      return ::quarry::runtime::decode_failure<B>(decoded.error, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
    }
    if (!builder.set_count(*decoded.value)) {
      return ::quarry::runtime::decode_failure<B>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
    }
  }
  return ::quarry::runtime::decoded_value<B>(builder.build());
}


std::optional<B> decode_B(std::span<const std::byte> input) {
  auto decoded = decode_B_result(input);
  if (!decoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(decoded.value);
}

inline const ::quarry::runtime::BrfV2RecordLayout& brf_v2_layout_A() {
  static const ::quarry::runtime::BrfV2RecordLayout layout{
    .record_id = 1U,
    .presence_bitmap_size = 1U,
    .fixed_region_size = 22U,
    .complete_fixed_record_size = std::optional<std::uint32_t>{38U},
    .fields = {
      ::quarry::runtime::BrfV2FieldLayout{0U, 0U, 17U, 0U, 168U, 21U, ::quarry::runtime::BrfV2FieldStorage::InlineFixedNestedRecord, {}} ,
    },
  };
  return layout;
}

struct A {
  A() = default;

  bool has_value() const {
    return value_.has_value();
  }
  const ::B* value() const {
    return value_ ? &*value_ : nullptr;
  }

private:
  friend class ABuilder;
  std::optional<::B> value_;
};

class ABuilder {
public:
  bool has_value() const {
    return value_.has_value();
  }

  bool set_value(const ::B& value) {
    if (!validate_value(value)) {
      return false;
    }
    value_ = value;
    return true;
  }

  A build() const {
    A value;
    value.value_ = value_;
    return value;
  }

private:
  static bool validate_value(const ::B& value) {
    return true;
  }

  std::optional<::B> value_;
};

::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const A& value) {
  std::vector<::quarry::runtime::FieldBytes> fields;
  fields.reserve(1U);
  if (value.has_value()) {
    auto field_bytes = ::encode_result(*value.value());
    if (!field_bytes.has_value()) {
      auto nested_path = std::move(field_bytes.path);
      nested_path.push_back(::quarry::runtime::PathElement{
        .field_index = 0U,
        .array_index = std::nullopt,
      });
      return ::quarry::runtime::encode_failure<std::vector<std::byte>>(field_bytes.error, std::move(nested_path));
    }
    fields.push_back(::quarry::runtime::FieldBytes{
      .field_index = 0U,
      .bytes = std::move(*field_bytes.value),
    });
  }
  return ::quarry::runtime::encode_brf_v2_record_result(brf_v2_layout_A(), fields);
}


std::optional<std::vector<std::byte>> encode(const A& value) {
  auto encoded = encode_result(value);
  if (!encoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(encoded.value);
}


::quarry::runtime::DecodeResult<A> decode_A_result(std::span<const std::byte> input) {
  const auto parsed = ::quarry::runtime::parse_brf_v2_record(input, brf_v2_layout_A());
  if (!parsed.record.has_value()) {
    return ::quarry::runtime::decode_failure<A>(::quarry::runtime::brf_v2_decode_error(parsed.error), {}, parsed.offset);
  }
  if (parsed.record->record_id != 1U) {
    return ::quarry::runtime::decode_failure<A>(::quarry::runtime::DecodeError::unexpected_record_id, {}, 0U);
  }
  ABuilder builder;
  if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 0U); field != nullptr) {
    auto decoded = ::decode_B_result(field->bytes);
    if (!decoded.value.has_value()) {
      auto nested_path = std::move(decoded.path);
      nested_path.push_back(::quarry::runtime::PathElement{
        .field_index = 0U,
        .array_index = std::nullopt,
      });
      const std::optional<std::uint64_t> nested_offset = decoded.byte_offset.has_value()
        ? std::optional<std::uint64_t>(field->field_offset + *decoded.byte_offset)
        : std::nullopt;
      return ::quarry::runtime::decode_failure<A>(decoded.error, std::move(nested_path), nested_offset);
    }
    if (!builder.set_value(*decoded.value)) {
      return ::quarry::runtime::decode_failure<A>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}}, field->field_offset);
    }
  }
  return ::quarry::runtime::decoded_value<A>(builder.build());
}


std::optional<A> decode_A(std::span<const std::byte> input) {
  auto decoded = decode_A_result(input);
  if (!decoded.value.has_value()) {
    return std::nullopt;
  }
  return std::move(decoded.value);
}