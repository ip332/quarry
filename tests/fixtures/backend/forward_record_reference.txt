=== generated/schema.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: <root>

#include <cstddef>
#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

struct B {
  B() = default;

  bool has_count() const {
    return count_.has_value();
  }
  const std::uint32_t* count() const {
    return count_ ? &*count_ : nullptr;
  }

private:
  friend class BBuilder;
  std::optional<std::uint32_t> count_;
};

class BBuilder {
public:
  bool has_count() const {
    return count_.has_value();
  }

  bool set_count(const std::uint32_t& value) {
    if (!validate_count(value)) {
      return false;
    }
    count_ = value;
    return true;
  }

  B build() const {
    B value;
    value.count_ = count_;
    return value;
  }

private:
  static bool validate_count(const std::uint32_t& value) {
    return true;
  }

  std::optional<std::uint32_t> count_;
};

std::optional<std::vector<std::byte>> encode(const B& value) {
  std::vector<::breadcrumbs::runtime::FieldBytes> fields;
  fields.reserve(1U);
  if (value.has_count()) {
    std::vector<std::byte> field_bytes;
    if (!::breadcrumbs::runtime::append_u32(field_bytes, *value.count())) {
      return std::nullopt;
    }
    fields.push_back(::breadcrumbs::runtime::FieldBytes{
      .field_index = 0U,
      .bytes = std::move(field_bytes),
    });
  }
  return ::breadcrumbs::runtime::encode_record(2U, fields);
}

struct A {
  A() = default;

  bool has_value() const {
    return value_.has_value();
  }
  const ::B* value() const {
    return value_ ? &*value_ : nullptr;
  }

private:
  friend class ABuilder;
  std::optional<::B> value_;
};

class ABuilder {
public:
  bool has_value() const {
    return value_.has_value();
  }

  bool set_value(const ::B& value) {
    if (!validate_value(value)) {
      return false;
    }
    value_ = value;
    return true;
  }

  A build() const {
    A value;
    value.value_ = value_;
    return value;
  }

private:
  static bool validate_value(const ::B& value) {
    return true;
  }

  std::optional<::B> value_;
};

std::optional<std::vector<std::byte>> encode(const A& value) {
  std::vector<::breadcrumbs::runtime::FieldBytes> fields;
  fields.reserve(1U);
  if (value.has_value()) {
    return std::nullopt;
  }
  return ::breadcrumbs::runtime::encode_record(1U, fields);
}
