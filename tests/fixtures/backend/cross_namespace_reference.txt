=== generated/alpha/one.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: alpha.one

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

namespace alpha {
namespace one {

    struct First {};

    class FirstBuilder {
    public:
      First build() const {
        return First{};
      }
    };

    std::optional<std::vector<std::byte>> encode(const First& value) {
      std::vector<::breadcrumbs::runtime::FieldBytes> fields;
      (void)value;
      return ::breadcrumbs::runtime::encode_record(1U, fields);
    }


    std::optional<First> decode_First(std::span<const std::byte> input) {
      const auto parsed = ::breadcrumbs::runtime::parse_record(input);
      if (!parsed.record.has_value() || parsed.record->record_id != 1U) {
        return std::nullopt;
      }
      FirstBuilder builder;
      return builder.build();
    }

} // namespace one
} // namespace alpha
=== generated/beta/two.generated.hpp ===
// Generated by Breadcrumbs.
// Namespace: beta.two

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "alpha/one.generated.hpp"
#include "runtime/binary_record.hpp"

namespace beta {
namespace two {

    struct Second {
      Second() = default;

      bool has_first() const {
        return first_.has_value();
      }
      const ::alpha::one::First* first() const {
        return first_ ? &*first_ : nullptr;
      }

    private:
      friend class SecondBuilder;
      std::optional<::alpha::one::First> first_;
    };

    class SecondBuilder {
    public:
      bool has_first() const {
        return first_.has_value();
      }

      bool set_first(const ::alpha::one::First& value) {
        if (!validate_first(value)) {
          return false;
        }
        first_ = value;
        return true;
      }

      Second build() const {
        Second value;
        value.first_ = first_;
        return value;
      }

    private:
      static bool validate_first(const ::alpha::one::First& value) {
        return true;
      }

      std::optional<::alpha::one::First> first_;
    };

    std::optional<std::vector<std::byte>> encode(const Second& value) {
      std::vector<::breadcrumbs::runtime::FieldBytes> fields;
      fields.reserve(1U);
      if (value.has_first()) {
        return std::nullopt;
      }
      return ::breadcrumbs::runtime::encode_record(2U, fields);
    }


    std::optional<Second> decode_Second(std::span<const std::byte> input) {
      const auto parsed = ::breadcrumbs::runtime::parse_record(input);
      if (!parsed.record.has_value() || parsed.record->record_id != 2U) {
        return std::nullopt;
      }
      SecondBuilder builder;
      if (::breadcrumbs::runtime::find_field(*parsed.record, 0U) != nullptr) {
        return std::nullopt;
      }
      return builder.build();
    }

} // namespace two
} // namespace beta
