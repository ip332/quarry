=== generated/alpha/one.generated.hpp ===
// Generated by Quarry.
// Namespace: alpha.one

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "runtime/binary_record.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 2U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

namespace alpha {
namespace one {

    struct First {};

    class FirstBuilder {
    public:
      First build() const {
        return First{};
      }
    };

    ::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const First& value) {
      std::vector<::quarry::runtime::FieldBytes> fields;
      (void)value;
      return ::quarry::runtime::encode_record_result(1U, fields);
    }


    std::optional<std::vector<std::byte>> encode(const First& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::quarry::runtime::DecodeResult<First> decode_First_result(std::span<const std::byte> input) {
      const auto parsed = ::quarry::runtime::parse_record(input);
      if (!parsed.record.has_value()) {
        return ::quarry::runtime::decode_failure<First>(parsed.error);
      }
      if (parsed.record->record_id != 1U) {
        return ::quarry::runtime::decode_failure<First>(::quarry::runtime::DecodeError::unexpected_record_id);
      }
      FirstBuilder builder;
      return ::quarry::runtime::decoded_value<First>(builder.build());
    }


    std::optional<First> decode_First(std::span<const std::byte> input) {
      auto decoded = decode_First_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

} // namespace one
} // namespace alpha
=== generated/beta/two.generated.hpp ===
// Generated by Quarry.
// Namespace: beta.two

#include <cstddef>
#include <optional>
#include <span>
#include <utility>
#include <vector>

#include "alpha/one.generated.hpp"
#include "runtime/binary_record.hpp"

static_assert(::quarry::runtime::kGeneratedCodeApiVersion == 2U,
              "Generated Quarry code is incompatible with the installed Quarry runtime. Regenerate the code using a compatible quarry-schema-compiler release.");

namespace beta {
namespace two {

    struct Second {
      Second() = default;

      bool has_first() const {
        return first_.has_value();
      }
      const ::alpha::one::First* first() const {
        return first_ ? &*first_ : nullptr;
      }

    private:
      friend class SecondBuilder;
      std::optional<::alpha::one::First> first_;
    };

    class SecondBuilder {
    public:
      bool has_first() const {
        return first_.has_value();
      }

      bool set_first(const ::alpha::one::First& value) {
        if (!validate_first(value)) {
          return false;
        }
        first_ = value;
        return true;
      }

      Second build() const {
        Second value;
        value.first_ = first_;
        return value;
      }

    private:
      static bool validate_first(const ::alpha::one::First& value) {
        return true;
      }

      std::optional<::alpha::one::First> first_;
    };

    ::quarry::runtime::EncodeResult<std::vector<std::byte>> encode_result(const Second& value) {
      std::vector<::quarry::runtime::FieldBytes> fields;
      fields.reserve(1U);
      if (value.has_first()) {
        auto field_bytes = ::alpha::one::encode_result(*value.first());
        if (!field_bytes.has_value()) {
          auto nested_path = std::move(field_bytes.path);
          nested_path.push_back(::quarry::runtime::PathElement{
            .field_index = 0U,
            .array_index = std::nullopt,
          });
          return ::quarry::runtime::encode_failure<std::vector<std::byte>>(field_bytes.error, std::move(nested_path));
        }
        fields.push_back(::quarry::runtime::FieldBytes{
          .field_index = 0U,
          .bytes = std::move(*field_bytes.value),
        });
      }
      return ::quarry::runtime::encode_record_result(2U, fields);
    }


    std::optional<std::vector<std::byte>> encode(const Second& value) {
      auto encoded = encode_result(value);
      if (!encoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(encoded.value);
    }


    ::quarry::runtime::DecodeResult<Second> decode_Second_result(std::span<const std::byte> input) {
      const auto parsed = ::quarry::runtime::parse_record(input);
      if (!parsed.record.has_value()) {
        return ::quarry::runtime::decode_failure<Second>(parsed.error);
      }
      if (parsed.record->record_id != 2U) {
        return ::quarry::runtime::decode_failure<Second>(::quarry::runtime::DecodeError::unexpected_record_id);
      }
      SecondBuilder builder;
      if (const auto* field = ::quarry::runtime::find_field(*parsed.record, 0U); field != nullptr) {
        auto decoded = ::alpha::one::decode_First_result(field->bytes);
        if (!decoded.value.has_value()) {
          auto nested_path = std::move(decoded.path);
          nested_path.push_back(::quarry::runtime::PathElement{
            .field_index = 0U,
            .array_index = std::nullopt,
          });
          return ::quarry::runtime::decode_failure<Second>(decoded.error, std::move(nested_path));
        }
        if (!builder.set_first(*decoded.value)) {
          return ::quarry::runtime::decode_failure<Second>(::quarry::runtime::DecodeError::bounds_exceeded, {{.field_index = 0U, .array_index = std::nullopt}});
        }
      }
      return ::quarry::runtime::decoded_value<Second>(builder.build());
    }


    std::optional<Second> decode_Second(std::span<const std::byte> input) {
      auto decoded = decode_Second_result(input);
      if (!decoded.value.has_value()) {
        return std::nullopt;
      }
      return std::move(decoded.value);
    }

} // namespace two
} // namespace beta
